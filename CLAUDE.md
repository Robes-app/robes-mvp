# Robes MVP — Claude Code Reference

## ⚠ THIS BRANCH: `beta` — the working branch for beta.byrobes.com
Renamed from `claude/beta-deployment-setup-wdvxb6` (2026-07-12); this is the branch all beta work happens on going forward. It duplicates `signup-flow` and layers the **P0 simplification** (tech handoff 2026-07-11: wardrobe-first utility engine) on top, for live testing on beta.byrobes.com (point the Railway staging service's source branch at `beta`; point it back at `signup-flow` to restore the pre-P0 beta). Deltas vs `signup-flow`:
- **Prompt box is a bare text entry** — pills stripped, and the old photo/"From wardrobe" attach row is gone (beta feedback 2026-07-11); the bundle's typewriter rotating placeholder (its examples in `dashboard-assets/144529f6….js` `PROMPT_EXAMPLES`, now incl. a weekly line) is the sole intent guide. Attachment lives in the **+ menu** (`#cb-addmenu`): Upload / Take a picture / **From wardrobe** (`#cb-addopt-wa` → `__cbWardrobePick`). The bundle photo path feeds `_cbPhotoData` via a document-capture `change` listener on `#cb-file` (capture because `Dash.onPhoto` clears the input) + `_rbDownscale`; a wardrobe pick renders the bundle's `#cb-attached` chip and downscales via the shared `_rbUrlToDataUrl`. `window.__cbClearPhoto` clears state + chip. A photo attachment ALWAYS routes to the 'style' track (`_cbDetectIntent` early-returns on `hasPhoto` — no other track consumes the photo). `_cbSetIntent` survives as the concierge-card scaffold injector (Daily / Weekly cards); a card-armed intent is only trusted while the prompt still starts with the scaffold's static prefix (`_cbSeedPrefix`), otherwise detection re-runs.
- **Weekly Plan track is live and plan-first** (extension of the Daily Look, workflow borrowed from the Travel Edit): the weekly intent always opens the **week planner modal** (`__wkOpen`/`_wkPlanPaint`, `#wk-plan-modal`) — a Mon–Sun calendar (`_wkWeekDays`: upcoming Monday, labels "Monday · 14 Jul") where she assigns activities per day (input + smart chips), leaves a day blank (Robes plans it) or taps **"Leave free"** (`null` in `dayPlan` — NO outfit), with **"+ Plan next week too"** extending to 14 days and "Skip — let Robes plan the days" as the escape. `__wkPlanGo` → `_wkGenerate(brief, dayPlan, weekDays)` → **`POST /api/weekly`** (`WEEKLY_SCHEMA`, gemini-2.5-flash, thinking 0, 12000 tokens; owned pieces render their wardrobe photos, unowned pieces get generated stills since 2026-07-22 — see the Weekly imagery delta): the calendar is injected as an authoritative itinerary (her plan per day = authoritative, free days omitted and re-stamped positionally server-side as `{rest: true}`; `user_activity` carried per day). The result (`__wkRenderResult`, `#wk-result-page`) is the day strip `#wk-strip` (weekday + date, "· your plan" marker, dimmed left-free cards) + a **Daily-parity day console** in `#wk-day` (`_wkPaintConsole`): LEFT "The Look" dark stylist panel (slot-labelled tiles, first wide, hover flick arrows `.wk-tnav`, Anchored badges, "N of M from your wardrobe"), RIGHT "The Rack" (slot eyebrow, provenance, description, **flick-through** cluster + **Anchor** toggle per card). Flicking reuses the Daily helpers (`_dlOptions`/`_dlOptIndex`/`_dlApplyOption`) — option set = served original + owned same-category pieces (travel precedent, no AI alternates in the weekly schema). **"↻ Restyle this day"** and **"✎ The real plan"** / free-day **"✎ Dress this day"** (`#wk-day-modal`, incl. "Leave this day free") post **`POST /api/weekly/day`** (`WEEKLY_DAY_SCHEMA`, 2600 tokens) — a surgical single-day re-mix that holds ANCHORED pieces fixed and gets `weekSummary` for routing continuity; `_wkApplyDay` re-marks anchors on the fresh items. Every mutation (flick/anchor/restyle/free) persists via `_wkPatchSaved` into the saved lookbook entry (`_wkActiveSaveId`; `type: 'weekly-plan'`, `wkData`, label "Weekly plan", reopen via `__snOpenItem` with `{skipSave, savedId}`). `.rb-sfoot` footer: Back to dashboard / **Plan a new week** (reopens the planner with values kept — `_wkPlan` survives). The Weekly Planner card lost its "Coming soon" tag — CTA `_cbSetIntent('weekly')` injects "A weekly plan for [my upcoming work week]"; submit routes to the planner.
- **Intent routing is three-track** (daily / weekly / travel, + key-piece styling): `_cbDetectIntent` gained `weekly` (word-boundary `\bweek\b` never matches "weekend"), lost `moodboard`; precedence piece → travel → weekly → daily (a trip or week span outranks a piece packed inside it). The clarify loop offers the four options; an unroutable legacy intent falls back to clarify, never a blind render.
- **Moodboards fully hidden** behind `var _RB_MB_HIDDEN = true` (declared at the TOP of `__robes_personalize` — early code paths read it): no `#rb-mb` row, no avatar item (dashboard + stylenotes, Style notes slots after Lookbook), `/moodboards` + `/moodboard/:slug` deep links and popstate reopens gated. All moodboard code kept — flip the flag to restore.
- **"Your piece, styled" card is center stage + temporary** — mounted ABOVE the wardrobe tracker (prompt box → card → tracker), and **collapses off the DOM** (animated max-height, then `remove()`) the moment she interacts with it (Add next piece / See the full looks) or types into `#cb-ta`; the tracker snaps up into its slot. The result is already saved in the lookbook (`cardSaveId`). `mount()` guards on `collapsed` so a late paintReady can't resurrect it.
- **Post-add fork modal** (`window.__rbAddFork`) — after every successful non-edit wardrobe add (skipped when `_waAfterAdd` was armed by a swap/snap-mine flow; fires once at the END of a batch): "Style it 3 ways" (→ `_cbStyleSubmit` with the piece's photo re-fetched + downscaled via `_rbUrlToDataUrl`) or "Build today's look around it" (→ `__dlSubmit`).
- **All four result pages hide their siblings on render** (`_rbHideResultPages(except)`) — they're equal-z-index (40) fixed overlays, so DOM order (not open order) decides stacking; without the mutual hide a stale later-appended page paints over the one just opened.
- **Rollback caveat**: weekly plans sync to `lookbook_items` as `type: 'weekly-plan'`. The `signup-flow` build can't render that type — after pointing Railway back, any weekly rows saved during the test show as "Key piece"-labelled cards that dead-end on tap (harmless; delete the rows or redeploy this branch to fix).

### 2026-08-08 — Weekly track REMOVED (day-chip planning takes over; ADR-001)
Week planning moves to calendar day chips (separate brief) — this pass is the clean removal + safe landing only. **Supersedes every "Weekly Plan track" reference below**: the weekly artifact/console/engine surface is gone. Decision record: `architecture-decisions.md` ADR-001 (option A — sweep).
- **Deleted server-side**: `POST /api/weekly` + `POST /api/weekly/day`, `WEEKLY_SCHEMA`/`WEEKLY_DAY_SCHEMA`, the weekly-only helpers (`weeklyClosetBlocks`, `weeklyNormaliseItem`, `weeklyCategorySlot`, `weeklyTruncateItems`, `weeklyCoverageGaps`, `WEEKLY_ITEM_RULES`) and the weekly still-life image job; `publicSharePayload`'s `weekly-plan` branch. **`heroMark`/`heroDirective` survive** (daily + travel consume them) — they now live where `weeklyClosetBlocks` used to. `FORMULA_ROLES` survives (`TRAVEL_ROLES` aliases it). `/api/intent` lost the `weekly` enum value — its prompt now files a home-week plan as `unclear`, so the app asks instead of acting.
- **Deleted client-side** (dashboard-personalize.js): the entire `_wk*`/`__wk*` module (planner modal `#wk-plan-modal`, day modal `#wk-day-modal`, `#wk-result-page`, generate/imagery/console/evening/restyle/persist — ~1,000 lines), `_pdRowsWk` + the weekly `_pdSync`/`_pdSyncSaved` branches, `wkResultPage` in every overlay closer, the Lookbook "Weekly plans" tab + `__snOpenItem`/`_snTypeLabel`/`_shareBuild`/`__rbRename`/`_shareActiveEntry` weekly branches, the concierge **Weekly Planner card (removed from the DOM, cards renumber 01/02)**, the "Plan my week" chip/clarify options/suggestion pill, `_cbDetectIntent`'s weekly block (a week-shaped prompt now lands in the clarify loop), the intake's weekly commit + `_ikRestylePlan` + the whole plan-scope machinery (`_ikComputePlanScope`/`_ikAutoScope` — only the day chip exists now), DayCard/rail/month weekly branches (week chip, sage dot, "Open the week →", weekly bands). The bundle typewriter lost its weekly line (`PROMPT_EXAMPLES` in `144529f6….js`).
- **Precedence retiered**: `_pdTier` is now `look 3 > daily 2 > travel 1` (ties to latest `updated_at`, unchanged). `_pdSupersede` needed no change — it was always type-generic. `_rbTrackCfg`'s unknown-kind fallback moved weekly → **daily**. `_MS_UNLOCKS` dropped the 5-piece "Plans your week" milestone — the ladder is 3/10/15 (ticks 15/50/78%, fill interpolates through them; ftue_harness updated).
- **Existing data (ADR-001 option A — sweep)**: `_lbCloudPull` archives any `weekly-plan` row it pulls (`type` → `'weekly-plan-archived'`, blob kept recoverable in `data`) and deletes its `planned_days` rows via `_pdDeleteSource`; `snLoad()` filters `weekly-plan*` types so a cached row can never render or re-push. **Run `supabase/weekly_plan_sweep.sql` once** to settle dormant accounts in one shot. The backfill tools (`scripts/backfill_planned_days.mjs`, `supabase/planned_days_backfill.sql`) no longer emit weekly rows — re-running them can't resurrect swept data. `/admin` keeps both type labels for forensics.
- **Type set**: `lookbook_items.type` is now `key-piece · daily-look · travel-edit · moodboard` (+ the read-only `weekly-plan-archived`). No migration — the column is unconstrained text.

### 2026-08-08 — Spec E: mobile parity for the shared console (Look Template)
Same skeleton, one column, nothing dropped — a CSS block in `_RBC_CSS` (`@media(max-width:767px)`, the "E · Mobile parity" section) plus two markup hooks in the shared console. Verified: `looks_harness` **171/171** (4 new E checks at 390px), `travel_console_smoke` 44/44, `ftue_harness` 139/139, `addflow_harness` 102/102.
- **01 One header**: the rack's duplicate `.rbc-rackhead .ey` eyebrow and `h2` title hide ≤767px — the look head (and each page's masthead) carries eyebrow + name; the rack's `.rbc-hbtn` action chips stay as a wrapping row at ≥44px. The composer's name INPUT lives in the rackhead but is neither `.ey` nor `h2`, so it survives.
- **02 Single-line rows**: `.rbc-mslot` (new span in `_rbcRow`, hidden on desktop) prefixes the slot into `.rbc-sub`, which reorders ABOVE the name (`.rbc-body>div:first-child` goes flex; sub `order:-1`) — slot · status share the eyebrow. The image `vslot`/`vcount` pills and `.rbc-hownote` yield; thumb narrows to a 56px column; `.rbc-arrow` 44px, `.rbc-act` min-height 44px.
- **03 Share badge**: `_rbConsole` appends `.rbc-share-m` (34px circle, check-badge register, → `__rbShare`) as the board's last child whenever `cfg.lookActionHtml` exists — appended AFTER the tiles so the `data-n` nth-child scale rules never shift. Hidden on desktop; ≤767px it shows and the full-width `.rbc-action` hides, leaving the footer to the dock. The Look detail passes no `lookActionHtml` (Wear/Pin/Pack are its actions) and correctly gets no badge.

### 2026-08-07 (second pass) — Looks Refine, custom tags, ghost strips, drag-cast roles (Look Template follow-ups, Annie)
Four amendments on top of the A3 + F tranche below. Verified: `looks_harness` **162/162** (11 new checks — bar/Refine, "+ tag" door + vibe: prefix write, draggable rows + re-cast + look_pieces.role persistence, ghost strips + ink-in), `travel_console_smoke` 44/44, `ftue_harness` 139/139, `addflow_harness` 102/102.
- **Looks tab bar: + New look & Refine-by-tags.** The grid bar (`#rb-lk-bar`) now carries the sort toggle, a **Refine** pill (ink `hot` state while filters are live) and a primary **+ New look**. The Refine drawer (`.rb-lk-refwrap`) filters on the four look-tag axes — each axis lists its vocabulary plus any custom tags her looks carry (`_lkRefineOpts`); a look's read is its stored `tags`, else the inherited overlap of its pieces (`_lkTagsOf` — the same read the detail shows, so legacy looks filter too). Within an axis picks OR, across axes AND (`_lkMatchRefine`); nothing-matches renders a named state with a Clear link, never a bare empty grid. State `_lkRefine {climate[], light[], wear[], vibe[]}` + `_lkRefineOpen`.
- **F2 "+ tag" — Wear and Vibe are open axes.** The tag sheet's Wear-it-for and Vibe groups close with a dashed "+ tag" chip → inline input (`__rbTagAdd`/`__rbTagCommit`; Enter/blur commits, Escape cancels, 28-char cap). A typed value matching ANY axis's vocabulary (case-insensitive) selects that option instead — the disjointness guard. **Flat-array round-trip: a custom Vibe stores as `vibe:Foo`; an unknown plain string reads as a custom Wear tag** (`_rbTagsParse`/`_rbTagsFlat`). **HANDOFF: Climate and Light stay vocabulary-only — never open those axes**, they have no prefix and customs there would corrupt the recovery.
- **B1 amendment — ghosted formula strips are the composer's education layer.** ~~Empty rows under forecast strips~~ **superseded same day (Annie): a slot must NEVER forecast a role — her trousers can anchor, her top can be the exclamation.** The composer renders NO empty slot rows at all: the four canonical strips always render (`.rbc-rolestrip.ghost` until a piece lands in the group — education AND standing drop targets), and the ONE dashed **+ Add a piece** is the way in, opening the **A2 chooser** (`__lkAddOpen`/`_lkAddPaint`, `#rb-lkadd-sheet`): step 1 "What kind of piece?" — **Still open in this look** (the unfilled preset slots, shown ONLY once the look already holds a piece; an empty look goes straight to the categories) then **All categories** (the sheet's fifteen) — step 2 the wardrobe picker (slot pick keeps the slot's legacy-cat filter via `_lkRowOptions`; category pick filters at sheet level via `_waSheetCatOf`) + **Snap a new piece** (arms `_waAfterAdd` → `__lkApplyNew`, or `__lkRowSnap` for a slot pick). A ✕ on a filled row removes it — the slot returns to the chooser's Still-open list, never a bound placeholder. `_LK_START_ROWS`/`_lkRows` survive as internal placement state (presets fill first; `__lkRowOpen`/`_lkPickerHtml` inline pickers no longer render in the rack).
- **Drag & drop role casting on every rack.** `_rbRackRolesHtml` gained `cfg.onRoleDrop` (+ `cfg.roleCtx` for surfaces needing addressing) — rows wrap in `.rbc-dragrow[draggable]`, strips become drop targets, one delegated runtime (`_rbcDndInit`, HTML5 DnD — pointer devices; touch stays read-only for now). A drop on a row re-casts to that row's group. Handlers per surface: composer `__lkCRoleDrop` (draft `_lkNewRoles`, saved at `__lkSave`), Look detail `__lkDRoleDrop` (silent apply — presentational like a rename, no promotion gate), Daily `__dlRoleDrop` / Weekly `__wkRoleDrop` (set `it.role` on the blob item, existing patch persists), Travel `__tvRoleDrop` (sets the FORMULA entry's role so the cast carries the look everywhere it's pinned; a day-scoped add takes it on the capsule item). All casts validate through `_rbRoleNorm` and fire `role_cast {surface}`.
- **Migration 16** (`supabase/look_roles_migration.sql`, **NOT yet run on Robes_p0**): nullable `look_pieces.role`. Until it runs, `_lkPiecesCloud` strips the column on a PGRST204 naming it and retries (`_lkRoleCol`) — casts on saved Looks then live in the localStorage cache only. Null = heuristic decides at render; no backfill.

### 2026-08-07 — Formula roles + Look tags (Claude Design "The Look Template" handoff, spec A3 + F)
First tranche of the Look Template spec (`The Look Template.dc.html`, design chats 2026-08-06/07): the four dressing-formula roles surfaced as Rack strips, and the look-level tag intelligence layer. Verified: `looks_harness` **151/151** (7 new role/tag checks — strips lead rows, Canvas-before-Anchor order, tag row, sheet axes, PATCH shape), `travel_console_smoke` 44/44, `ftue_harness` 139/139, `addflow_harness` 102/102.
- **Formula roles are display-level casting, sourced from the generators** (spec A3: roles attach to PIECES, Robes casts them, she never files them): canonical set `_RB_ROLES` = The Canvas / The Anchor / The Texture / The Exclamation Point (display order per the spec's examples — Canvas leads). Sources by authority: Daily's step titles (stamped onto items at flatten; "The Accents" folds into "The Exclamation Point" via `_rbRoleNorm`), Travel's `formula[].role`, Weekly's new per-item `role` (schema enum `FORMULA_ROLES` — `TRAVEL_ROLES` now aliases it — emitted by /api/weekly + /api/weekly/day, validated in `weeklyNormaliseItem`, budgets 12000→13000 / 2600→2800), then the `_rbRoleGuess` slot/name heuristic for hand-built looks and legacy saves (a cap/scarf reads Texture, jewellery Exclamation). The Anchor ROLE and the Anchor ACTION stay one idea (founder call 2026-08-07, spec A3 note 03).
- **`_rbRackRolesHtml(items, cfg)` is the one rack painter**: regroups rows by role (stable within a group), heads each group with a `.rbc-rolestrip` hairline — used by `_rbConsole`, the Look detail rack and the composer (filled rows lead with strips; empty dashed rows follow UNLABELLED — roles are cast as pieces land, spec B1). Handlers ride `it.idx`, so regrouping never breaks a callback — but **rack row DOM positions are no longer stable; never assert nth-child on `.rbc-rack`** (the looks-harness flick check was rewritten name-based for exactly this).
- **Look tags — four axes on the LOOK, not the piece** (spec F2: thermal Climate ×1 / Light ×1 / Wear-it-for ≤2 / optional Vibe, `_RB_TAG_AXES`): a quiet `.rbc-tags` row on The Look panel (climate · agenda · vibe; Daylight stays silent, Twilight earns its chip; empty = "Untagged —" invitation, never a required step) opening the shared `__rbTagSheet` (one modal for every surface; a third Wear pick retires the oldest; Vibe is the only group allowed to stay empty). Rendered via `cfg.tagsHtml` on all five surfaces: Daily (`data.look_tags`), Weekly (day or `evening_look` holder by reference — `_wkTagHolder`), Travel (per look, `__tvTagsEdit(li)`), Look detail, composer.
- **Generated looks arrive tagged** (spec F3 auto-tag): `LOOK_TAGS_SCHEMA` + `LOOK_TAGS_RULE` + `normLookTags` in server.js; `look_tags` added to DAILY_SCHEMA (top level), WEEKLY day objects + WEEKLY_DAY_SCHEMA, and both travel look schemas — carried through every normaliser, refreshed on day restyle (`_wkApplyDay`) and evening dress, persisted in the lookbook blobs (`_dlPatchSaved` gained `look_tags`).
- **Hand-built looks inherit** (spec F3): `_rbInheritLookTags(pieceIds)` maps the pieces' coarse tags (dominant season → thermal range, occasions → agenda vocabulary; vibe is never inherited — only Robes suggests a vibe). The composer shows the inherited row live (`_lkNewTags` null = derived each paint) and stores at save; passive accrual and variant promotion seed tags too (the variant carries its ancestor's).
- **NO MIGRATION: saved-Look tags ride the existing `looks.tags text[]`** (migration 14 created it unused) — the four vocabularies are deliberately disjoint, so `_rbTagsParse` recovers the axes from a flat array. `_lkPatch`/`_lkCreate`/`_lkPushCloud` carry `tags`; an emptied sheet stores null, never `[]`. **HANDOFF RULE: keep the four tag vocabularies disjoint across axes** — overlap would corrupt the flat-array round-trip.
- **Deliberately NOT in this tranche** (next passes of the spec, in priority order): F1 piece-tag vocabulary refinement (two season ranges; Occasion folds into Evening & Event), A2's greyed "Shop similar via affiliate · Not ready yet" swap source ~~+ open-slots-first category chooser~~ (chooser shipped 2026-08-07, composer), D activity presets seeding composer slots, ~~E mobile share badge on the mosaic~~ (E shipped 2026-08-08).

### 2026-08-05 — Wardrobe add/edit + Refine redesign (fast add, one confirm screen)
From the Claude Design "Wardrobe add-edit" handoff (11 screens) + the locked product decisions. Cataloguing is one confirm screen with progressive disclosure, not a form to survive. Verified with the REWRITTEN `scripts/addflow_harness.mjs` (**102 checks green** — supersedes the 41-check reveal harness; the reveal itself survives) + regression runs: `ftue_harness` 139/139, `looks_harness` 144/144, `travel_console_smoke` 44/44.
- **One form, four doors** (`_waFormPaint` in the `_waSetupAutoTag` closure, state `_waForm {mode, view, photo, readable, revealed}`): the photo add, add-without-a-photo, batch pieces and the **edit modal** all render the same `.fm-step` form — the bundle form survives only as the hidden save bridge (`__waSawSubmit` → `#wa-hidden-form` → `WA.submit`, unchanged contract). Three views: **summary** (read-only "Here's what Robes saw" — photo with tag pops incl. L2/L3, label banner, a FOUR-row ledger The piece / Brand / Category / Colour-with-dot; tapping a row opens the editor on that field; the staged reveal still plays once per piece) ⇄ **details** ("Edit the details" — compact 64px-thumb header so nothing shows twice, fields The piece / Brand / Category / Subcategory / Item type / Colour) ⇄ **tags** ("Add tags and notes" — see the axes below). Manual/unreadable adds open on details with the "Add a piece." headline + photo slot; edit opens on details with `Update piece →` + a quiet "Remove from wardrobe" row (`_waOpenEdit` is now 4 lines — the patched `WA.open`'s edit branch calls `_showEditForm(it)` at 150ms).
- **Step 1 per screens 02-revised/10**: the dropzone is breakpoint-split (beta feedback 2026-08-05 — you can't take a photo from a desktop): **desktop is drop-first** ("Drop an image here" + one ink **Choose files** button, `.rb-wf-dt`), **≤767px keeps the snap/attach pair** ("Snap or attach the piece" + Take a photo / Attach photos, `.rb-wf-mb`) — **Take a photo goes STRAIGHT to the camera** via its own `#wa-rb-cam` input carrying `capture="environment"` (the one legitimate exception to the never-capture rule: a dedicated camera button, never the picker), **Attach-photos rides its own photos-only `#wa-rb-lib`** (`accept="image/*" multiple`, no extensions), and desktop Choose-files + the drop zone + every poke-the-input flow keep the capture-less `#wa-rb-file` (whose extension list fixes the macOS-Safari multi-select quirk). **iOS ALWAYS interposes its source sheet on non-capture file inputs — verified against Apple's forums 2026-08-06: `capture` is the ONLY bypass (camera), there is no library equivalent, and no accept/multiple combination suppresses the sheet. Do not burn time re-attempting a direct-to-library jump; Photo Library is the sheet's first row and that is the platform ceiling.** — plus a **From-a-link field** (coming-soon door) and **"Add without a photo"** (`__waManualStart`; its details view carries an "Add a photo" slot → `#rb-wf-photoin`, a straight attach with no re-scan). The `__waAddChooser` modal is deleted — step 1 IS the chooser; the name survives and now just opens `WA.open()` (wishlist branch kept).
- **No native OS dropdown for category** (locked decision 1): Category / Subcategory / Item type are custom pickers (`__waFieldPick` → `_waPopShell` — anchored popover on desktop, bottom sheet ≤767px) reading `_waTaxTree`; attributes are category-scoped (decision 2 — L2 list only ever holds the picked sheet L1's subcategories, the Sub/Type row doesn't render for tree-less categories or when the tree fetch degraded). **Colour is a single swatch + quiet Change** → `__waColourPop` palette popover (screen 07). `_buildSwatchRows`/`__rbPickSwatch`/`__rbInjectSwatches` are gone; `_waSwatchBg(name)` (wiring scope) is the one dot-renderer shared by the form, the popover and Refine.
- **Two tag axes, held apart** (decision 4): **Season** (sage tint) → `seasons[]`, **Wear it for** (rose tint; vocabulary **Work · Evening · Occasion · Travel · Active** + free "+ tag" chips) → `occasions[]`. `WA_OCCASIONS` holds that vocabulary. **The default state is DISPLAYED, never stored** (founder call 2026-08-06: the Refine rules must read back from the piece): an untagged piece shows **Year-round** and **Everyday** selected on the form, each default is exclusive with its axis's specific tags (picking one displaces it, emptying the axis re-selects it — an axis is never empty), and `WA.submit` strips a lone default back to **null** so the DB never stores it. A legacy stored 'Everyday' folds into the same default at prefill. State `window.__rbWaDetail = {seasons, wear, addingTag}`; Notes lives in the tags view.
- **The fast-add payload is the irreducible minimum** (decisions 3/6/7/9): `WA.submit`'s `_V2_KEYS` shrank to `['seasons','occasions']` — **price, fit_confidence, sentiment and hero_position are omitted from the payload entirely** (PATCH omission preserves old rows' values; the columns stay reserved), the grid-card **star is the one favouriting mechanic** (now ALWAYS visible: translucent ink circle off, solid ink on — hover-reveal dropped by design), and **Silhouette & fit is cut from the flow** — no pills anywhere in add/edit/Refine; the analyse still lands `silhouette_fit` in `item_dna` untouched. The taxonomy fold + bounded PGRST204 strip-and-retry loop are unchanged.
- **Browse cascade + trail row** (screens 01/09): a drillable category tab opens the **Subcategory › Item type cascade** (`__waCatTap` → `_waCascadeOpen/Paint` — two-column flyout under the tab on desktop, on mobile a bottom sheet where **every pick applies the filter and closes** — tap, filter, done (beta feedback 2026-08-05); reopening the tab's cascade lands on the drilled subcategory's item types with ‹ back. Both mobile sheets (cascade + Refine) size with `dvh` + `env(safe-area-inset-bottom)` so iOS Safari's bottom bar can't swallow the list). Filtering runs live via **`_waDrill {l2, l3}`** in `_waFilteredItems`. Below the tabs sits **`#rb-wg-trail`**: breadcrumb "Robes › Bottoms › Jeans › Barrel-leg jeans ✕" (✕ = `__waTrailClear`, clears the whole category trail; Refine picks survive) + italic serif live count + right-aligned **+ Add piece / Refine** buttons at every width — the **mobile FAB and the wardrobe "Pack a trip" pill are retired** (`__wgPack*` stays defined-but-doorless; packing lives in the travel intake).
- **Refine holds four things** (same-day design follow-up: the Wear-it-for tag was settable but not filterable — no way to ask for "everything I wear for work"): Season / **Wear it for** (an **Everyday** chip leads, then the context vocabulary + any free tags her pieces carry; filters on `occasions[]` with the **Year-round posture** — founder call 2026-08-05: untagged = Everyday via `_waItemWear`, and an Everyday piece passes ANY pick, she wears it for anything. the same Everyday chip now leads the form's Wear-it-for row too, selected on untagged pieces — display of the null state, stripped at save) / Colour (full 21-swatch palette, selected-names italic note; the colour WHEEL is gone) / Brand, with Clear all + "Show N pieces". DOM order is the mobile-sheet order (Season, Wear, Colour, Brand); desktop re-places via grid areas — Season | Colour | Brand on the first row, Wear it for full-width above the footer. Both tag axes keep their form tints in the filter (`.rb-ref-chip.sea.on` sage / `.ctx.on` rose). Fits, worn/never (the card's Never-worn badge carries that signal) and the L2/L3 chip sections are removed — `_waRefine = {seasons, colors, wear, brand}`. ≤767px the drawer renders as a fixed bottom sheet with its own "Refine ✕" header (`.rb-ref-mhead`) and a stacked full-width CTA over a centred Clear all.
- **Gotcha shipped with this**: the bundle's `WA.open` focuses `#wa-label-in` on a **220ms timer** — every `_waFormPaint` appends a hidden dummy `#wa-label-in` (the pattern steps 1/2 already used) or the edit path throws; at submit the dummy simply shadows the hidden-host copy for the label (same node written and read).
- **Two live bugs fixed with the beta test (2026-08-05)**: (1) **stale session token** — the dashboard boot captured `window.__robes_session` ONCE; supabase-js auto-refreshes the JWT (~hourly) and fires TOKEN_REFRESHED, so a long-open tab's copy expired and every REST write 401'd ("Something went wrong" on wardrobe saves). The boot's `onAuthStateChange` listener now re-captures the session on every event, and `WA.submit`'s catch names the session case ("refresh the page") instead of the generic line. (2) **Coming Soon under the modal** — `.cs` sat at z-index 300, the same layer as `.fm` (wa-modal) but earlier in the DOM, so the paste-a-link door opened invisibly underneath; `.cs` is now z-index 1000 (above every surface it can be opened from: fm 300, swap/hero sheets 950, form pickers 990).
- CSS: form/picker/popover styles in `#rb-wa-form-style` (injected by `_waFormCss`); trail/cascade/refine-sheet/star rules in the existing `#rb-wa2-style`.

### 2026-07-31 — 3-level wardrobe taxonomy (migration 15) — LIVE on beta
Pushed to `beta` (auto-deploys beta.byrobes.com) 2026-07-31. **Migration 15 has NOT yet been run on Robes_p0** — until it is, the degrade paths carry the release: saves strip `category_l2`/`category_l3` on PGRST204 (`_waTaxCols`), analyse's L2/L3 read simply isn't persisted, and the Refine Type section doesn't render; run `supabase/wardrobe_taxonomy_migration.sql` once in the Supabase SQL editor to switch the feature on. Categorisation goes three-level per the taxonomy sheet (350 item types, 55 subcategories, 14 sheet-level categories, compiled from Zara + Editorialist + ICP gap fills). **Columns, not jsonb** (the queried-across-pieces promotion rule): `wardrobe_items.category_l2` / `category_l3`, both nullable text — **null is the NORMAL state for every pre-migration piece, no backfill**. `category` stays the back-compat single-level L1 every existing surface reads.
- **`wardrobe_taxonomy.js` is the source of truth** (new root module beside `style_dna.js`): the full tree as `TAXONOMY_GROUPS`, plus `resolveTaxonomy(l2, l3)` (case-insensitive validation; unknown l3 under a known l2 degrades to the l2 alone, unknown l2 → null) and `taxonomyPromptBlock()` (one line per group, L3s pipe-separated). **The sheet's 14 L1s are richer than the legacy enum, so each (l2, l3) node carries the legacy `l1` it folds into** — Knitwear → Tops (except coatigan → Outerwear, knit dress → Dresses, knit trousers/skirt → Bottoms), Jewellery → Accessories, Tailoring splits per garment (suit blazer/waistcoat → Outerwear, suit trousers/skirt → Bottoms, full suits → Other), Activewear splits (tops/bottoms/jackets), sleep/underwear/co-ords → Other, Swim & beach incl. cover-ups → Swimwear. L2 names are unique tree-wide, so an L2 alone recovers its sheet L1 and legacy L1; nothing is lost by the fold.
- **Migration 15** (`supabase/wardrobe_taxonomy_migration.sql`) is **GENERATED** by `scripts/gen_taxonomy_migration.mjs` from the module — edit the module and regenerate, never the SQL. Adds the two columns, the `wardrobe_taxonomy` lookup table (pk `(l2, l3)`, cols `l1`/`sheet_l1`/`l2`/`l3`, RLS authenticated-read, 350 idempotent upsert seed rows), a **composite FK** `wardrobe_items(category_l2, category_l3) → wardrobe_taxonomy` (MATCH SIMPLE — a null l3 passes, so "l2 known, l3 unsure" stays writable; the server validates first, the FK is the backstop against prompt drift), and a partial index on `category_l2` (the Refine Type filter is the first consuming surface).
- **`/api/wardrobe/analyse` emits the pair**: `ANALYSE_SCHEMA` gained required `category_l2`/`category_l3` (empty string = no fit), the prompt carries the full tree via `taxonomyPromptBlock()` (~64 lines; `maxOutputTokens` 600 → 700). Server-side, `resolveTaxonomy` validates the emitted pair and **a valid pair DECIDES the legacy `category`** (the deterministic fold — `category` and `category_l2` can never disagree on a surface that filters both); an invalid pair degrades to the model's own single-level `category` with null L2/L3. All three response shapes (ok / noItemDetected / analysisFailed) carry the two keys.
- **Save paths carry the pair with the retry-without degrade** (acceptance: no surface breaks when migration 15 hasn't run): `WA.submit`'s single retry became a **bounded strip-and-retry loop** — on PGRST204 it strips the tier the error names (taxonomy cols are named in the message; otherwise the v2 fields), flips `_waTaxCols`/`_waV2Cols`, and retries, so a fresh project missing both migrations still lands the core row. Adds read `window.__waSawL2/L3` (stashed by `_runStep3`); **edits carry the row's saved pair forward** and a manual category pick that contradicts the filed L2 **drops the pair** (`_WA_TAX_L2` client map, L2 → legacy categories it spans — split L2s like Cardigans span two). Onboarding's insert and the legacy handoff persist retry once without the columns the same way.
- **Sheet-L1 display layer (same day — Annie: show the sheet's categories, and give L2/L3 their own fields)**: everywhere the USER reads or files a category, L1 is now the sheet's 14 (Knitwear, Tailoring & suiting, Jewellery…); the STORED `category` column stays the legacy enum via the fold, so swap pools, `_dlSlot`, generation payloads and old rows are untouched. Client statics in personalize: `_WA_SHEET_CATS` / `_WA_TAB_CATS` / `_WA_L2_SHEET` (L2 → sheet L1) / `_WA_LEGACY_TO_SHEET` (legacy → sheet, bijective on the 9) / `_WA_SHEET_DEFAULT_LEGACY` (L1-only pick → stored legacy) + `_waSheetCatOf(it)` (L2 wins, legacy mapping for pre-migration pieces). **`GET /api/wardrobe/taxonomy`** serves the full tree (1h cache) so `wardrobe_taxonomy.js` stays the single source of truth — client fetches once at boot into `_waTaxTree`; fetch failure degrades every new surface to the legacy single-level UI.
- **Piece metadata is a three-select cascade** — add step 3 AND the edit modal: CATEGORY (sheet L1s) › SUBCATEGORY (`#wa-saw-l2`) › ITEM TYPE (`#wa-saw-l3`), state `window.__waSawTaxSel {sheet, l2, l3}` with shared handlers (`__waTaxCatChange/L2Change/L3Change`, painter `_waTaxPaintSelects`; L1 change resets the pair, L3 disabled until an L2 is picked; cleared by `WA.close`). The edit modal rewrites the bundle `#wa-cat` options to sheet L1s and injects the same row after it. **`WA.submit` derives the stored legacy `category` from the picks via `_waTaxLegacy`** (per-L3 fold for split L2s — a coatigan filed under Knitwear › Cardigans STORES Outerwear; harness-pinned), so the sheet names never reach the column and `category`/`category_l2` can't disagree. The reveal ledger's Category row reads "{Sheet L1} · {item type}" and live-tracks re-filing.
- **Wardrobe filtering is sheet-level**: the category tabs are `_WA_TAB_CATS` (All + the 14 + Other) and the grid filters via `_waSheetCatOf` — a new jumper lands under Knitwear while a pre-migration jumper stays under Tops until re-filed (honest, no backfill). Refine's L2 section is labelled **Subcategory**, and a new **Item type** (L3) section sits beside it — both scoped to the active tab (item types additionally narrow to any selected subcategories), selected-elsewhere chips stay visible so they're deselectable; `_waRefine.types`/`.itemTypes` in `_waMatchRefine`/count/clear. Pre-migration wardrobes carry no L2/L3 → neither section renders. **Every other category surface (swap pools, `_dlSlot`, composer slot-matching, generation payloads, the travel key-piece browser) still reads legacy L1 only** — upgrade each deliberately, per the brief.
- Verified: `addflow_harness` 41/41 (incl. 9 taxonomy checks: cascade prefill, L1-reset, split-L2 fold on save, sheet tabs) + `ftue_harness` 139/139 green, module unit checks (fold overrides, case-insensitive resolve, unknown-l3 degrade), live analyse-route degrade shape with a dead Gemini key.

### 2026-07-30 — Travel Edit restructure: looks-first (the day-card restructure the Look entity was briefed to unblock)
The stylist insight (docs/stylist-reference-editorialist.md: "I don't start with a suitcase — I start with outfits") + beta feedback, rebuilt end to end. **A trip is looks pinned to days**, not a day-by-day grid: `/api/travel` now returns flat, day-agnostic `looks[{occasion,title,how,formula}]` built around the trip's high-level plans (Night in / Night out / Beach day / Pilates…), and the user pins each look to the days it suits. Supersedes the plan-first flow — the day planner modal, the day console, `✎ The real plan`, the Day/Evening slot pairs + travel evening verbs, the deferred-outfits path, the capsule-repoint flick and the stale-bar batched restyle are all gone, with `/api/travel/day` + `/api/travel/outfits` replaced by **`/api/travel/looks`** (occasion re-mix, ≤1 gap piece). See the Travel Edit section below for the full architecture.
- **The four new rules**: (1) the result page opens on **Looks**; "The Edit" pane is now **The Travel Rack**. (2) A day has no plan title of its own — a day is just the looks pinned to it, and unfilled days read *Free*. (3) **One look, many days** — 📌 Pin to days, tap Day 3 and Day 5, done; unplaced looks stay in the row. (4) **Adjustments know their scope** — a swap from a day defaults to "Only Day N" (`look.overrides[di][fi]`, badged "Swapped for Day N only"), the sheet also offers "All pinned days" (`look.slotOverrides[fi]`); the Travel Rack's card swap stays identity-everywhere. Both scopes point at capsule indexes, and a swapped-in wardrobe piece joins the case (`_tvCapsuleIndexFor`) so packing stays truthful — both pieces travel.
- **Intake (travel) — the unfurl is SLIM** (Annie's wireframe feedback, same day: "prompt should only unfurl to capture location, date & vibe before a dedicated landing page"): the travel unfurl carries ONLY the where / when / **vibe** crumbs (vibe = new `/api/intent` field — the styling mood the prompt itself carries, never invented; editable inline via `_ikVibeEdit`/`_ikVibeDone`, provenance-tracked) plus a quiet read-only "Plans noted, yours to place" chip strip when the classifier heard `day_intents` — no plan editing, no looks row, no build-around rack in the unfurl (the rack is gated off travel). Commit (`Next · Build the outfits →`) validates where + start date, assembles `_tvBrief {dest, dateFrom, dateTo, brief, vibe, anchors, plans, packLookIds}` and opens the **build page**. A Look's **`Pack it`** seeds both the pieces (`_lkPackSeed`) and the look itself (`_lkPackSeedLook`) into the intake/modal. Day-scoped travel prompts style a fresh look via `/api/travel/looks` and pin it to that day (`__tvStyleLookForDay`) — no more single-day restyle.
- **The build page** (`_tvShowBuildPage` → `#tv-build-page`, a dedicated full-page stage — z-40 fixed overlay in the app's own tokens, NOT a modal; the wireframe was structural guidance only): "The travel edit · step 2 of 2" / "Build the outfits." over Where/When/Vibe pill crumbs (each taps back into the step-1 modal via `__tvOpen({restore:true})`), then **plan chips** (`day_intents` pre-selected; `_TV_PLAN_DEFAULTS` + custom add), the **plan→look mapping list** ("Your looks, your plans" — see the bullet below; this replaced the independent "Looks you already love" tick-row) and **"Key pieces for the case"** (the key-piece browser with category pills + weather filter + Snap new, OPTIONAL — the 2-piece floor is gone, looks lead). `__tvBuildGo` requires ≥1 plan → `_tvGenerate()`. The overlay closers (`__rbCloseResultOverlays`/`_rbHideResultPages`/`__tvGoBack`) all drop it.
- **Plans are covered, not duplicated** (UX feedback same day: 4 plans + 4 ticked looks generated 8 looks — coverage was never declared): each selected plan row in `_tvPlanMapPaint` is either **assigned one of her saved Looks** ("✨ Robes styles it · use a saved look" opens an inline LookTile picker; assigned shows mini mosaic + "travels as styled" + ✕, `_tvBrief.planMap {planLabel: lookId}`) or left with Robes. A look already used by another plan shows dimmed "with {plan}" and picking it MOVES it (one look, one plan on the build page — pin-to-days multiplies later). Deselecting a plan chip drops its mapping. `_tvGenerate` splits: mapped looks import with `occasion = plan` and their plans go up as **`coveredPlans`**; only the unmapped rest go as `plans`. Unmapped Pack-it seeds still travel whole (occasion "Packed whole", quiet strip with "map it to a plan above" + ✕ via `__tvLookPackToggle`).
- **Modal path** (concierge card / wardrobe pack-a-trip / moodboard): step 1 = trip fields (dest / dates / brief); `__tvSubmit` → the same **build page** — one build stage for every door.
- **`_pdRowsTv` is pins-aware**: first pinned look per date → slot `day`, second → slot `evening` (the index caps a date at two slots), unpinned dates → free rows; legacy day-blob and deferred branches kept for old saves. DayCard strip + home rail ride the same rows.
- **Migration on render** (`_tvMigrate`): each old day slot becomes a look pinned to its day (evening slots keep an "· evening" occasion); old deferred saves (`days: []`) become zero looks → a "Style the looks →" invite through `/api/travel/looks`. `_tvPatchSaved` persists `capsule + looks + tripDays`, clears legacy `days`, and re-syncs `planned_days` — **this also fixes a live bug: `_tvPatchSaved` was called 12× but never defined on this branch**, so travel mutations threw and never persisted or re-indexed.
- `_rbSwapModal` gained `cfg.headHtml` (the scope control + "Back to the styled piece" ride the shared sheet); look cards and the intake/build-modal look rows compose `window._rbLookTile` per the handoff rule. ~~Deliberately NOT the "The Look" console~~ — **superseded same day** by Annie's UX pass ("we do not want to lose this beautiful functionality"): the shared console came back, see the next bullet.
- **The console returns + the result re-tabs (UX feedback, same day)**: the result tabs are now the lookbook's Grid|Calendar segment — **Days | Looks | The Rack** (`.tvm-seg`, plain uppercase labels with counts; legacy `'outfits'`→`'days'`, `'edit'`→`'rack'`; opens on Looks per rule 1). **Tapping a day card unfolds the weekly-style console** — `_tvPaintDayConsole` → `_tvConsoleFor(li, di)` → the shared `_rbConsole` Look panel + Rack in a `.tv-con` grid (mirrors `#wk-day .wk-con`); a day holding several pinned looks tabs them inside the panel exactly like the Day/Evening switcher (`__tvDaySetLook`, occasion labels). **Tapping a look card unfolds the same console dayless** (`_tvPaintLookConsole`; pins line replaces the switcher). **The capsule flick is restored** (`_tvConFlip` — cycles the case's same-category members, applied at the current scope: from a day → `overrides[di][fi]`, from the Looks tab → `slotOverrides[fi]`; landing back on the look's own piece clears the override — harness-pinned). Rack rows: `_rbcProvenance` + the scope badge, formula ROW NOTE, Pack/Packed or `+ Add` as the third action, Swap → the scoped sheet (`__tvDayConSwap`/`__tvLookConSwap` → `__tvLookSwap`); head buttons Unpin-from-this-day / Pin-to-days + Pack this look. **Imported looks are fully interactive too** (follow-up feedback, same day: "it should all be exactly the same") — `_tvMigrate` resolves each imported look's pieces to REAL capsule formula entries (match by wardrobe id, then exact label; an unpacked piece joins the case with its `wardrobe_match`), so saved-Look imports ride the identical console, flick, scoped swap, Pack and 1:3 maths; `pieces` now carry `{id, name, image, category}` from `_tvGenerate`, and the "Packed whole from your looks" line survives as the panel quote when the look has no `how`. `_tvLookBlock` remains only as the formula-less fallback. Free days invite "✦ Style a look for this day →" (`__tvStyleLooks({pinTo})`). State: `_tvSelDayI`/`_tvSelLookI`/`_tvDayLookIdx` replaced `_tvSel`; `_tvPaintDetail` survives as a repaint shim over both consoles so every existing funnel (`__tvPinSync`, `__tvPackToggle`) keeps working. No anchor on travel (no consumer — the restyle verb is `/api/travel/looks`).
- **A look is always adjustable, and the adjustment knows its scope** (follow-up, same day — the worked example: Look 01 pinned to 1 Aug is Wear A `{a·b·c·d}`; pinned to 10 Aug with the shoes swapped and a blazer added is Wear B `{a·b·c·e, f}` — ONE look, worn twice; neither pure immutability (version explosion, kills "the Thursday one, worn 11×") nor pure mutation (editing rewrites five days of history)): the console racks gained **remove** (row ✕ / swipe) and **"+ Add a piece"** (the shared `__rbcAddMenu` chooser — wardrobe pick joins the case via `_tvCapsuleIndexFor`, photo/upload arms `_waAfterAdd`). **Day scope** writes `look.dayAdds[di] = [ci…]` (extra case pieces, badged "Added for Day N only" — flick/swap repoint the add in place, the sheet hides the scope segment) and `look.dayDrops[di] = [fi…]` (slot hidden that day alone); **look scope** mutates `formula` (push / splice — the splice REMAPS every fi-keyed map: `slotOverrides`, each day's `overrides`, `dayDrops`, or later slots point at the wrong piece). `_tvLookEntries` is the one resolver (drops filtered, adds appended with synthetic `fi = formula.length + k`); console handlers are ROW-indexed and re-resolve entries at call time. Unpinning a day deletes its overrides + dayAdds + dayDrops; `_pdRowsTv` mirrors the day's effective pieces so the rail/wear log carries Wear B's blazer. Two-piece floor at the current scope. **Promotion stays explicit** — a travel variant never mints a Look entity; "Save as a new look" on the Look detail is the only door (offered-not-applied, entity-level).
- Travel harness rewrite: first tranche shipped as **`scripts/travel_console_smoke.mjs`** (44 checks green — seg tabs, day/look consoles, multi-look switcher, scoped flick + badge isolation, the worked example (day add/drop isolation, look-scope add, remove-with-remap), free day, imported-look console parity + case-join, rack pane, legacy-save migration); the remaining Stage 3/5 sections are still superseded and flagged.

### 2026-07-30 — Look as a saved entity (migration 14; brief `docs/look-entity-brief.md`, Claude Design handoff `Looks.dc.html`)
Wardrobe was a catalogue of pieces with no catalogue of **looks** — a generated outfit was a position inside a plan blob, with no identity, no persistence and no way to wear it again. This lands Phase 1 (the entity, accruing from wears she already confirms) + Phase 2 (manual build). Verified with `scripts/looks_harness.mjs` (**111 checks green** at 1280px + 390px: primitive, tab, detail, wear/undo write shapes, promotion, composer, degradation) + regression runs of `ftue_harness` (139) and `addflow_harness` (32).
- **Three objects, never one** (brief §2): `Look` is mutable and keeps its identity through small edits; a `Wear` is `{look, these exact pieces, this date}` and is **immutable**; the DayCard is presentation. `supabase/looks_migration.sql` has **no update policy on `wears`** and a unique `(user_id, look_id, worn_on)` — corrections are delete-and-recreate, and confirmation is idempotent so a double-tap or a re-synced blob can't inflate the count "wear more, buy less" is measured on. `wears.piece_ids` is a **snapshot**: the detail's history keeps showing the sandals a look was worn in after the look moved to slides (harness-pinned).
- **`LookTile` is the shared primitive, extracted BEFORE the tab shipped** (brief B2 sequencing): `_ltTile` / `_ltStripHtml` / `_ltMosaicHtml` / `_ltCells` / `_ltToneOf`, exposed as `window._rbLookTile`. **`_dcCard` now composes it** for the title and the thumb strip and owns only day context (eyebrow, evening line, membership chip, states, ring). The class PREFIX is a parameter — `dc` on day surfaces, `lt` on look surfaces — so DayCard's markup is **byte-identical** to its pre-extraction output and the DayCard harness's reference strings still hold. **HANDOFF RULE: any surface that draws a look composes `window._rbLookTile`. Do not build a second look card** — two components rendering the same content on different surfaces is the `planned_days` divergence in another costume.
- **Wardrobe gains a tab, not a destination** (A1): the existing `#rb-wsub` row is now **All pieces | Looks | Wishlist** (`__waSetView('looks')`, `/looks` route + deep link, crumb `Wardrobe › Looks`). The panel header still reads *Your wardrobe*; nav stays Wardrobe / Lookbook. Grid is `_ltTile` mosaics — **title only**, count on hover (printed at ≤767px where there is no hover). One control: a **Last worn ↓ / First worn ↑** toggle (never-worn falls to the END descending, the FRONT ascending — the never-worn question in one tap). `_waV2Sync` builds the surface BEFORE its visibility pass reads it; without that the first switch left the tab blank behind its own CSS default.
- **Look detail is the live console's components end to end** (UX-review pass, 2026-07-30 — the first cut drew the mock's own components; Annie: this was meant to be an enhancement of what exists): The Look renders through `_rbConsole` as the **standing 4:5 composition** (`rb-lookv2` board, note in the quote slot, palette, canonical ownership line) or the look's photograph at the same ratio; **"The Rack" is `_rbcRow` rack rows** — flick cycles her same-category pieces, **Swap opens `_rbSwapModal`** — and BOTH routes go through the promotion gate when the look has history (a flick on a worn look asks Update / Save-as-new, never applies silently). **Streamline pass (same day, Annie: Look/Daily parity + mobile)**: the **name leads the page** — `.rb-lk-mast` (eyebrow + serif title + the app's `rb-rename-tbtn` **pencil** → inline input via `__lkTitleEdit`/`_lkTitleEditing`; a standing input read as a form and buried the name below the stacked Look panel on mobile) sits ABOVE the `.rb-lk-con` grid with the action row; the card list is titled **The Rack** (was "The pieces"; Daily's `rackTitleHtml` occasion h2 was dropped — the masthead already names the look, never repeat it under the rack); **stats moved BELOW the Rack**; actions are THREE — `Wear it today` · `Pin to a day` · `Pack it` — **`Swap a piece` dropped** (redundant: every rack row swaps), and `.rb-lk-act` restyled to the console `rbc-hbtn` register. Stats are pieces / wears / last worn / **cost per wear, shown only when priced pieces and a wear both exist**. Titles are **offered, not applied** (A6). **Looks are deletable** — hover-✕ on grid tiles (the lookbook's `sn-rm` pattern) + a quiet "Delete this look" on detail, both through `_rbConfirmDelete`; FK cascade takes `look_pieces` + `wears`, `_pdDeleteSource` sweeps pins. **Tile metadata prints** (hover-reveal was reverted — in this app hover reveals actions, never information). **Notice panels are hairline + cream, never ink-bordered** — ink borders mean *selected* (anchored, active day).
- **A tap IS the wear, with a quiet undo on the card** (A4, C1 — undo-on-tap): no confirm dialog, no toast. Undo DELETEs the wear and decrements `times_worn`. **Passive accrual (B3) is wired into all three existing wear paths** — `_dcMarkWorn` (flagged), `__rbRailWear` (`?daycard=off`) and `__dlWear` (daily console): `window.__lkAccrue(pieceIds, …)` creates the Look if that exact composition isn't one yet, then writes the Wear, and OWNS `times_worn` for wears it records (each caller falls back to its own patch loop when accrual declines — already logged today, or fewer than two owned pieces). Identity is the **exact piece set**, never a similarity threshold.
- **Variant promotion is explicit or it doesn't happen** (A5/B5): swapping a piece on a look **with history** opens Update this look / Save as a new look / Leave it, and nothing is applied until she picks. Update mutates composition and says the wears stand; promote writes a new row carrying `origin_look_id` + `source:'variant'` and never touches the ancestor's `look_pieces` (harness-asserted). A look with no history just takes the edit.
- **The composer IS the live console** (Phase 2; rebuilt same day — Annie: *"the entire functionality should carry across, we are not reinventing the wheel"*, after a first cut shipped a bespoke card/rack at the wrong scale): `_rbConsole` draws The Look at the ratified 480px column (`body.rb-lookv2`, board tiles populating as pieces land), `_rbcRow` draws every filled rack card — the same flick cluster (cycling her same-category pieces), the same **Swap through `_rbSwapModal`** (wardrobe candidates by category + Snap mine, post-add hook landing the new piece in the row), the same corner ✕ (`onRemove`; a core slot returns to its dashed placeholder, an added row drops). The only bespoke markup is the dashed empty-slot row (`.rbc-row.rb-lk-rempty`, picker inside the row) — a state no generated console has. `+ Add a piece` is the shared `__rbcAddMenu` chooser; its apply (`__lkApplyNew`) fills the first empty slot that takes the piece's category, else appends. **Copy (Annie, 2026-07-30): rack eyebrow is "The Rack"; the name field is EMPTY with placeholder "Name your Look" (no "Robes suggests"/"Named by you" subtext)** — A6 still holds at save: an untouched field saves the offered name marked provisional. A dress in the Top slot quietly retires the Bottom row. **No extraction anywhere**: a photo is the look's image and fills the panel, hosted via `/api/wardrobe/upload` first so base64 never reaches a row. Handler idx→row mapping rides `_lkConMap`, rebuilt on every paint. **A slot never dead-ends** (Annie's shoe, same day): every inline picker — composer empty rows AND the detail's swap list — carries a dashed "New piece" tile, and an EMPTY category renders an invitation + "Add a piece" instead of "Nothing else in that category yet"; both arm `_waAfterAdd` and open the standard wardrobe add modal (`WA.open`, same poke-the-file-input pattern as `__rbcAddSnap`), so the freshly filed piece lands straight in the slot. The Looks grid ends with the same amplified `.rb-add-card` the pieces grid uses ("+ New look") — the CTA must not exist only in the empty state.
- **Pin writes `planned_days` with `source_type:'look'`** — the pin store is the index, so a pinned look shows on the home rail and month calendar through the same DayCard. `_pdTier` gives `'look'` the top tier (an explicit pin of a named look is the most deliberate statement about a day; it only ever wins on the index-fed surfaces, since the weekly/travel artifacts derive their strips from their own blob), `_rbOpenPlannedDay` branches to the Look detail (source_id is a uuid, never a numeric lookbook id), and `_dcPieceTotal` resolves the total from the look.
- **Migration 14 gotcha, hit on the first run**: Robes_p0 already carried an **unused, empty `public.looks`** table from an earlier experiment (nothing in the client, server or edge functions read it). `create table if not exists` skips silently against it, and the first policy then fails with a bare `42703: column "user_id" does not exist` — an error that names nothing useful. The migration now opens with a **preflight `do $$` block** that raises a sentence instead, and refuses to proceed if a foreign `looks` table exists. The guarded drop used to clear it (aborts rather than destroying data, so it is safe to paste):
  ```sql
  do $$
  declare n bigint;
  begin
    if to_regclass('public.looks') is not null then
      execute 'select count(*) from public.looks' into n;
      if n > 0 then raise exception 'public.looks holds % row(s) — not dropping.', n; end if;
    end if;
    drop table if exists public.wears cascade;
    drop table if exists public.look_pieces cascade;
    drop table if exists public.looks cascade;
  end $$;
  ```
- **Deliberately NOT built**: garment extraction from a photo (Phase 3 — must not gate 1 and 2); the Travel Edit day-card restructure (blocked on this, briefed separately) — `Pack it` instead routes to the trip intake with the look's pieces as the shortlist (`window._lkPackSeed`), per the standing rule that every route lands on the home prompt. **The wireframes' explanatory copy was not carried over** (founder instruction, 2026-07-30): the design doc's object-model cards, the 01/02/03 empty-state columns, "A wear is a fact, so it needs a date", the immutability footnotes — all annotation, not product copy. The empty state is a headline, ONE line and two ways forward.

### 2026-07-29 — Wardrobe logging: the onboarding reveal, every time
**Partially superseded 2026-08-05 (add/edit redesign above)**: the reveal survives as the read-only summary view (4-row ledger, no Cut row), but the collapsed full form, `__rbSawExpand`, the swatch wall and the fit pills are gone — see the 2026-08-05 delta for the current anatomy.
In-app cataloguing (step 3 of the add flow) was a full review form — 21-swatch picker, silhouette pills, notes, all expanded — which read as info overload beside onboarding's filing "magic" (the scan + labelled tags that wow every demo). `_runStep3` (in `_waSetupAutoTag`, `dashboard-personalize.js`) now plays the ONBOARDING reveal on every add; the form survives intact but collapsed. Verified with `scripts/addflow_harness.mjs` (**30 checks green**: reveal anatomy, collapse/expand, live sync, save payload, unreadable path, 2-piece batch) + a 139-check FTUE regression run.
- **The reveal**: photo panel with staggered tag pops (category / editorial colour / brand / fit — same spots and pop curve as onboarding's `kpTagPop`, here `rbSawPop`), the serif label banner, then the 5-row "What Robes files" ledger (The piece / Category / Brand / Colour / Cut) filling row by row ("Reading" → value, 420ms steps) — pure theatre, the analysis already landed in step 2. CSS injected once as `#rb-wa-saw-style` (`.rb-saw-*`); Retake is now an overlay chip on the photo.
- **Details collapse behind one row**: "✎ Adjust the details" (`#rb-saw-toggle` → `window.__rbSawExpand`) expands `#rb-saw-edit`, which holds the ENTIRE old form — label/category/brand, the swatch rows, fit pills, notes, and the `#rb-wa-detail-host` progressive-capture expander. Every field exists in the DOM from first paint (just hidden), so the `window.__waSaw*` state + `__waSawSubmit` hidden-bundle-form bridge is byte-for-byte unchanged.
- **The ledger never lies**: edits in the expanded form live-sync their ledger row and the banner (`window.__rbSawSync`; swatch picks sync via a delegated click listener on `#rb-saw-edit` reading `__waSawColor` a tick later; pill removal syncs via `_renderFitPills(true)` — the initial render passes no flag so the Cut row can't pre-paint ahead of the reveal).
- **Unreadable piece = no theatre**: `!tag.label` skips tags + ledger and opens the editor expanded with the give-it-a-name copy; the empty-label submit guard calls `__rbSawExpand(true)` before focusing, since the name field now lives inside the collapsible.
- Untouched: steps 1–2, batch add (each piece replays the reveal, details re-collapse, "PIECE n OF m" tag carries), edit mode (the full bundle form on revisit is correct there — revisiting IS editing), and all of `WA.submit`.

### 2026-07-29 — Learning meter (Clodagh test: "15 read as the wardrobe's item limit")
The wardrobe section is reframed from a **completion meter to a learning meter** — Clodagh read "1 / 15" as a cap on how many pieces the wardrobe holds. All in the `_ms*` block of `dashboard-personalize.js` + the `#wtrk` markup/CSS in `dashboard.html`; the harness pins every rule below.
- **No denominator, anywhere.** `#wtrk-num` is a bare count with a small-caps "Piece(s) filed" beneath (`.wtrk-num-lbl`, block span — the old `.wtrk-num span` "/ 15" rule is gone). The wardrobe empty state's "0 / 15" is now "0 · Pieces filed"; the concierge Daily pill dropped its "· n/15" tail; the styled-card nudge dropped "of 15". **HANDOFF RULE: never render the count as a fraction (`n / 15`, `n of 15`, `n/15`) on any wardrobe surface — the denominator is what created the limit read.** `_WA_TARGET` (15) survives as the last milestone + card-retire threshold only, never as a rendered denominator.
- **The track no longer ends.** `_msBarHtml` is scaled to `_MS_SCALE = 20`: ticks sit at each unlock's explicit `pos` (15/25/50/78%), the fill interpolates piecewise through those positions (`_msFillPct` — so it meets each tick exactly at its count; a straight n/20 would underrun the nudged 78% last tick), and the track's last quarter fades to near-transparent via a CSS mask on `.rb-ms-track` (75% → 5% alpha). At 15 pieces the fill reaches 78% — **there is no state where the bar reads full. HANDOFF RULE: the track must never fill to 100% at the last milestone.**
- **Milestones name capability, not features**: `_MS_UNLOCKS` entries gained `cap` — **Dresses today / Plans your week / Packs your trips / Knows your taste** — which is what the bar columns render; `label` (Daily look / Weekly planner / …) survives for the derived headline + gating copy. Section eyebrow is a static "Robes is learning" (the JS `Your wardrobe`/`Wardrobe completion` toggle is gone — "completion" was the frame being retired).
- **No lock language**: the headline's earned branch reads "{Feature} just opened." (was "unlocked"), the wardrobe empty-state header reads "What more pieces bring" (was "What pieces unlock"). **HANDOFF RULE: features are never locked and copy never says lock/unlock** — this restates the app-wide never-lock convention at the copy level so it can't regress in build.
- Harness updated in step (`scripts/ftue_harness.mjs`): capability labels, tick positions, piecewise fill, fade mask, bare-count + no-denominator + eyebrow assertions per count, pill no-fraction at 3–14, empty-state header/no-unlock checks — **139 checks green** (supersedes the 110 count below).

### 2026-07-28 — Onboarding was bypassed for a second signup in the same tab
`rb_onboarded` was a **single unscoped sessionStorage key**, set when any account finished onboarding and never cleared. `onboarding.html` checks it **before** the `onboarded_at` lookup, so a tab that had already onboarded one account sent the *next* account signed up in it straight to `/dashboard` — the flow skipped entirely, and the dashboard gate read the same flag so it never bounced back. The new user's `onboarded_at` was correctly `null` the whole time; nothing ever read it. Hit by anyone testing repeated signups in one browser tab.
- Same bug class as the lookbook's `__anon` keys — **an unscoped browser key leaking across accounts**. Fixed the same way: `rb_onboarded__<uid>` in both readers (`onboarding.html`, `dashboard.html`) and the writer, plus a `removeItem('rb_onboarded')` on the way past so a tab still carrying the legacy global recovers on next load.
- The flag keeps its real job: a loop guard **for that same user** when the `onboarded_at` write failed.
- **Open risk, not yet fixed:** `supabase/onboarding_migration.sql` ends with an unguarded `update … set onboarded_at = coalesce(created_at, now()) where onboarded_at is null;`. The migration list above invites re-running "so a fresh project (or a missed one) can be reconciled" — **re-running that file stamps every pending user as onboarded, permanently**. Guard it before the list is replayed again.

### 2026-07-28 — Intake rack thumbnails, Cancel label, desktop multi-select
- **Intake rack thumbnails were stretched**: `#rb-intake .ik-ri .im` is a `<span>` with no `display`, so it stayed **inline — and `aspect-ratio` does not apply to non-replaced inline elements**. Inside `.ik-rack` (a flex row, default `align-items:stretch`) each tile grew to the tallest sibling, so a long two-line label stretched every image with it (measured 82×108 instead of 82×82). Fixed with `display:block` on `.im` plus `align-items:flex-start` on the rack. **Any `aspect-ratio` box that is a `<span>` needs an explicit `display`** — this pattern recurs across the injected racks.
- **`Never mind` → `Cancel`** on both intake action rows (`.ik-cancel`): on a config panel the soft dismissal read as optional rather than as the way out.
- **Desktop multi-select hardening**: `#wa-rb-file` already carried `multiple`, and every add entry point (tracker CTA / fast-add / add tile / grid card / panel button / empty-state CTA) was verified to open a multi-select picker and queue a 3-file batch in Chromium — the reported failure could **not** be reproduced there, so this is browser-specific (Safari/macOS the likeliest). Two known Safari quirks pre-empted: the wildcard-only `accept="image/*"` gained explicit extensions (`.jpg,.jpeg,.png,.heic,.heif,.webp`), and the input moved off `display:none` to a visually-hidden clip rect, since Safari has historically not reliably activated fully-hidden file inputs inside a `<label>`. If it still fails, get the browser + version before changing more.

### 2026-07-28 — Homescreen FTUE (Claude Design handoff: "Guided homescreen, empty to unlocked")
**Partially superseded 2026-07-29 (Learning meter delta above)**: the bar's presentation — ticks/fill scale, "n / 15" numeral, milestone labels, "Wardrobe completion" eyebrow — changed; the thresholds, gating, module order and card-retire rules below still hold. The first-time run is now a single milestone ladder. **One config drives everything**: `_MS_UNLOCKS = [{at:3,daily},{at:5,weekly},{at:10,travel},{at:15,styleNotes}]` in `dashboard-personalize.js` — re-tune the thresholds there and the progress card, the wardrobe empty state and the concierge gate all follow. No other gating flag. Verified with `scripts/ftue_harness.mjs` (boots the real dashboard against a Supabase stub at n = 0/1/3/5/10/15/16 plus both empty states, the first-load race and 390px): **110 checks green**. Run it with `npm i --no-save playwright && node scripts/ftue_harness.mjs` (set `CHROME_PATH` if playwright's bundled browser build isn't installed).
- **The progress card is the hero** (`#wtrk`): the plain bar + kicker/copy are replaced by the shared milestone bar (`_msBarHtml`) — ticks at 3/5/10/15 and four labelled columns. **No per-tick status text** (2026-07-28, founder call): "Locked" would be a lie since every edit is built and open, "n away" repeats the headline, and earned is already carried by the tick + numeral going dark — the headline states, the bar shows. **The card is height-budgeted**: numeral and label share one line, the CTA sits inline in the top row, and the section holds ~365px at 1280px against ~353px for the pre-FTUE tracker. The harness asserts `<= 380px` so it can't silently balloon again. The headline is **derived, never authored** (`_msHeadline`): it names the next unlock and the distance to it, or what was just earned at a boundary. Fill is `itemCount/15` on a 650ms `cubic-bezier(0.4,0,0.2,1)` (disabled under `prefers-reduced-motion`). **"Just unlocked" is a one-session label** — `_msTrack` seeds `_msPrevCount` from `rb_ms_last__<uid>` so a threshold crossed between sessions still reads as earned on the first render back. **The card retires at ≥16, not ≥15** (changed): at 15 it still has a job — Style Notes has only just unlocked — and from 16 count + adding live in the Wardrobe tab, with nothing in its place.
- **Module order splits on the first unlock** (`_rbFtueOrder`, idempotent, runs on every `_waSyncCounts`): below 3 pieces the progress card leads directly under the greeting; from 3 the prompt leads again as it did before, preserving the 4.7 mobile above-the-fold call. `#rb-rail` travels with `.concierge` in both; `#rb-styled` always sits immediately above the tracker, which is where its own `mount()` puts it, so the two never fight.
- **The concierge is absent below 3 pieces** (`_rbGateConcierge`) — a brand-new user is never shown a shelf of edits she has nothing to feed. From 3 the section appears with **all three edits open**: Daily Look, Weekly Planner and Travel Edit are all built, and the app-wide never-lock convention holds (the design's 5/10-piece locks were deliberately NOT implemented — founder call).
- **Style Notes is introduced once, at 15** (`_rbSilPrompt`, now a re-runnable function rather than a one-shot timeout, since the count lands async): absent below the threshold, then a **dark card** — the only dark surface on home, so the introduction reads as an arrival rather than another nudge. Still gated on missing `style_dna` chapters and dismissible per user.
- **Weather is deliberately UNCHANGED.** The mock's strip was placeholder art, not a spec (founder call, 2026-07-28) — an FTUE pass that promoted it to every width was reverted. Desktop keeps the nav pill (`#nav-weather`); under 768px the nav pill hides and `.dash-wx-m` carries icon · day · city · temp under the greeting. Don't re-derive this from the mock.
- **Wardrobe empty state** loses its prose block (too text heavy on the emptiest screen — this supersedes the copy approved 2026-07-27) and gains the **same milestone bar**, `status:false`, under a "What pieces unlock" header, plus two decorative `.rb-ghost-card` tiles behind the live add card.
- **Lookbook empty state** gains "Ways to fill it" (`_snPaintWays`): it **clones the real `.svc` concierge cards** off the dashboard rather than hand-rolling a second set of rows — one design, one place to change it, imagery included. **Cloning is gated on `.svc-daily`** — the marker personalize leaves when it relabels/reorders the cards at 600ms — with a bounded 150ms retry and a repaint hook fired at the end of that transform. Keying on the transformed DOM rather than a timer is what makes it deterministic: any open that beat 600ms used to paint the legacy bundle trio ("Key piece, three ways", photo imagery, wrong order) and only corrected on refresh. Until the transform lands the bare empty state shows; it never paints legacy. Each clone is stripped of its inline `onclick` (cloneNode carries the bundle's modal-opener ATTRIBUTE even where personalize overrode the property) and its `.rb-lock-wrap` progress pill, then wired to `window.__snWay(intent)` → close the page → `_cbSetIntent(intent)`. **Every route lands on the home prompt box** — the empty Lookbook never opens a modal of its own (founder call 2026-07-28: the hand-rolled version "opens old paths to logging"). `_CHIP_DEFS` gained a `travel` scaffold purely so `_cbSetIntent('travel')` can arm the prompt; no chip renders it. Removed the moment anything is saved.
- **Deliberately not built** (the mock predates this branch): the mock's three-tile week rail — `#rb-rail` already IS that module, wired to `planned_days` and sharing the DayCard renderer, so it was left untouched; the guided anchor-chip prompt builder — the live box (textarea + `+` menu + chips) stays at every count, founder call.

### 2026-07-28 — Gender identity setting (migration 13)
"How do you identify?" lives in the Account details modal as a **collapsible field** (set once, then rarely revisited — collapsed by default, header shows the current value): Woman / Man / Prefer not to say. **Every new signup defaults to `woman`** via the `profiles.gender_identity` column default (`supabase/gender_identity_migration.sql`, migration 13 — ADD COLUMN backfills existing rows too), and the value normalises to `'woman'` at every read (`_rbGender()` client-side, `normGender()` server-side), so a pre-migration profile behaves exactly as before.
- **Server** (`server.js`): `genderDirective(g)` replaces the six hard-coded "Unless the brief clearly indicates a male wearer, style for a woman." system-prompt lines (`/api/daily`, `/api/weekly`, `/api/weekly/day`, `/api/travel`, `/api/travel/day`, `/api/travel/outfits`) and is injected into `/api/alternates` + `/api/moodboard`; `/api/style` keeps its richer woman-default sentence and swaps in the directive for the other two values. **`man` = menswear only, everywhere** — male cuts/sizing/brands, never womenswear; the directive also tells the model the prompts' generic "she"/"her" refers to this male client (the stylist prompts stay she-voiced rather than rewriting every pronoun per-request). **`unspecified` ("Prefer not to say") = model's best judgement from the brief**, gender-neutral when nothing points a direction. Imagery follows: `wearerNoun`/`wearerWears` drive the Daily anchor shot and Travel hero ("one man… He wears" / "one person… They wear"); `/api/style`'s `wearer` field is forced to `man` under the man setting, otherwise the model's judgement stands (so frames match the looks it wrote); the moodboard `hero_looks` rules name the model's gender. Weekly/other stills are garment-only still-lifes — no change needed.
- **Client** (`dashboard-personalize.js`): `_rbGender()` (by `_rbStyleDna`), sent as `gender` on all ten generation POSTs (style ×3 incl. the onboarding-handoff quietStyle, daily, weekly, weekly/day, travel, travel/day, alternates, moodboard). The account modal's save PATCHes `gender_identity` and **retries without the column when the migration hasn't run** (error body names the column), so the rest of the form still saves; the in-memory `__robes_profile` syncs on success. Selected pill = warm cream `#F3EFE6` + `#C9BCA6` border + ✓ per the app convention. The dashboard boot select gained `gender_identity` with its own retry tier (gender → onboarded_at → bare).
- Onboarding needs no change — the column default covers the `handle_new_user` insert. Style Notes analysis/try-on (her own photo, identity-preserving) is deliberately untouched.

### 2026-07-24 — DayCard convergence (design spec v2.0 + engineering brief; audit `docs/daycard-audit.md`)
One canonical day renderer across the four day surfaces — home rail, weekly strip, travel strip, lookbook calendar. All code in the **DayCard module** in `dashboard-personalize.js` (right after `_rbDayStrip`): `_dcCard(data, opts)` (the renderer, CSS `#rb-dc-style`, classes `.rb-dc`), `_dcMoments(dayM, eveM, o)` (the normaliser — planned_days-shaped moments → DayCardData `{eyebrow,title,evening,pieces,pieceTotal,chip,state,modifier,pinned}`), plus the peek overlay + `_rbSetFocus`. **Flag: default ON, `?daycard=off` is the per-device kill switch** (localStorage `rb_daycard`; every surface falls back to its exact pre-DayCard render — harness-verified parity). Verified with a Playwright harness (Supabase shim + REST stubs): **65 checks green** at 1280px + 390px covering all four surfaces, peek actions, ghost-band supersession, flag-off parity and the mobile treatments.
- **Live-pass polish (Annie, same day)**: (1) **No ring on the strips** — body-tap already lands the console on the day, so the "Focus day" sparkle read as a second, different action; the ring survives only on the home rail where it genuinely differs from the body (peek vs prompt-scope). `_rbSetFocus` keeps its weekly/travel branches for programmatic use. (2) **Mobile shows 2 thumbs** — 3 + the `+N` chip bled off narrow cards; the 3rd thumb carries `.t3` (hidden ≤767px) and a mobile-only `.ov.m` chip re-counts against 2 shown (desktop keeps 3 + `.ov.d`); `.rb-dc` also gained `overflow:hidden` as a belt. (3) **Mobile calendar compaction** — compact cells were 150px slivers with bleeding thumbs at 390px (the DayCard path had no <768px rules, unlike legacy `.rb-mc`): ≤1000px cell thumbs hide, ≤767px cells go square (aspect 1, 5px pad), the evening text line + footer status hide and a ☾ `.dc-evemark` carries the two-moment signal.
- **One data path (§6.4 decision)**: rail + calendar read `planned_days` as before; the **strips now derive DayCardData LIVE from the plan blob through the SAME row builders the index uses** (`_wkStripV2` → `_pdRowsWk`, `_tvWeekV2` → `_pdRowsTv`) — one shape, no freshness gap, the index stays persistence-only. Old saves that can't emit dated rows (no `week_iso`/`dateFrom`) fall back to the legacy strip automatically.
- **Action model (spec §6)**: body opens, ring sets focus, no "Open →" labels anywhere. Rail/calendar body-tap → **day peek overlay** (`__rbDayPeek`, `#rb-dpk` — Annie's §6.2 call: day + evening moments at a glance, "Open the day →" click-through to `_rbOpenPlannedDay`, and **"Wore it ✓"** for past days via `_dcMarkWorn`, which repaints via `_rbRailRepaint` — a soft in-place repaint, because a refetch would revert the worn stamp until the debounced upsert lands). Strip body-tap lands the console on the day (`__wkOpenDay`/`__tvOpenDay` — inside the artifact the console IS the day's open view); the top-right **ring** (`.dc-ring`, sparkle) scopes the prompt on home (`__rbRailFocus` → `_ikScopeDay`) and re-points the console on the strips (`_rbSetFocus`). The rail's old flag-dependent body branch (`__rbRailTap`) survives only on the `?daycard=off` path. Empty/free cards fold into ONE empty state ("Nothing yet. / Tap to dress it." — component rule) whose tap aims the prompt (`__rbRailScope`).
- **Title config point (audit D-02, Annie: user's words win)**: `_DC_TITLE_MODE = 'user'` — `_dcTitleOf` prefers `activity` (her words) over `headline` (the rewrite), sentence-cased via `_dcSentence` (first letter only — never lowercase user text, proper nouns). `_wkApplyDay`'s occasion overwrite is untouched; her words surface anyway because the row builders prefer `user_activity`. Flip to `'robes'` to invert everywhere.
- **Slots per spec v2.0**: eyebrow (full = uppercase Inter; compact = serif numeral; travel = `DAY N`, no calendar date), evening line `Evening · {occasion}` with word-boundary truncation (`_dcTruncate`; the `Evening · Evening look` placeholder renders as bare "Evening" — §11.3), **thumbnails 3 + `+N` overflow** (from the LEAD moment only so the denominator matches the visible look; `_dcPieceTotal` resolves the total from the parent blob via the lookbook cache — `item_ids` is owned-only and can't carry it, D-03; **the `{n} yours` ownership count is DROPPED by product decision — do not resurrect**), membership-only chip (`_dcChipOf`: trip/week; **a daily look carries NO chip**; suppressed on strips — the artifact masthead carries it), states `empty-past/empty/past/today/planned` (+ calendar-only `void` for out-of-month — now DISTINCT from past-empty, §7) with modifiers `worn`/`packed` (footer status only) + the `pinned` ring kept from the rail. `_DC_THUMB_MODE = 'swatch'` restores the palette whisper if §10.1 reopens. Weekly strip drops "your plan" + `{n} yours`; travel strip drops "2 looks" and gains the `Packed ✓` modifier.
- **Ghost bands fixed (audit D-08, unconditional — not behind the flag)**: `_pdSupersede` — two same-type sources with **overlapping date ranges** are the same plan re-planned → only the freshest survives WHOLESALE (per-date `_pdFreshest` couldn't drop a rescheduled trip's rows on dates the fresh trip no longer covers). Applied in `_pdSlots` (rail), `_mvFresh` (calendar) and `comingUp` (its select now carries `updated_at`). Daily looks exempt (day+evening siblings coexist by design). Band lane assignment now **sorts segments by start column** before the greedy pass (was source-discovery order). Post-fix, >2 bands per column is near-impossible, so the `+N more` overflow is defensive: under the flag it's a button → popover of the hidden bands (`__mvMore`, `#rb-mv-pop`).
- **Calendar rhythm (§11.2)**: under the flag every week row reserves the SAME two band lanes (`padTop` constant), so band-carrying rows no longer sit taller — the "blank hero slot" the audit dismissed was this reservation varying per row. Cells are compact DayCards (min-height 150, no aspect-ratio); `worn` renders on the calendar (the §10.4 proposal, shipped as proposed — flip by not passing the modifier if it reopens).
- **Dead CSS removed (audit D-12)**: the never-emitted `.tvm-day` rules (a fossil from before the strips were unified onto `_rbDayStrip`).
- Rollback: `?daycard=off` per device, or set `_DC_FLAG_KEY` default; the legacy renders are all intact behind the branch points (`cardHtml`, `_wkPaintStrip`, `_tvPaintWeek`, `_mvPaint`, `__mvCell`).

### 2026-07-24 — The Diary Stage 6: the continuous spine (handoff Phase 6 — month view)
Month view lives INSIDE the Lookbook (no `/calendar` route — out of scope §9): a **Grid | Calendar** segmented toggle (`#sn-viewseg`) in the lookbook header; Calendar swaps the tabs/grid for `#sn-cal` (month serif title + ‹ › nav, `__mvNav`) via a `rb-cal-on` class on `#sn-page` (class, not inline styles — `snRenderPage` after `_lbCloudPull` re-sets the grid's inline display underneath). The lookbook always reopens on Grid. All code in the `_rbMonthView` IIFE (after `_rbRail`); CSS `#rb-mv-style`. Verified with a dedicated Playwright harness: 19 checks green covering all seven §6.5 acceptance items, in a timezone-shifted (America/Los_Angeles) context, plus Stage 2 (31) + Stage 3 (57) regression runs.
- **Bands — a trip is ONE event**: `_mvBands` groups `_pdMonth` rows by source (weekly + travel only; **daily looks never band** — decision 7, the cell is their whole expression), extent = the source's row dates, segments computed per Monday-start week row from REAL weekday offsets (never a hardcoded col). First visible segment carries the title ONLY when the band genuinely starts there (`is_start`); every other segment is "↳ {title}, continued" (italic, reduced). A band clipped by the grid boundary still renders its partial segment — the fetch runs grid±7d so the band knows its true start. **Lanes**: up to two per week row (greedy by start column), overlapping weekly+trip stack without collision, row `padding-top` derives from lane count, beyond two lanes a quiet "+N more" indicator. Sage = weekly, mauve = trip (the rail's dot colours as washes). Tap a band → `__snOpenItem(source)` + `month_band_opened {source_type}`.
- **Cells** (`.rb-mc`, aspect 1.25 → 1 on mobile): serif numeral, 2-line-clamped activity line, ≤3-thumb strip (hidden <1000px, onerror-hidden), states per §6.3 — past settled cream fill / today ink ring / future outlined dashed / free dashed + italic "left free" / pinned mauve ring (same accent as rail + intake) / bare (out of month, or in-month with nothing). **A date with two moments shows the daytime line + a compact ☾ marker** — never two stacked lines. A standalone daily look is full cell content, visually equal to a planned day. **Same-type supersession (`_pdFreshest`) applies per date before anything renders** — a re-planned trip never paints twice and a superseded plan's evening can't ghost a ☾.
- **One spine (§6.4)**: `window._rbOpenPlannedDay(m)` is THE opener — `__rbRailOpen`, `__mvCell` and the deep link all route through it (same `__snOpenItem` + select-day landing), harness-asserted: cell tap and rail tap land on the identical day console. **`?d=YYYY-MM-DD` deep link** on `/dashboard` resolves the date's precedence winner via `_pdRail(d,d)` (daily > travel > weekly) and opens its console from boot.
- NOT built (spec'd but deliberately deferred): the full shared card component with a density prop across rail/cell/artifact (the three renders share data + opener but not one template yet); month rows render only the current fetch — no infinite scroll, ‹ › nav only.

### 2026-07-24 — The Diary Stage 5: the capsule constraint + weekly evening looks (handoff Phase 4 + Phase 3 remainder)
The one place travel is DELIBERATELY different, now enforced: **on a trip you dress from a case, not a wardrobe** — plus the weekly console's day/evening switcher (the §3.3 remainder). Verified in the Stage 3 harness (52 checks green incl. the same-day evening-rules additions below) + Stage 2 rerun (30).
- **Trip day console is capsule-scoped**: `__tvFlip` cycles the slot through the trip's OTHER capsule members in the category (`_tvCapsulePool`) by **re-pointing this day's formula `item_index`** (`_tvRepointDay`) — day-scoped, membership untouched, other days untouched (the pre-Phase-4 flick offered owned wardrobe pieces and applied trip-wide). `__tvDaySwap` opens the shared modal with `cfg.pool` (capsule members, "From your case") and `cfg.adopt` — the **adoption section** ("From the rest of your wardrobe", one-line inline confirm: "Picking one adds it to your case and restyles this day only") → `__tvAdoptApply` appends the piece to membership (wardrobe_match, tier inherited), re-points the current day, fires `capsule_adopted {from:'console'}`. The rack hint (`cfg.rackHintHtml`, new `_rbConsole` slot) names MEMBERSHIP, not packing. Travel's Build-2 alternates prefetch/fetch retired (capsule candidates need no AI alternates — tokens saved).
- **Capsule-level swap (Edit tab) = membership change**: `__tvSwap`/`__tvSwapApply` keep the identity-swap-everywhere behaviour, then report the blast radius — `#tv-stale-bar` ("{piece} now packs — N days wear this slot · ↻ Restyle those days") → `__tvRestyleAllDays` = ONE batched `/api/travel/outfits` call (`__tvOutfitsGo` now accepts a ready-made plan array carrying the days' `user_activity`), firing `capsule_swap_restyle {days_affected}`. Packed toggles drive NO generation (decision 4, harness-asserted).
- **Weekly evenings get their own look, lazily**: when `wkData.evenings` covers the active day the weekly console shows a **Day/Evening switcher** (`__wkSetSlot`, `_wkActiveSlot` reset on render + day change). The evening's look generates on first open via the EXISTING `/api/weekly/day` (`__wkDressEvening` — activity framed as "the EVENING of this day only"; no schema change) into `d.evening_look {occasion, note, items}`, riding the blob. **Every console mutation targets the selected moment only** — flick/anchor/swap/remove/add/save all resolve their item array through `_wkConItems()`. `_pdRowsWk` evening rows enrich from `evening_look` (headline/thumbs/item_ids) so the rail's evening line gets real content.
- **Evening rules, consistent across all three tracks (same-day fix — Annie's bug report)**: *by default the evening is LEFT FREE; every dressed day carries a Day/Evening switcher; an evening ask is an ADDITION, never a restyle of the day.* (1) **Server**: the `/api/travel`, `/api/travel/outfits` and `/api/travel/day` prompts now add an Evening slot ONLY when the day's plan names an evening moment (was "exactly 2 slots" — the itinerary block's "both slots answer to it" softened to match); the client renders 1-slot days cleanly. (2) **Consoles**: the weekly switcher renders on EVERY dressed day (inline `segBtn` segmented pair, "Evening · free" until dressed — no longer gated on `wkData.evenings` coverage); travel's occ control is the fixed `['Day', 'Evening · free']` pair — `_tvLookState` returns `{s: eveS, eveFree: !eveS}` for `_tvActiveOcc === 1` instead of clamping back to Day. Both evening-empty states are an invitation card with an inline input (`#wk-eve-act` / `#tv-eve-act`) + "✨ Dress the evening →". (3) **The addition verbs**: `__wkDressEvening(activity)` creates/updates the `wkData.evenings` moment then generates with a the-day-is-NOT-to-be-changed brief; NEW `__tvDressEvening(activity)` does the trip equivalent via `/api/travel/day` and takes ONLY the Evening slot from the response — the existing Day slot is never replaced (a returned `new_item` still joins the capsule). Both fire `day_planned`. (4) **The scoped prompt routes evening asks as additions** (`_ikRestyleDay` → `_ikEveningIntent` — explicit framings only: "add an evening look" / "evening look|outfit|plan" / "for|in the evening" / "tonight" / "date night" / "after dark"; bare "dinner"/"drinks" still restyle the day): weekly/travel winners open the artifact at the day and call the DressEvening verb; a daily/empty date submits `__dlSubmit(text, {anchorDate, slot: 'evening'})` — `dlData.slot` stamps and `_pdRowsDl` emits an `evening` row, so the evening daily look COEXISTS with the day's look at the same date (restyles keep the saved slot). (5) `_wkPatchSaved` now persists `wkData.evenings` alongside days (a prompt-created evening moment used to vanish on reopen). (6) **The daily console carries the pair too** (Annie's second report, same day): a date's day + evening are two SEPARATE saved daily looks — the switcher (`__dlSetSlot`, rendered whenever `dlData.anchor_date` exists) navigates between siblings (`_dlSibling` — newest daily look at the same anchor date wearing the other slot; exclusion comes from `opts.savedId`, never `_dlActiveSaveId`, which still points at the previous look mid-render), the empty side renders the invitation in place of the console (`#dl-eve-act` + `__dlDressSlot` → `__dlSubmit({anchorDate, slot})` — an addition, never a restyle), and the tab NEVER leaves the daily context — a first cut bounced to the artifact of another track owning the date's moment (a trip's dinner), which read as being dumped into the travel edit (Annie's report, same day); the rail stays the cross-track surface, and a daily evening she creates simply outranks the trip's at read time (daily > travel > weekly). Console head/rack labels read "{weekday} evening" on the evening look. (7) **Ghost moments are dropped at read time** (`_pdFreshest`, applied in `_pdSlots` + the daily tab's cross-track lookup): re-planning the same dates coexists in `planned_days` by design, but a superseded plan of the SAME type is now dropped WHOLESALE per date — without this an old trip's evening row (no contender in the fresh trip, which left its evening free) won the evening slot and painted a rail moment that was empty on click-through. Cross-type complements still resolve per slot. Harness: Stage 3 57 checks (daily-pair + evening-ask sections), Stage 2 31 (ghost-supersession check).
- Config consumption completed: `artifact.hasCapsuleTab`/`hasPackProgress` now gate the travel tabs + masthead pack progress (weekly's shell would simply omit them). The full single-artifact-shell merge remains the one undelivered Phase 3 item — two render functions, config-fed strings, documented trade-off (regression surface vs. hygiene).

### 2026-07-23 — The Diary Stage 4: track config (handoff Phase 3, first tranche)
A week and a trip are the same object with different constraints — **`_RB_TRACKS`** (`dashboard-personalize.js`, right before the `_ik*` section; also `window._RB_TRACKS`) is the one config object with three consumers, resolved via `_rbTrackCfg(kind)` (unknown kinds fall back to weekly). Verified inside the Stage 3 harness: 42 checks green incl. reference-string assertions on both artifacts (zero visual drift) and a throwaway fourth-track fixture.
- **Shape**: per track (`weekly`/`travel`/`daily`) — `engine` (which generator commit routes to), `intake` (`defaultDays`/`maxDays`/`requiresDates`, `rackLabel`, `quickAdd` chips, `primaryLabel(st)`, `maxDaysError`), `artifact` (`eyebrow` — strings deliberately verbatim from the pre-refactor renders: "Your week, planned"/"The travel edit"/"Your daily look" — `hasCapsuleTab`, `hasPackProgress`), `console` (`rackScope: wardrobe|capsule`, `addVerb`, `rackHint` reserved). `rackScope: 'capsule'` is declared but NOT yet enforced — that's Phase 4's one deliberate structural difference (Stage 5); travel flick-through still offers owned wardrobe pieces until then.
- **Consumers wired**: the intake's per-kind ternaries (chips, range caps + errors, rack label, commit CTA, date-requirement, generic date-clamp) all read config; `_ikCommit` routes by `engine` so a fourth track rides an existing generator; the three `_rbConsole` adapters take `addChipLabel` from `console.addVerb`; the three artifact masthead eyebrows read `artifact.eyebrow`.
- **Fourth-track proof** (acceptance §3.5.3): the harness injects `_RB_TRACKS.retreat` (3-day default, own chips/labels, weekly engine) and the intake renders + commits through `/api/weekly` with no component changes.
- NOT yet unified (honest scope — the remaining Phase 3 work, sitting behind this because its regression surface is the working artifacts): the `__wkRenderResult`/`__tvRenderResult` shells are still two functions (config feeds their strings but the trees are separate), `hasCapsuleTab`/`hasPackProgress` are declared-not-consumed, and the weekly console has no day/evening switcher (weekly evenings still fold into the day's plan line).

### 2026-07-23 — The Diary Stage 3: the single prompt + unfurl intake (handoff Phases 1–2, FLAGGED)
The prompt becomes the one field that generates, scopes and restyles — the `diary_prompt_intake` flag: **default ON for everyone since 2026-07-24 (Stage 6 merge day); `?diary=off` is the per-device kill switch (persisted in localStorage `rb_diary_prompt`), `?diary=on` clears it.** (The stage shipped default-OFF/opt-in; Annie flipped it once Stages 1–6 were live-tested.) Flag off = the concierge behaves exactly as before (verified — the Stage 3 harness runs its parity test through the kill switch, and Stage 2 pins `rb_diary_prompt='off'` since it asserts the pre-diary rail tap semantics). All client code lives in the `_ik*` section of `dashboard-personalize.js` (before `_rbOnboardHandoff`); 31 harness checks green incl. flag-off parity, plus a 29-check Stage 2 regression run.
- **`POST /api/intent`** (`server.js`, INTENT_SCHEMA): gemini-2.5-flash, temp 0, thinking 0, 500 tokens, 5s server race. Returns `{intent: daily|weekly|travel|unclear, destination, date_start/end, day_intents[], confidence}`. Hard rules in-prompt: NEVER invent a destination or date ("somewhere warm" → destination null); relative dates resolve against the CLIENT's local calendar date (`clientDate` in the POST body — never server now()); confidence <0.6 → the app asks instead of acting. Captured in generation_log via the wrapped client. Client aborts at 4s.
- **Scope chip** (`#rb-scopechip` in `.cb-input`, created on demand by `_ikChipPaint`): `_ikScope {kind: none|plan|day}`. A weekly plan covering today AUTO-scopes (`_ikComputePlanScope` — label is the plan's real title, fallback "Week of {date}", never a placeholder); **trips never auto-scope** (decision 3 — a week is ambient, a trip is an occasion). Day scope = `.sel` (mauve, destructive-adjacent). Esc or chip-tap from a day returns to the PLAN scope, never straight to none. Rail integration: under the flag a populated card's BODY tap scopes the date (`__rbRailTap`), the card CTA opens (`__rbRailOpenCta`); flag off keeps tap=open.
- **Routing (`_ikSubmit`, intercepts `_cbSubmit` under the flag)**: photo → style track (P0 rule, classifier never sees it); day scope → `_ikRestyleDay` (winning moment from the planned_days cache: daily/empty → `__dlSubmit {anchorDate}`; weekly/travel → opens the artifact AT that day and drives the existing single-day modal + `/api/weekly/day`|`/api/travel/day` with her words as the activity); plan scope + no date/place signal → `_ikRestylePlan` (full `/api/weekly` re-gen from the saved blob's dayPlan/week_iso — a NEW row that coexists and wins at read time); else → classifier → confident daily submits directly, weekly/travel open the unfurl, unclear/failure → clarify state with the prompt preserved (never a dead end). Re-entry guarded (the bundle's send pipeline can double-fire).
- **The unfurl** (`#rb-intake`, attached — `.concierge-box.rb-attached` drops its bottom radius so the panel continues the field): reading state (skeleton, min 400ms) → ready; failure → clarify. **Crumbs**: where (editable; empty = legitimate invitation "add a place"), when (native date inputs; re-deriving preserves activity/pin/evening BY DATE; >21 days refused inline; end<start swaps), provenance note ("read from your prompt" / "edited by you" / "our suggestion"). **Build-around**: category chips from HER wardrobe only, multi-select rack → `anchorItemIds` on `/api/weekly` (BUILD-AROUND prompt block: woven where they serve, never forced); empty wardrobe → section absent. **Day list (sparse-primary)**: pin control (SVG pin, mauve wash `.ik-row.pinned` — pinned = SHE fixed it; classifier `day_intents` arrive pre-filled AND pinned, provenance "your prompt"), moment cell → tray (input + track-specific quick-add chips + Leave free), **+ Add an evening** (only when the row has one moment — never fourteen rows of placeholders). **Commit**: weekly → `_wkGenerate(…, {anchorItemIds, pinnedDays, evenings})` — evening activity folds into that day's authoritative plan line ("Office — evening: date night", one look reads the whole day until Phase 3 parity) AND lands as its own `evening` index row via `wkData.evenings`; `wkData.pinned_days` → rows `pinned=true` (`_pdRowsWk` extended). Travel → **generates DIRECTLY from the unfurl** (bug-fix same day — Annie: "everything should now happen in prompt, not modal"): the crumbs are the brief, the day list is the day planner, the rack is the shortlist (travel heading "What's tempting you?"), commit assembles `_tvBrief` + calls `_tvGenerate(plan)` — the old `#tv-brief-modal`/`#tv-plan-modal` never appear on this path (they survive for the concierge card / wardrobe pack-a-trip entries, and `__tvOpen` kept its dest/dateFrom/dateTo/plan prefill opts). The modal's guardrails moved inline: destination + dates required, shortlist floor `_TV_MIN_ANCHORS` (waived when the wardrobe has <2 pieces), trips cap at 10 days (the engine ceiling — never show rows the server would silently cut). Cancel/abandon writes NOTHING (verified).
- **Suggestion pills** (`#rb-sugg`, max 3, state-derived — NEVER a place/date the user hasn't given): unscoped → add-pieces (<15), plan-my-week (only when no weekly rows cover the coming week; city name only from `__rbCtx`), finish-deferred-trip (real title), dress-today (only when today has no rows); day scope → three tone modifiers (never dated/placed); plan scope → two plan-wide modifiers + dress-today. Telemetry `source: 'pill'` vs `'typed'` — the honest read on whether the field works.
- **Capture**: `prompt_submitted {intent, scope, source, latency_ms, ok}` on every submit incl. failures; `intake_opened/dates_edited/day_edited/day_pinned/evening_added/anchor_selected/committed/abandoned` per the §7.1 vocabulary.
- Deviations flagged for Phase 3: confident `daily` skips the intake (momentum > ceremony — the intake's single-day mode arrives with parity); weekly evening moments get no separate look yet (folded into the day's plan line).
- Same-day polish: the daily generation overlay title is the hero tagline "One prompt. Dressed for anything." on both the `/api/daily` and legacy dress-me overlays (was "Dressing you for today…" — wrong on any scoped day); the anchored-restyle overlay keeps "Restyling around your anchors…". The Daily console headers + lookbook subtitle derive their weekday from `dlData.anchor_date`, never render-time now().

### 2026-07-23 — The Diary Stage 2: the home calendar rail (handoff Phase 5)
First read surface over `planned_days` — seven cards across the top of home (yesterday, today, next five), mounted directly under the concierge prompt (`#rb-rail`, CSS `#rb-rail-style`, built by the `_rbRail` IIFE before `_rbOnboardHandoff`). Verified with a Playwright harness: 25 checks green at 1280px + 390px. **DATE-driven, one card per date, never one per moment** — a date holding two moments shows the daytime activity as the headline and the evening as a quiet second line. **"Palette whisper" card anatomy (Annie's design 1c, same day — supersedes the earlier warm-frame pass)**: text-led calendar cells with NO imagery. Stack: eyebrow "TODAY · THU 23" (relative label + short date, month only when it differs), serif activity ONCE (never repeated, never a truncated parent-title description), quiet evening line, a **dot-coded source tag** (`tagHtml`/`srcDot`: cream dot = Daily look, sage = week "WEEK OF 22 JUL" from `week_iso[0]`, mauve = trip "IBIZA TRIP" — the free day carries the tag of the plan it belongs to), then **up to three colour swatches** (`dayColors`: the day's `item_ids` → wardrobe `item_dna.display.primary_color_hex`, falling back to the `_ALL_SWATCHES` name map; no resolvable colours → no row, never placeholder dots), CTA. **Geometry is LOCKED**: 7 equal grid columns — today the SAME width (color only, per the 1c mock; the earlier wider-today grid is gone), body `min-height:172px`, title zone reserves TWO lines (`min-height:2.3em`), row heights equalise via grid stretch; mobile all cards 158px. The rail is the most compact module on home — **height ladder**: week-ahead compact < wardrobe tracker < concierge medium (`.svc-img` 16:9) < lookbook tall hero (`.rb-sn-img` 3:4). Judge changes on the FULL homepage with every module mounted, never the rail alone. Cards resolve through `_pdRail` (precedence pre-applied); repaints on boot (lazy-uid poll), after `_lbCloudPull`, ~700ms after every `_pdSync` write, and on `visibilitychange` (exposed as `window._rbRailPaint`).
- **States**: `past` (dimmed cream body, **"Wore it?"** CTA → "Worn ✓"), `today` (image over inverted-ink stack, wider desktop column — grid `1fr 1.5fr ×5`), `planned` (Open →), `free` (dashed + diagonal hatch, italic "Left free", **"Dress it →"** — a free day has nothing to open, so its tap SCOPES like an empty card), `pinned` (mauve ring — render support only, no write path until the intake), `empty-today` (ink dashed, "Dress today →"), `empty-future` (dashed invite), `empty-past` (ghost, inert). Cold start = six invitations, never a blank strip. Below 1000px the grid becomes a scroll-snap strip (166px cards, today 206px) that **centres today on mount**.
- **Tap semantics (flag-dependent since Stage 3)**: FLAG OFF — a populated card's tap OPENS the day (`__rbRailTap` → `__rbRailOpen`: `__snOpenItem` + `__wkSelectDay`/`__tvSetTab('outfits')`+`__tvSelectDay` land on the tapped day, `day_opened_from_rail` fires); an EMPTY card aims the prompt at the date (`__rbRailScope` arms `_cbAnchorDate` + `_cbIntent='dress-me'`, injects "An outfit for {Friday 25 Jul} — [the plan]"; `_cbResolve`'s dress-me branch passes the armed date as `__dlSubmit(prompt, {anchorDate})`, cleared by `_cbReset`). FLAG ON — EVERY card's body tap scopes the date via the chip (`window._ikScopeDay`), and opening lives on the card CTA (`__rbRailOpenCta`). Both paths fire `rail_day_scoped {state, offset_from_today}`; a scoped daily look lands on the tapped calendar date, not today, and its console/subtitle read the ANCHOR date's weekday (fixed 2026-07-23 — render-time now() said Thursday on a Friday look).
- **Mark worn** (`__rbRailWear`, past cards): stamps `worn: true` **blob-first** into the day object (`wkData.days[i]`/`tvData.days[i]`/`dlData`) so a later resync can't revert it (row builders emit `status: 'worn'` from the stamp), `_pdSyncSaved` re-syncs, `times_worn` +1 on every owned piece in the day's `item_ids` (the write path that makes cost-per-wear real), card settles to "Worn ✓" without reload.
- **"Coming up" strip** (`.rb-upnext` in `#rb-rail-up`): the next plan starting AFTER today+5 — one cheap `planned_days` query; leads with the parent's editorial hero (the lookbook card `img` — for a trip that's the destination hero shot), real title, date range + **derived counts** (trip: capsule pieces + slot count as looks; week: dressed days — never stored), right-aligned "Open the trip →"/"Open the week →" CTA. Renders only when one exists. The mock's "From your lookbook" strip is deliberately NOT duplicated — the dashboard's existing Lookbook row already serves it.
- Frames hide themselves on image error (`onerror` — a dead Cloudinary URL must not paint a broken-image glyph). Copy strings are provisional (`// COPY: needs sign-off`). Degradation: migration missing → seven dated empty cards + the usual one-time warn.

### 2026-07-23 — The Diary Stage 1: `planned_days` index (migration 12)
First stage of the Diary/calendar architecture (22 Jul `planned_days` brief + 23 Jul Diary handoff rev B, §0.2 amendments folded in — the slot/pinned/anchor_item_ids columns ship in migration 12 itself since no write path predates it). **Data layer + write paths only; nothing user-visible consumes it yet** (the home rail is Stage 2). Verified with a Playwright harness (Supabase shim + REST stubs): 43 checks green at 1280px + 390px incl. precedence, degradation and a 23:30-US-timezone date check.
- **`supabase/planned_days_migration.sql` (migration 12, NOT yet run on Robes_p0)**: `planned_days` — an INDEX over the wkData/tvData/dlData blobs, never a replacement (blobs stay authoritative for rendering). **A moment is a row**: unique on `(user_id, source_id, day_index, slot)`, `slot in ('day','evening')` check-capped at two (a third slot is a product decision with a migration attached), `pinned` (a fixed moment she placed — a generation constraint, not "more looks"), `day_date` is `date` (local-calendar concept — client sends local ISO, never timestamptz), `source_id` is text with NO FK (lookbook PK is `(user_id,id)`, rows can be local-only), denormalised `status/activity/headline/thumb_urls/item_ids`, reserved `anchor_item_ids` (Phase 2 build-around) + `ootd_photo_url`/`worn_at` (OOTD capture phase, unwritten). Own-all + admin-read RLS.
- **Client module** (`dashboard-personalize.js`, after `_lbCloudPull`): `_pdSync(sourceType, sourceId, blob)` — per-source 600ms-debounced upsert (`on_conflict` + `Prefer: resolution=merge-duplicates,return=minimal`) followed by two sweep DELETEs (`day_index=gte.N` for shrunk ranges, `slot=eq.evening&day_index=not.in.(…)` for vanished evenings — a ghost evening on the rail is the same bug class as orphans on delete). `_pdSyncSaved(id)` re-derives from the lookbook cache — the one call every patch path shares. Row builders `_pdRowsWk/_pdRowsTv/_pdRowsDl`: weekly = 1 row/day slot `day`; travel = 1 row per MOMENT (Day+Evening slots per dressed day, 1 free row for rest days, deferred trips band as bare `planned` rows with her typed `trip_day_plan` as activity); daily = 1 row at `anchor_date`. Thumbs prefer truthful wardrobe photos, then generated stills, never a still on a flicked piece (`_dlAltered` gate). **Degradation**: table missing (PGRST205/42P01) → warn once, `_pdDown` stops further calls for the session, app behaves exactly as before.
- **Date emission (brief §5)**: `_wkWeekDays` now emits `iso` alongside its display strings; `__wkPlanGo` → `_wkGenerate(…, weekIso)` stamps `week_iso[]` into the render data/save (positional against `days` — the server generates one entry per calendar day). Travel already had `dateFrom` (server echoes it); dates derive `dateFrom + day_index` via UTC arithmetic. Daily: `__dlSubmit` accepts `opts.anchorDate`, defaults to the LOCAL today (`_pdLocalISO`, never `toISOString()` — the 23:00-US off-by-one), restyles keep the saved look's original `anchor_date`; stored as `dlData.anchor_date`.
- **Write-path wiring**: fresh saves (dl/wk/tv snAdd branches) + every mutation funnel — `_dlPatchSaved`/`_wkPatchSaved`/`_tvPatchSaved`, the `__dlSubmit` savedId restyle branch, and the three `_xxPersistImages` (stills landing late refresh `thumb_urls`). `_lbCloudDelete` also fires `_pdDeleteSource` (no FK cascade exists — orphaned index rows paint ghost days). `__dlWear` stamps `dlData.worn` → row `status: 'worn'` (blob-first so a later resync can't revert it; `worn_at` stays reserved). Old saves without `week_iso`/`anchor_date` emit nothing client-side — the backfill covers them. Rename needs NO index patch: parent titles resolve at read time.
- **Read helpers** (unconsumed until Stage 2): `window._pdRail(fromISO, toISO, cb?)` — 7 DATE-driven slots whether or not rows exist, cb fires from localStorage cache then fresh; precedence resolved per `(date, slot)`: `daily > travel > weekly` (auto-planned tier reserved), ties to latest `updated_at` — a trip's evening contends with a week's evening, never displaces its daytime. `window._pdMonth(from, to)` — ALL rows unresolved + a `sources` map (band extents need the losing rows). Parent title/type resolves from the local lookbook cache (`_pdParent`), never a per-card query.
- **Backfill (run once AFTER migration 12), two equivalent forms**: `supabase/planned_days_backfill.sql` — paste-and-run in the Supabase SQL editor, no service key needed (this is the form actually used on Robes_p0) — or `SUPABASE_SERVICE_ROLE_KEY=… node scripts/backfill_planned_days.mjs [--dry-run]` from a terminal. Both are re-runnable (upserts): weekly/travel blobs only, **daily looks skipped entirely** (decision log #2, no guessing at `created_at::date`); old weekly saves get dates parsed from "Monday · 14 Jul" labels with nearest-year-to-`created_at`; unparseable sources skipped and surfaced (SQL query 5 lists them).
- **Capture**: `day_planned {source_type, day_index}` fires on single-day plan edits (`_wkApplyDay` with activity, `_tvApplyDayResult` with user activity); `day_opened_from_rail` vocabulary ships with the rail.
- **Decided precedence answers brief §13 Q3**: re-planning a week over the same dates COEXISTS in the table and resolves at read time (tier, then latest `updated_at`) — write-time exclusion loses data.

### 2026-07-22 — Weekly imagery (closing the generation_log zero-row gap)
The image-row distribution in `generation_log` (Daily 29 / Travel 8 / Weekly 0) evidenced two things. (1) **Weekly made no image-generation calls at all** — the monogram tile on unowned pieces was the designed behaviour, not a failure rate. Fixed: `/api/weekly` now runs the same background still-life pipeline as Travel. (2) **Travel's 8 rows against a 15-piece capsule is the deliberate `frames < 8` cap** (`server.js`, hero shot + ≤7 stills, sized to the client's 5-minute polling ceiling) — NOT rate-limit timeouts; the imageless capsule cards are unowned pieces past the cap, working as designed. Travel is unchanged. Weekly specifics:
- **Server**: after coverage validation, one still-life per **distinct unowned** piece — deduped by lowercased name across the week, because the routing discipline re-wears pieces across days, so every appearance shares one frame; owned pieces are skipped (wardrobe photos are truthful and free). Capped at 8 stills (same polling-ceiling maths as Travel). Response gains `jobId` + `imageCount` (omitted entirely when every piece is owned — zero frames means no job). Background loop mirrors Travel exactly: 3s stagger, 50s race per frame, Cloudinary-hosted URLs only, `logAI` feature `'weekly'` stage `'image'` — and because it rides the wrapped `ai.models.generateContent`, weekly image rows now land in `generation_log` automatically.
- **Client** (`dashboard-personalize.js`): `_wkPollImages`/`_wkSetImage`/`_wkSettlePlaceholder`/`_wkPersistImages` — the Travel poller pattern verbatim (`data-wkimg="i"` wraps, `.wk-img-ph` placeholders, onerror → monogram settle, `image_fallback` track with `surface: 'weekly-plan'`, 300s ceiling, persists into `wkData.generatedImages` + backfills the lookbook card `img` when the save had no owned photo). `wkFrame` in `_wkPaintConsole` mirrors `_tvFrame` (wardrobe photo → generated still → monogram; a flicked piece never wears the original's still via `_dlAltered`; reopened saves with a missing frame settle straight to monogram since `jobId` is stripped on save). Day-strip thumbs include stills. `__wkRenderResult` calls `_wkStopPolling()` up front so a stale poller can't patch an earlier plan's frames into new data. `_wkApplyDay` re-attaches the week's existing stills by name onto a restyled day's fresh items (`/api/weekly/day` itself still generates no imagery, matching `/api/travel/day`). `publicSharePayload`'s weekly branch includes stills.
- **Travel hero resurfaced (same day)**: the hero shot had been generated-but-invisible in-app since the 2026-07-10 day-console "The mood" tile removal, surviving only as the lookbook thumbnail and the `/board` share lead/OG image (`addImg(row.img)` runs first in `publicSharePayload`). It now renders once at TRIP level — a 200px "The mood · {destination}" tile beside the Edit pane's stylist summary (flex row, wraps below the text on narrow screens, `tv-noprint`). The tile only mounts when the frame exists or a live job can still deliver it (`data-tvimg="0"` so `_tvSetImage` patches it; old saves with no stored hero show no tile); a failed hero settles to the destination-initial monogram (`_tvSettlePlaceholder` special-cases i=0 — no capsule item owns frame 0).
Tranche 2's Build 1 originally proposed one acquisition verb (`Save`) everywhere; the Build 1 amendment (Part A, same day) corrected this after re-reading Travel's `Pack`/`Add` as a matched trip-scoped pair (Keep / Worth adding), not the same slot as the new verb. Implemented per the amendment, not the original brief:
- **Travel unowned rack piece** (day console `thirdHtml` + the capsule card, both routed through `__tvAddOwn`): unchanged label/behaviour (`+ Add`, sets `it.added = true`, toast "added to the pack", trip-scoped) — **plus** a new silent, idempotent write to `wishlist_items` via `_wlSaveFromItem(it, {silent:true})`, fired async after the existing render/toast so it never delays or narrates the trip-scoped action. No second confirmation.
- **Daily/Weekly unowned rack piece**: new third rack action, `Save` (`.rbc-act.save`, filled ink — same weight as Travel's `+ Add`), wired to `__dlSaveWishlist`/`__wkSaveWishlist` → `_wlSaveFromItem(it)` → `wishlist_items` (`source_type: 'robes'`, "Robes suggests" chip), toast **"Saved to your wishlist."** Owned pieces get no third action on these two surfaces (no case to add to). Once saved, the button becomes an inert `<span class="rbc-act done">✓ Saved</span>` — same idempotency the shared helper also guards server-side-of-state (a stray second click no-ops on the `it.wishlisted` flag rather than double-inserting).
- **`_wlSaveFromItem(it, opts)`** (new shared helper, lives by `_WL_SRC`): the single write path for all three surfaces — reuses the `wishlist_items` table/pipeline rather than reopening the manual-capture modal (`__wlOpenAdd` stays the screenshot/photo flow only). `it.wishlisted` persists on the item object exactly like `packed`/`added`/`anchored` already do (no schema change — it rides inside each surface's saved JSON blob via `_dlPatchSaved`/`_wkPatchSaved`/`_tvPatchSaved`).
- **Tile chip label is surface-scoped**, not hard-coded: `_rbcTile` reads `cfg.addChipLabel` (falls back to `'Add'`); all three `_rbConsole` call sites set it explicitly — Travel `'Add'`, Daily/Weekly `'Save'`.
- Neither verb touches `wardrobe_items` — ownership capture is still only the swap modal's Snap-mine path and the pack-toggle own-modal (`__tvQuickOwn`/`__tvOwnSnap`), unchanged.

### 2026-07-22 — Build 3: look-contextual row notes + copy rules (Tranche 2)
Closes the F7/F20 copy defects the Look/Rack audit found — the row/panel notes were either unused (`description`) or generic stylist-copy tells (`X yet Y`, benefit justification). `server.js` now carries four shared prompt-rule constants, interpolated into every stylist system prompt rather than repeated per-endpoint: `ROW_NOTE_RULE` (per-garment, 6–12 words, verb-led participle, never repeats the item name), `PANEL_NOTE_RULE` (per-look, 30 words, describes balance/register, garment TYPE only), `WEEK_SUMMARY_RULE` (40 words, no specific garments), `BANNED_CONSTRUCTIONS_RULE` (the audited tells — "X yet Y", "perfect for"/"ensuring"/"effortlessly"/"elevated"/"versatile", virtue framing, quotation marks — appended once per prompt, covers every field in that call).
- **New field `how`** on `DAILY_SCHEMA` and `WEEKLY_SCHEMA` item properties (and inherited into `WEEKLY_DAY_SCHEMA`, which reuses `WEEKLY_SCHEMA.properties.days.items.properties.items`) — the row note, required in place of `description` (which stays in the schema, now optional, purely an internal cue for the still-life image prompt — never rendered). Travel's existing per-formula `note` field is aligned to the same `ROW_NOTE_RULE` text rather than given a new field (`server.js` pillar §3 in `travelSystem`, plus `TRAVEL_DAY_SCHEMA`/`TRAVEL_OUTFITS_SCHEMA`'s rule #2) — it already had the right shape, just no length/register discipline.
- **Panel note**: Daily's `stylist_summary` field rule is replaced wholesale (was "2–3 sentences… name the garments… golden-ratio thinking", no cap) — this is the F7 close Tranche 1 §4.3 set up (removed the framework-vocabulary instruction) but didn't finish. Weekly's per-day `note` (rendered as the console's mood quote, `quoteHtml: d.note`) is now explicitly the day's panel note; the week-level `stylist_summary` and `WEEKLY_DAY_SCHEMA`'s refreshed `stylist_summary` both point at `WEEK_SUMMARY_RULE` instead of a bespoke 2–3-sentence instruction.
- Client (`dashboard-personalize.js`): Daily's and Weekly's `_rbConsole` conItems both gained `noteHtml: it.how ? '<div class="rbc-hownote">'+esc(it.how)+'</div>' : ''` — the same `.rbc-hownote` class Travel already used for `x.f.note`. **Flicking/swapping a piece clears or replaces its row note** (`_dlApplyOption` now sets `it.how = o.how || ''`) — a row note describes a SPECIFIC piece, so it can't survive a flick to a different one without going stale; only the original served piece (`it.orig.how`, stashed the first time `_dlOptions` runs) and an on-demand alternate that carries its own `how` (Build 2) keep one, everything else renders no row note rather than a mismatched line.
- **F20 occasion casing**: removed the ALL CAPS prompt instructions for `occasion_label` (Daily) and `week_label` (Weekly) — both now sentence case at generation. Removed the `text-transform:uppercase` CSS that was forcing visual caps regardless (`.dlm-kws .kw`, the weekly `week_label` pill's inline style) and normalise both to sentence case at render (`cap1(...toLowerCase())` for occasion_label; an inline equivalent for week_label) — belt-and-braces against any pre-existing saved lookbook rows that still carry the old ALL CAPS strings. `trip_label` (Travel) and the moodboard's `aesthetic_tags` keep their ALL CAPS treatment — out of scope, not named in the audit.
- Server-side defence in depth (not a substitute for the prompt rule): `how` is truncated to 160 chars wherever an item is normalised (`weeklyNormaliseItem`, the Daily per-item loop) — mirrors the existing pattern for `note`/other fields.

### 2026-07-22 — Build 2: on-demand AI alternates for Weekly + Travel (Tranche 2)
Weekly and Travel's flick-through carousel (`_dlOptions`) only ever offered the served original plus owned same-category pieces — no AI-suggested alternates, unlike Daily (whose `DAILY_SCHEMA` generates 2 per item upfront). Generating alternates upfront for Weekly (up to 84 items across a 14-day plan) would be a large schema/token cost for options mostly never viewed, so this is fetched lazily, one piece at a time.
- **`POST /api/alternates`** (new, `server.js`): takes `{item: {name, category, brand}, context: [other piece names in the look], styleDna, styleIcons}`, returns `{alternates: [{name, brand, retailer_hint, price_point, how}, …]}` (exactly 2, same shape as Daily's existing `alternates` plus the new `how` row note from Build 3). `gemini-2.5-flash`, thinking off, 500 tokens — deliberately the cheapest model already validated on this API (see the Gemini model chain note in Common gotchas), not a frontier model, per the brief. Rides the same `ai.models.generateContent` wrapper as every other endpoint, so it's automatically captured in `generation_log` with zero extra wiring.
- **Ordering guard (mandatory, not conditional — the generation_log gate was dropped, see the addendum below)**: `_dlOptions(it, opts)` gained `opts.aiFirst` — Daily keeps its established order (original, then AI alternates, then owned — alternates were always generated upfront ahead of owned); Weekly and Travel now pass `{aiFirst: false}` at both their `_rbConsole` conItems mapping and their `__wkFlip`/`__tvFlip` handlers, so owned wardrobe matches always list before any on-demand AI alternate. This is what stops Build 2 from quietly making new-to-buy easier to reach on the surface (Weekly) already suspected of over-weighting it.
- **Fetch + cache** (`dashboard-personalize.js`, by `_dlApplyOption`): `_rbFetchAlternates(it, surface, otherNames)` — a session-lifetime `Map` keyed on `surface|category|name` de-dupes in-flight and completed calls, so a second Swap on the same-named piece never re-fetches (`_rbAltKey`). `_rbPrefetchAlternates(items, surface, repaintFn)` fires the fetch for every not-yet-fetched piece in the **visible day/slot only** (never the whole week or trip) right after that day/slot paints (`_wkPaintConsole`/`_tvPaintConsole` each call it on their own last line — naturally covers every render path: initial mount, `__wkSelectDay`/`__tvSelectDay`/`__tvSetOcc`, restyle, anchor, flick, remove), then repaints once if anything genuinely new landed (a no-op if everything was already cached, so it can't loop). **Swap is still the explicit named trigger** (`__wkSwap`/`__tvSwap` each fire `_rbFetchAlternates` directly when the modal opens) — usually redundant with the prefetch, but the fallback if the background fetch hadn't resolved yet or failed transiently.
- **Accepted cost**: a beat of latency on the first flick where Daily is instant — the visible-day/slot prefetch covers most of it, per the brief. Not solved by moving generation upfront (that's the token-cost problem this build exists to avoid).

### 2026-07-22 — Build 2 addendum: instrumentation (Tranche 2)
The pre-ship `generation_log` owned-vs-total query (above) turned out to be unanswerable — at the volume in the table (five Daily generations, one Weekly, one Travel) and with `detail` carrying no ownership fields, there was nothing to query. Two separate problems, both fixed:
- **The gate is removed, not conditional.** The ordering guard is mandatory on every surface regardless of what any future query shows — it was already correct under every branch of the original decision table; the query was only ever going to set urgency, never permission.
- **`detail` now carries `owned_count`/`item_count`.** `logAI(...)` (the console-only structured logger) already computed these on all three surfaces, but never reached the queryable table — only the auto-wrapped `ai.models.generateContent` writes to `generation_log`, and its `detail` only ever carried `{input_images, gen_id, user_prompt}`. Each of `/api/daily` (top of the file), `/api/weekly`, `/api/weekly/day` and `/api/travel`'s text-generation success path now fires one extra manual `glog()` call (`genCtx.getStore()` for `user_id`/`gen_id`, same precedent `weekly_coverage_uncorrected` already used) with `detail: {stage: 'composition', owned_count, item_count}` — no schema change, `detail` is jsonb.
- **Weekly logs a per-day breakdown, not just the week average.** A week-level ratio hides a single day that's almost entirely new-to-buy — the exact failure the query exists to catch. `/api/weekly`'s composition row carries `days: [{label, owned_count, item_count}, …]` alongside the week totals, in the same row (one row, not one per day). `/api/weekly/day` (the single-day restyle) logs its own flat `owned_count`/`item_count` — no per-day array needed since it only ever describes one day.
- **Logging-path audit (done before writing any of the above — an incomplete table would just produce a more detailed wrong answer)**: every Gemini call across every surface — text and background image loops alike — runs through the single wrapped `ai.models.generateContent` (one `GoogleGenAI` instance in the whole codebase, wrapped once at module load, `if (SUPA_SERVICE_KEY)`, no other reassignment). The background image IIFEs (`/api/style`, `/api/daily`, `/api/travel`, `/api/stylenotes/tryon`, `/api/moodboard`) are invoked synchronously inside their route handler *before* `res.json()` returns, so the request's `AsyncLocalStorage` context (`genCtx`) survives into every staggered `setTimeout`-delimited iteration after the response is sent — verified with a standalone repro script, not just reasoning about it, since this is exactly the kind of thing that's easy to get backwards. The apparent oddity in the original query (`/api/daily` showing `has_image` on far more rows than rows with a full JSON payload) isn't a missing-logs bug: only text-model calls get a parsed payload in `response` (image-model calls deliberately reduce to `{has_image}` so base64 never lands in the table), and one Daily generation is exactly one text call plus several image calls — the ratio is the design working as intended, not evidence of a gap. `endpoint`/`status`/`environment` are already consistent by construction: `endpoint` comes from the `/api` middleware's `req.baseUrl + req.path` (or is set explicitly to match on every manual `glog()` call), `status` is drawn from the same small enum everywhere (`ok`/`partial`/`error`/`timeout`), and `environment` is one process-wide constant (`APP_ENV`, derived from `PUBLIC_URL` at boot) — a single deployed instance can never log a mixed environment value.

### 2026-07-21 — Hero Rack woven into the wardrobe (integration pass, parallel session)
Supersedes the presentation half of the 2026-07-14 Hero Rack bullet — the mechanics (star toggle, picker, season-awareness, `hero_position`, cap 10) are unchanged; only how it surfaces:
- **No titled section, no empty-state card, no separate rail**: the `.rb-hero-head` ("The Hero Rack" + echo + count note), the "Which five pieces…" `.rb-hero-empty` invite AND the `#rb-hero` rail itself are gone (design feedback same day: the rail read as separated from the wardrobe by the filter tabs). Heroes live **IN the wardrobe grid**: `_waRender` sorts in-season starred pieces to the front (by `hero_position`); off-season heroes and everything else keep newest-first order — an off-season hero just sits in its normal grid slot with its filled star. Zero heroes = a plain grid, no chrome at all. The grid-card `.rb-star` (hover-reveal desktop, always visible on touch) is the whole feature surface; starring toasts **"Saved to your Hero Rack"** (un-star: "Removed from your Hero Rack") and re-sorts the grid. `__waHeroPicker` is kept but door-less (reachable only via code / the edit-modal expander toggle); the v2 setup/sync guards now key on `#rb-wsub`, not `#rb-hero`.
- **Dashboard tracker strip leads with heroes** (`_waSyncCounts` `#wtrk-items`, so only while the tracker is on the page, <15 items): tiles sort hero-first (by `hero_position`, non-heroes keep newest-first) with a small ink `.wtrk-star` badge on hero tiles.
- **Generation priority**: the daily / weekly (+`/weekly/day`) / travel `wardrobeItems` payloads now carry `hero: true` + `seasons[]` on starred pieces (omitted otherwise — JSON.stringify drops the undefineds). Server: `heroMark(i)` appends `★ HERO (Summer/…)` (untagged → `Year-round`) to the closet lines, and `heroDirective(closetItems)` injects a prioritise-when-occasion-AND-season-match block — wired into `/api/daily` directly, into `weeklyClosetBlocks`' `stateDirective` (covers `/api/weekly` + `/api/weekly/day`), and into `travelSystem` beside its stateDirective (trip heroes answer to the destination's micro-climate, not the home season). `/api/travel/outfits` + `/api/travel/day` are capsule-driven and untouched.
- Verified 2026-07-21 with a Playwright harness (Supabase shim + REST stubs): 18 checks green at 1280px + 390px/touch, incl. the `/api/daily` payload capture.

### 2026-07-21 — Admin panel + capture plane (migration 11, deployed to beta same day)
Built from the admin architecture doc (record-browsing over analytics, custom in-repo page over SaaS). Full reference in the "Admin panel + capture plane" section; this is the day's delta:
- **Migration 11** (`supabase/admin_capture_migration.sql`, run on Robes_p0 2026-07-21): `profiles.is_admin` + `is_admin()`, `events` / `generation_log` / `feedback` tables, admin-read RLS on the existing tables, `admin_user_overview()` RPC (drop-and-recreate — the function gained `p_sort`/`p_dir` the same day, and the old 4-arg signature must be dropped or named-arg RPC calls turn ambiguous). Admin granted to slatteryannie@gmail.com.
- **`/admin`** (`public/admin.html`): Users (sortable headers, server-side sort via the RPC, paginated + search) / User detail (profile + DNA, wardrobe, wishlist, lookbook with /board links, feedback, activity, **Generations** trail) / Generation health (7-day per-endpoint aggregates, not-OK table, prompt/response inspect modal) / Feedback feed. Beta/production env filter.
- **Capture wiring**: client `_rbTrack` + `_rbFbCloud` via `_rbCapturePost` (`Prefer: return=minimal` — see gotchas); server `glog` wrapper on `ai.models.generateContent` behind `SUPABASE_SERVICE_ROLE_KEY` (set on the beta Railway service 2026-07-21) with an AsyncLocalStorage `/api` middleware carrying `{endpoint, userId, genId, userPrompt}`.
- **Prompt → calls → artifact** (`gen_id`): every generation submit mints `_rbGenId()`; the id lands in each `generation_log.detail` AND the saved lookbook entry's data, so the admin Generations section shows her typed words, one chip per LLM call, and what it saved as.
- **Launch-day fixes**: (1) events/feedback inserts were failing 42501 for non-admins — `return=representation` triggers `INSERT…RETURNING`, which RLS checks against the admin-only SELECT policies; `_rbCapturePost` posts `return=minimal`. (2) Generations from tabs loaded before the deploy log with null user — a refresh fixes attribution.
- Old Airtable feedback is NOT backfilled; the Supabase feedback corpus starts 2026-07-21. Pre-gen_id generation_log rows stay ungrouped single rows.

### 2026-07-12 — LLM experience fixes (key-piece imagery + styling quality)
Beta feedback (mixed genders in generated looks, prompt not reflected in imagery, heads/feet cropped, style icons ignored, missing images, contrived styling moves). All in `server.js` `/api/style` + the kp render paths unless noted:
- **The wearer is decided once, by the text model** — `STYLE_SCHEMA` gained a required `wearer` (`'woman'|'man'`) with the rule that a menswear/unisex *piece* (sportswear, boyfriend jeans) NEVER makes the wearer male; only the user's words do. All three look-image prompts use `one ${wearer}` (they previously said "one person" with no gender, so each of the 3 independently-generated frames could flip gender on a menswear-coded piece). Missing/invalid → defaults to woman.
- **Shared image-prompt fragments** (top of the style section in `server.js`): `FULL_BODY_FRAME` — head-to-toe framing, full head/hair + both shoes visible with margin, never crop face/head/hands/feet (replaces "tall portrait crop", which invited the cropping) — and `styleIconsImageLine(styleIcons)` — injects the user's style icons into the IMAGE prompts (previously DNA/icons steered only the text model). Both applied to: kp look images, Daily anchor shot, Travel hero shot, moodboard `hero_look` frames.
- **The image prompt now carries the user's brief** (first 200 chars) and, when a photo is attached, an explicit role line: reproduce the piece faithfully but compose a new scene — never copy the photo's framing/background/crop (generated frames were inheriting the piece snapshot's composition).
- **Key-piece image generation is strictly sequential** — one generation in flight, 3s between frames, 40s per attempt, ONE retry per frame after an 8s pause (long enough to clear a rate-limit window); a slow call that outlives its race timeout still lands its image on the job (slot taken only if still empty). The original `Promise.all` burst of 3 simultaneous calls with a single 50s race and no retry was the cause of the recurring "2 of 3 images" failures — concurrent calls contend for the image model's rate limit (a mere start-stagger is NOT enough; this is now consistent with daily/travel/moodboard, which were always serial). `_kpPollImages` ceiling raised 90s → 300s to match. NOTE: all beta users share one Gemini key — under multi-user load a frame can still occasionally drop both attempts; the next lever is Gemini quota tier, not code.
- **STYLING SANITY CHECK block** in the `/api/style` brief (after the layer formula): every styling move must honour the key piece's natural register — never belt knitwear/cardigans over athletic shorts, never force waist-cinching/hourglass tricks onto sporty silhouettes, never layer formal tailoring over gym wear. Silhouette/body-architecture DNA rules govern WHAT is selected, never a licence to contort HOW a piece is worn (fixes the "belt the cardigan over Umbro shorts to emphasize the hourglass" class of output — the DNA maxims were being demonstrated on every look).
- **Mobile crop fixes (CSS, not the model)**: (1) kp result card — under 700px the one-column layout put the cover-fit portrait image in a `min-height:300px` landscape box, slicing head + feet; the image now flows at its natural ratio (`position:static;height:auto` !important overrides in `kp-img-style`), min-height kept only as the placeholder backstop. (2) `/board` share page (`board.html`) — the lead image's forced `16/10` box and the `3/4` tiles did the same on phones; under 640px all grid images now render natural-ratio (mobile is a single-column feed), desktop keeps the 3/4 tile grid + spanning lead.

### 2026-07-12 — Look/Rack unification + interaction polish (parallel session)
- `docs/look-rack-template-audit.md` — per-context divergence audit of the Look/Rack templates; implemented as:
- **Shared components in `dashboard-personalize.js`**: `_rbConsole(cfg)` (one canonical Look-panel + Rack renderer, `.rbc-*` classes — Daily/Weekly/Travel day consoles are thin adapters; `data-dlimg`/`data-tvimg` poller contracts unchanged), `_rbDayStrip` (one calendar strip for Weekly + Travel), `_rbFeedbackBlock`/`_rbFeedbackArm` + one `__rbFbRate`/`__rbFbSubmit` pair, `_rbSwapModal` (shared swap modal — Weekly gained Swap; tile tap = swap everywhere, weekly tap-to-anchor retired), `_rbDayPost` (75s-abort POST guard behind `/api/weekly/day` + `/api/travel/day`).
- **Travel gained anchoring** (Anchor CTA per rack row, persisted in `tvData`, `/api/travel/day` holds anchored capsule pieces via an ANCHORED prompt block) and one-tap `__tvRestyleDay`; one restyle CTA everywhere ("↻ Restyle this day" in the rack header). Headers normalised ("The look · {Day} · {x} pieces", "The rack · {Day}"); Weekly console re-skinned to Daily parity + Rename + share flow (incl. weekly-plan branch in `publicSharePayload`); `transition_tip` added to weekly/travel schemas but the tip strip is retired from the render — **"The read" verdict block** closes the Look panel in all three consoles (`_rbcReadBlock`).
- **Rack cards are removable everywhere** — × badge + swipe-left-to-delete (shared delegated handler on `[data-rmfn]`); Daily/Weekly splice the item, Travel removes it from the look's formula only (capsule/packing keep it); two-piece minimum guard. Swipe-to-delete also covers lookbook cards (page grid + dashboard row) → existing `_rbConfirmDelete` modal.
- **"+ Add a piece" is a chooser** on every rack: From your wardrobe (grid picker → `__dlAddOwned`/`__wkAddOwned`/`__tvAddOwnedToLook`), Take a photo, Upload a photo — the camera/upload doors pop the OS picker in one click as soon as the wardrobe modal's step 1 mounts. Travel's day-rack add (`__tvAddPieceToLook`) files the snapped piece as an owned capsule Keep in this slot's formula.

### 2026-07-20 — Layout spec (held canvas, fluid content · Claude Design handoff)
One centred shell, two measures — the width follows the content, not one global rule. Tokens in `dashboard.html` `:root` (+ a mirrored copy in `stylenotes.html`): `--shell:1440px`, `--nav-h:64px` (was 60 — `sn-page`'s inline top now reads the var), and the stepped page gutter `--s6` (80px ≥1200 / 64px 1024–1199 / 40px 768–1023 / 24px <768).
- **Shell**: the nav bar bleeds full-width but its content aligns to the shell via the `.nav-in` wrapper (markup: `.nav > .nav-in > .nav-l + .nav-r` — keep the wrapper, the bar's hairline must span the viewport). `.dash`, `.wardrobe-panel`, the lookbook page, dl/tv/wk wrappers + payoff inners and `.rb-sfoot-in` all sit at `max-width:var(--shell)` with `--s6` padding (the old 900/960/1148/1180 caps are gone except kp's 900 content column, which is deliberate reading measure).
- **Grids hold their columns — never widen cells** (`minmax(0,1fr)` tracks everywhere; plain `1fr` = `minmax(auto,1fr)` and a card's min-content width silently widens its whole column — observed with the add-card at 3-col): wardrobe `.wg-grid` 5/4/3/2 (gap 16), lookbook `#sn-grid` 3/3/2/1 (gap 20), travel Keep/Worth-adding `.tvm-capgrid` 6/4/4/3 (gap 16), stylenotes palette `.g-6` 6 fixed → 3 <768 (gap 14).
- **Reading measure**: `.concierge` (prompt box) max 760px, `.dash-mast` (display headline) max 620px, travel `stylist_summary` max 900px; kp prose already ≤720.
- **Mobile shell is <768px, tablet band 768–1023 is desktop-shaped**: every nav-arch mobile rule (dock, FAB, back pill, header share, footer offsets, `_rbNavSync` matchMedia, stylenotes dock) moved 640 → 767. 768+ keeps the top bar + multi-column grids (portrait iPad never collapses to the phone shell). The ambient weather pill hides <768 and condenses to place·temp at 768–820 (`nth-child` hide of the day + its dot — the pill's span order is fixed, `_rbWeather` fills in place). Because the nav pill is gone <768px, a **mobile-only ambient strip** (`#dash-wx-m`, in the `.dash-mast` under `.dash-echo`) carries the "Monday · Dublin · 20°C" context there — `_rbSyncMobileWx()` (inside the `_rbWeather` IIFE) builds it from `window.__rbCtx` + the weekday and reveals it via a `.on` class only once geolocation + forecast land (never placeholder values); CSS shows `.dash-wx-m.on` at `max-width:767px` only. `.rb-tn-link` gets an invisible `::after` hit-area so nav links clear 44px on touch.

### 2026-07-20 — Nav architecture (anchored destinations · Claude Design handoff)
From the "Mobile Nav Architecture" + "Desktop Nav Architecture" mocks (Claude Design project 2c795d90…): one IA across breakpoints — three destinations (Home / Wardrobe / Lookbook), detail screens keep their parent lit, Style notes sits off the nav (account area). Static markup + CSS in `dashboard.html` (NAV v2 block), behaviour in the `_rbNavArch` IIFE in `dashboard-personalize.js` (after `_rbInitBreadcrumb`), Style notes changes in `stylenotes.html`.
- **Desktop (>640px — ≥768px since the layout spec moved the mobile shell)**: Wardrobe + Lookbook are inline text links (`#rb-topnav`, `.rb-tn-link`) beside the wordmark (which carries Home); active = ink underline. The `#rb-crumb` breadcrumb and the "Wardrobe · N" `.nav-wbtn` pill are retired **in CSS only** (`display:none !important`): `rbSetCrumb` still runs hidden, and the pill MUST stay in the DOM — the wordmark/popstate close paths click it programmatically.
- **Mobile (≤640px at first — now ≤767px per the layout spec)**: fixed glass dock (`#rb-dock`, z-48 — under the nav's 50 and every modal, over the z-40/45 result overlays) with the three tabs and ink top-bar active state ("Ink underline" variant from the mock). **No count badge on the Wardrobe tab** — the final mock removed it (the design doc's closing footnote still claiming the count moves onto the tab is stale; the phones are authoritative). Detail overlays (kp/dl/tv/wk) swap the wordmark line for a **"‹ Lookbook" back pill** (`#rb-backpill`) that closes the overlay and opens the lookbook list. Result-page action bars (`.rb-sfoot`, `.dlm-payoff`, `.tvm-payoff`) get `bottom:86px !important` at ≤767px so genuine actions ride ABOVE the dock (the mock's actions tier); body/panel/sn-page get bottom padding for dock clearance.
- **State sync**: `_rbNavSync` (also `window._rbNavSync`) derives the active destination from DOM visibility — wardrobe panel `.visible` → Wardrobe; `#sn-page` or any kp/dl/tv/wk overlay → Lookbook (every result is a saved lookbook entry); else Home — and runs on a 350ms poll (views open/close through too many independent paths for event wiring). `window.__rbNavGo(dest)` is the single tap handler ('home' reuses the wordmark onclick; 'back'/'lookbook' close overlays then `__snOpen()`; 'wardrobe' opens the panel).
- **Closing the wardrobe panel programmatically: use `App.goHome()`** — `App.showWardrobe()` never toggles closed (verified: repeat calls keep `.visible`); goHome is the one call that reverses the bundle's view-switch without the wordmark handler's hard-navigation fallback. `_closeWardrobe` in `_rbNavArch` does this.
- **Style notes** (`stylenotes.html`): topbar-l is now `ROBES | ‹ Home` (`.sn-back`; wordmark + sep hidden ≤767px so mobile shows just the back pill), the `.sn-wbtn` Wardrobe button is removed, avatar stays dark (account-area marker); its own copy of the dock (nothing lit, real hrefs to /dashboard /wardrobe /lookbook) renders ≤767px.
- **Share + Rename relocation (third pass)**: on mobile detail screens Share rises into the header — `#rb-share-btn` (glass circle beside the avatar, same `window.__rbShare()` flow) shows under the same condition as the back pill (`_rbNavSync`), while the footer Share buttons carry `.rb-share-foot` and hide ≤767px (desktop keeps them). **Rename is a small pencil in the title row at both breakpoints** (`.rb-rename-tbtn` beside `#kp-headline` / `.dlm-title` / `#wk-headline` / `#tv-headline` → `__rbRename('kp'|'dl'|'wk'|'tv')`); the footer Rename buttons are removed everywhere. **All four result surfaces rename identically**: `__rbRenameSave` persists the name as the lookbook card `title` AND writes it onto the render data's `headline` (into `kpData`/`dlData`/`tvData`/`wkData`), and `applyLive` updates the visible headline — so a rename sticks on the page and on every reopen. Key piece has no dynamic `headline` of its own, so `__kpRenderResult` shows `data.headline` as the H1 when a custom name exists (else the editorial default). (Earlier the kp rename only touched the footer meta and dl had no pencil at all — both read as "the name won't stick".)
- **In-screen changes from the same handoff (second pass)**: (1) list headers are the small-caps eyebrow style matching "Your daily look"/"The travel edit" — wardrobe panel `.wg-title` restyled (Inter 500 uppercase `var(--rose)`, `.wg-sub` retired, count vertically centred beside via `.wg-header{align-items:center}`), lookbook page header is "Lookbook" + `#sn-count` "N kept" (filled in `snRenderPage`), and the wardrobe's All pieces/Wishlist sub-tabs (`#rb-wsub`) sit BELOW the title (mock order: title leads), hairlined like the category tabs. **The lookbook has type sub-filters** (`#sn-tabs`, `.sn-ftab` — All / Key pieces / Daily looks / Weekly plans / Travel edits): `_snFilter` + `_snSetFilter` drive `snRenderPage`'s `visible` slice, a filtered-empty state reads "No {type} yet." (`#sn-empty-t`/`-s`), legacy `'look'` rows only show under All, and `__snOpen` always resets to All (wardrobe convention). (2) Style notes chapter tabs lost the 01/02/03 numerals (`.num` spans removed, `.lbl` restyled to the 13px text-tab pattern) and the section eyebrows read plain "Style notes". (3) **Add-a-piece rework**: the `#rb-wg-cta-head` header CTA is hidden on All pieces (it survives as the **wishlist-only** add path — the wishlist hides the toolbar, so don't delete it); desktop adds via `#rb-add-pill` ("+ Add piece", primary ink, `margin-left:auto`) in the filters row with **Refine demoted to outline** (dark only via `.active`); mobile adds via the `#rb-wa-fab` ink FAB (fixed right 18px / bottom 96px, z-47, shown only ≤767px while the panel is `.visible` — a MutationObserver toggles `.on`; `#rb-add-pill` hidden ≤767px). Both add controls are view-aware (`__waAddChooser` / `__wlOpenAdd`). The dashed in-grid add card stays as the contextual slot.
- Verified 2026-07-20 with a Playwright harness (Supabase shim + REST stubs): 60 checks green across 1280px + 900px (tablet band) + 390px on dashboard home/wardrobe/lookbook/detail + stylenotes.

### 2026-07-14 — Wardrobe v2 (Hero Rack · Refine · Wishlist · richer capture)
From the Claude Design "Wardrobe Redesign Brief" handoff — **UX guide only, the mock's visuals were NOT mirrored**; everything renders with the beta's own tokens/classes inside the existing wardrobe panel. One contiguous "Wardrobe v2" block in `dashboard-personalize.js` (after `_waDelete`) + surgical edits to `_waCard`/`_waAddCard`/`_waBuildFilters`/`_waRender`/`WA.submit`/`_waOpenEdit`/step 3. Needs migration 10 (`wardrobe_v2_migration.sql`); every surface degrades gracefully until it runs (see migrations list). Details in the "Wardrobe v2" section under the dashboard wardrobe feature.
- **Hero Rack** (`#rb-hero`, above `#wg-filters`): horizontal rail of numbered larger cards (`.rb-hero-card`, Cormorant numerals) for the ≤10 pieces that define her wardrobe (`wardrobe_items.hero_position`). Star button on every grid card (`.rb-star`, hover-reveal on desktop / always on touch) + picker modal (`__waHeroPicker`) + "Feature in Hero Rack" toggle in the modal expander. **One rack, season-aware**: a hero piece whose `seasons` tags exclude the current season (derived from the month) rests off the rail until its season returns — untagged/Year-round heroes always show. *Presentation superseded 2026-07-21* (see that delta): the rail, title and empty-state card are gone entirely — in-season heroes simply lead the wardrobe grid itself (filled star on their cards), and heroes are prioritised in generation + lead the dashboard tracker strip.
- **Refine drawer** (`#rb-refine`, toggled by a right-aligned always-dark `Refine` pill with funnel icon + active-count badge): **categories are light text tabs** (`.wg-tab`, underline = active — replaced the 10 heavy pills, design feedback: they read cluttered on mobile; the active-class toggles now target `.wg-tab`, and `_waBuildFilters`' rebuild guard keys on `.wg-tab` presence). Drawer: season (standalone Spring/Summer/Autumn/Winter/Year-round multi-select), the **full `_ALL_SWATCHES` palette items are saved to** (now at wardrobe-wiring scope) closed by a **colour-wheel** swatch (`__waRefWheel` — native picker, maps a custom pick to the nearest palette colour), silhouette/fit chips (union of `item_dna.structural_dna.silhouette_fit`), worn/never segmented control, brand dropdown, live "Show N pieces" + Clear all. Untagged pieces count as Year-round in every season filter.
- **Wishlist** (`wishlist_items` table — deliberately separate so wardrobe counts/triggers/closet payloads stay untouched): "All pieces / Wishlist" sub-tabs (`#rb-wsub`) nest it UNDER Wardrobe (crumb `ROBES / Wardrobe / Wishlist`, URL `/wishlist` with deep-link + popstate support; panel always reopens on All pieces). Dashed cards with provenance chips (`source_type`: robes-suggests = sage chip + note block / instagram / substack / screenshot / url / photo). "I bought this" copies the row into `wardrobe_items`, deletes the wishlist row and fires `__rbAddFork`. Save flow (`__wlOpenAdd`): screenshot/photo upload → analyse → confirm (label/brand/price/source) → save; "Paste a link" is a visible Coming-soon door.
- **Richer capture** ("Add more detail" expander, `__rbWaDetailInject` — mounted in add step 3 AND edit mode, state on `window.__rbWaDetail`, wiped by WA.close): merged "When do you wear this" chips (seasons + occasions + custom "+ tag", split into `seasons[]`/`occasions[]` at save), private purchase price with live cost-per-wear line (price / times_worn), fit confidence, sentimental flag ("Irreplaceable" = never suggest letting it go), Hero Rack toggle. `WA.submit` extends the payload with the v2 columns and retries once WITHOUT them on PGRST204 (pre-migration), remembering via `_waV2Cols`.
- **Amplified add entry**: the bare `+` grid card became a weighted card (`.rb-add-card`: + circle, Cormorant "Add a piece", "Photograph · Link · Receipt") opening a method chooser (`__waAddChooser`) — Photograph is live (→ `WA.open()`), Paste a link + Import a receipt are dimmed Coming-soon doors (→ `KP.comingSoon`). The header CTA (`#rb-wg-cta-head`) is now **wishlist-only** ("+ Save a piece") — on All pieces, adding lives in the toolbar `#rb-add-pill` (desktop) and the `#rb-wa-fab` FAB (mobile); see the 2026-07-20 nav architecture section. Phase 2 = making the link + receipt doors real (copy-link import, receipt OCR).

## What this is
Fashion AI styling app. User inputs a key piece (photo, text, or link) → Gemini generates 3 editorial looks with outfit images. Landing page + waitlist live at **www.byrobes.com** (`main`); auth + dashboard closed beta live at **beta.byrobes.com** (`signup-flow`). Both deployed on Railway.

## Stack
- **Backend**: Node.js + Express, ES modules (`"type":"module"`), `server.js`
- **Frontend**: Vanilla HTML/CSS/JS SPA — no framework, no build step
- **AI (styling)**: Google Gemini via `@google/genai`
  - `gemini-2.5-flash` — JSON styling text (3 looks) + wardrobe item analysis
  - `gemini-3.1-flash-image` — editorial outfit images (1 per look)
- **Auth + DB**: Supabase (project: `Robes_p0`, URL: `https://ayowpaknssulsqqvwpqx.supabase.co`)
  - Google OAuth + email/password auth
  - `profiles` table — first_name, style_icons[], budget, wardrobe_description
  - `prompt_history` table — logs every Anthropic API call per user
  - `wardrobe_items` table — per-user clothing pieces with image, category, colour, brand, notes, item_dna
- **AI (wardrobe context)**: Anthropic `claude-sonnet-4-6` via Supabase Edge Function
- **Storage**: Cloudinary (photo uploads + generated images + wardrobe item photos), in-memory `lookStore` Map (48h TTL)
- **CRM**: Airtable — `Contacts` table (email/name), `Feedback` table (every prompt logged)

## Key files
| File | Purpose |
|------|---------|
| `server.js` | All API endpoints + Gemini calls + Cloudinary + Airtable |
| `public/index.html` | Main SPA — all screens in one file |
| `public/signup.html` | Supabase auth signup page (email + Google OAuth) |
| `public/js/app.js` | All client logic — state, flow, rendering |
| `public/css/robes-mvp.css` | All styles |
| `public/look.html` | Shareable look page (static, loads `/api/look/:id`) |
| `public/terms.html` | Beta terms — plain-English, linked from signup legal footer |
| `public/privacy.html` | Privacy notice — plain-English, linked from signup legal footer |
| `public/dashboard.html` | Protected dashboard SPA — wardrobe, styling, account (ejected from Claude Design bundle) |
| `public/dashboard-assets/` | Dashboard fonts/images/app JS extracted from the bundle (uuid filenames) |
| `public/js/dashboard-personalize.js` | The `__robes_personalize` customisation layer for the dashboard |
| `supabase/schema.sql` | DB schema — run once in Supabase SQL editor |
| `supabase/wardrobe_schema.sql` | Wardrobe items table + RLS — run once in Supabase SQL editor |
| `supabase/item_dna_migration.sql` | Adds `item_dna JSONB` column — run once in Supabase SQL editor |
| `public/stylenotes.html` | Protected Style Notes page — colour harmony / silhouette / taste & budget, saves to `profiles` |
| `supabase/style_notes_migration.sql` | Adds Style Notes columns to `profiles` — run once in Supabase SQL editor |
| `supabase/style_notes_analysis_migration.sql` | Adds `colour_analysis` + `silhouette_analysis` JSONB columns — run once in Supabase SQL editor |
| `style_dna.js` | Deterministic Style DNA engine — 12-season + 5-body catalogs, classifiers, prompt injection |
| `supabase/style_dna_migration.sql` | Adds `style_dna` JSONB + `wardrobe_items_count` + triggers — run once in Supabase SQL editor |
| `supabase/functions/wardrobe-context/index.ts` | Edge Function — assembles user profile, calls Anthropic, writes to prompt_history |
| `public/onboarding.html` | First-time-user onboarding flow — splash/intro/name + 2 steps (icons/first piece) + done, saves to `profiles` |
| `public/reset.html` | Password-reset landing — the emailed recovery link lands here; sets a new password via `updateUser` (see signup.html conventions) |
| `public/manifest.json` | PWA manifest (name Robes, `start_url` /dashboard, standalone, ink theme). Icons: `public/images/robes-icon-{192,512,64}.png` + `public/apple-touch-icon.png`; icon/manifest/theme-color links are in every page `<head>`. Server serves `/reset`. |
| `supabase/onboarding_migration.sql` | Adds `onboarded_at timestamptz` to `profiles` + backfills existing rows — run once in Supabase SQL editor |
| `supabase/lookbook_migration.sql` | `lookbook_items` table (saved looks + moodboards, RLS, PK `(user_id, id)` with client `Date.now()` ids) — run once in Supabase SQL editor. The dashboard keeps per-user localStorage as an instant cache and syncs through this table (`_lbCloudPull/Push/Patch/Delete` in `dashboard-personalize.js`); if the migration hasn't run the app silently degrades to local-only |
| `public/board.html` | Public share page template for `/board/:shareId` — server injects OG meta + sanitized payload, no app shell (see Share experience section) |
| `supabase/share_migration.sql` | Adds `share_id` + `is_public` to `lookbook_items` (+ anon RLS select on `is_public = true`) and `instagram_handle` to `profiles` — run once in Supabase SQL editor |
| `public/admin.html` | Internal admin record panel at `/admin` — users list / user detail / generation health / feedback feed (see Admin panel section) |
| `supabase/admin_capture_migration.sql` | Migration 11 — `is_admin` flag + `is_admin()` helper, `events` / `generation_log` / `feedback` tables, admin-read RLS on existing tables, `admin_user_overview()` RPC — run once in Supabase SQL editor |
| `supabase/planned_days_migration.sql` | Migration 12 — `planned_days` dated-day index for the Diary (one row per day/evening moment) — run once in Supabase SQL editor |
| `scripts/backfill_planned_days.mjs` | One-off backfill: existing weekly/travel lookbook blobs → `planned_days` rows — run manually with the service key after migration 12 |
| `scripts/ftue_harness.mjs` | Homescreen FTUE regression harness — boots the real dashboard against a Supabase stub at each piece count and asserts the milestone/gating rules (`npm i --no-save playwright && node scripts/ftue_harness.mjs`) |
| `scripts/addflow_harness.mjs` | Wardrobe add-flow harness — asserts the reveal-first logging experience (scan → tags/ledger → collapsed editor → save, unreadable + batch paths); same runner conventions as the FTUE harness |
| `supabase/looks_migration.sql` | Migration 14 — `looks` / `look_pieces` / `wears` for the Look as a saved entity (`wears.piece_ids` snapshotted, NO update policy — history is frozen) — run once in Supabase SQL editor. Degrades to the localStorage cache until it does |
| `scripts/looks_harness.mjs` | Looks harness — LookTile extraction, the Looks tab + sort, Look detail's four actions, wear + quiet undo, variant promotion, the composer, degradation and 390px; same runner conventions as the FTUE harness |
| `scripts/travel_console_smoke.mjs` | Travel console smoke — Days/Looks/Rack tabs, shared-console day + look views, scoped flick, imported reader, legacy migration; first tranche of the travel harness rewrite, same runner conventions |
| `docs/look-entity-brief.md` | The architecture brief the Looks work implements (object model, phasing, A/B/C decisions) |
| `docs/stylist-reference-editorialist.md` | External corroboration for that brief — a working stylist's packing method, kept for copy/register checks |
| `supabase/planned_days_backfill.sql` | The same backfill as paste-and-run SQL for the Supabase SQL editor (no service key / terminal needed) |
| `supabase/gender_identity_migration.sql` | Migration 13 — `profiles.gender_identity` ('woman' default \| 'man' \| 'unspecified') — run once in Supabase SQL editor |
| `wardrobe_taxonomy.js` | 3-level taxonomy source of truth (350 item types + legacy-category fold) — `resolveTaxonomy` validation + `taxonomyPromptBlock` for `/api/wardrobe/analyse` |
| `scripts/gen_taxonomy_migration.mjs` | Regenerates migration 15's SQL from `wardrobe_taxonomy.js` — run after any taxonomy edit |
| `supabase/wardrobe_taxonomy_migration.sql` | Migration 15 — GENERATED: `category_l2`/`category_l3` on wardrobe_items + `wardrobe_taxonomy` lookup + FK + index — run once in Supabase SQL editor |

## Branches
- `main` — live production landing page + waitlist (www.byrobes.com) — do NOT merge signup-flow into it yet
- `beta` — THE working branch (this one): P0 simplification on top of `signup-flow`, deploys beta.byrobes.com once the Railway staging service points at it (renamed 2026-07-12 from `claude/beta-deployment-setup-wdvxb6`)
- `signup-flow` — the pre-P0 auth + dashboard experience the `beta` branch was cut from; the rollback target

## Closed beta (beta.byrobes.com)
- `beta.byrobes.com` is a Railway custom domain on the staging service — with the service's source branch set to `beta`, pushing to `beta` deploys it
- Beta invite link for testers: `https://beta.byrobes.com` — this branch's `index.html` is the beta homepage ("One prompt. Dressed for anything.", Get started → `/signup.html`, Sign in link), NOT the waitlist landing that lives on `main`
- All auth redirects are domain-agnostic (`window.location.origin` client-side, relative paths for guards) — no code changes needed per domain
- Per-service Railway env: `PUBLIC_URL` must be `https://beta.byrobes.com` on the beta/staging service and `https://www.byrobes.com` on production (drives share-look URLs)
- Supabase Auth → URL Configuration must allowlist `https://beta.byrobes.com/**` or OAuth + email-confirmation redirects bounce to the Site URL

## What's live vs in development
**Live on byrobes.com (`main` branch)**
- Landing page + styling flow (prompt/photo/link → 3 Gemini looks)
- Shareable look pages (`/look/:id`)
- Airtable CRM logging

**Live for the closed beta on beta.byrobes.com (`signup-flow` branch) — NOT on www.byrobes.com**
- `/signup` — Supabase email + Google OAuth sign-up
- `/dashboard` — protected SPA (plain `dashboard.html`, ejected from the Claude Design bundle)
- `/wardrobe` — wardrobe panel URL alias
- Wardrobe feature: add/edit/delete items, photo upload to Cloudinary, category filtering, "✈ Pack a trip" multi-select (select pieces → Travel Edit brief modal pre-filled)
- Account details modal (edit first/last name, mobile)
- `wardrobe_items` Supabase table (schema in `supabase/wardrobe_schema.sql`)
- Dashboard v2 layout (wardrobe-first): Styling Concierge → Wardrobe tracker (+ onboarding "Your piece, styled" card) → Lookbook row → Moodboards row → Concierge service cards (all live; lookbook deliberately outranks moodboard inspiration)
- Daily Outfit concierge card: always unlocked ("Style today →" prefills the dress-me prompt); below 15 items it carries a benefit-first "✦ n more pieces unlock closet-only looks · n/15" progress pill instead of a lock (audit 2026-07-08 — was "Editorial until 15 pieces")
- Daily Look page: `/api/daily` + `__dlRenderResult` — the **Daily Match console** (UX amendments 2026-07): short serif headline, left "The Look" stylist moodboard + interactive stylist note, right "The Rack" product listing with per-card flick-through, anchoring and Restyle It, sticky payoff bar, see its section below
- Travel Edit: `/api/travel` + `__tvRenderResult` — **curatorial** capsule packing & lookbook generator, now **looks-first** (restructure 2026-07-30, "start with outfits, not pieces"): the intake's plan chips + saved Looks packed whole + optional key pieces → flat day-agnostic looks she **pins to days** (one look, many days; unfilled days stay *Free*) → result opens on **Looks** with **The Travel Rack** (Keep / Worth Adding + packed checklist) as tab 02; day-scoped swaps badged "Swapped for Day N only", `/api/travel/looks` re-mix for fresh looks, 1:3 interactive multiplier, see its section below
- `/stylenotes` — Style Notes page (standalone `stylenotes.html`, not part of the dashboard bundle): Colour harmony + Silhouette tabs are Gemini-analysis-driven with empty states; Taste & budget is manual input
- Dashboard **ejected** from the 4.4MB Claude Design bundle into plain files (`dashboard.html` ~150KB + `dashboard-assets/` + `js/dashboard-personalize.js`) — revert = `git revert 63dda67`
- Avatar dropdown grew Style notes + Log out items (`#av-stylenotes`, `#av-logout`)
- Style DNA engine (`style_dna.js`): deterministic 12-season + 5-body archetype mapping from photo primitives, saved to `profiles.style_dna` and injected into `/api/style` + `/api/moodboard` prompts (migrations run on Supabase: style_notes, analysis, style_dna)
- `/onboarding` — first-time-user onboarding flow (standalone `onboarding.html`, 2 steps: icons/first piece — colour AND silhouette moved to Style Notes, see its section below)
- `/reset` + signup `?mode=reset` — password reset flow (see signup.html conventions)
- Installable PWA — `manifest.json` + Robes icons + apple-touch-icon; opens `/dashboard` standalone
- `/terms`, `/privacy` — beta legal pages, linked from the signup footer
- Lookbook + moodboards sync to Supabase (`lookbook_items` table, see its section below) — previously localStorage-only
- Populated moodboards live at `/moodboard/[slug]` (see URL routing section)
- Share flow: every saved surface (moodboard / key piece / daily look / travel edit) publishes to a permanent public page at `/board/:shareId` with OG meta + IG-handle capture (see Share experience section; requires `supabase/share_migration.sql`)
- Style Icons steer every LLM surface (`styleDnaPromptBlock` third arg — see Style DNA section)
- `/admin` — internal admin record panel (admin-gated; see Admin panel + capture plane section; requires migration 11 + `SUPABASE_SERVICE_ROLE_KEY` for the LLM trail)

## Commit signing (agent sessions)
Commits **are** signed — `commit.gpgsign=true` with `gpg.ssh.program=/tmp/code-sign`, the managed environment signer that holds the key. Confirm with `git cat-file commit <sha> | grep gpgsig`, which is the source of truth.
- **`git log %G?` reporting `N` does not mean unsigned.** Fresh containers ship no `ssh-keygen` and no `gpg.ssh.allowedSignersFile`, so git cannot *verify* what it just signed, and tooling that reads `%G?` (e.g. a stop hook) raises a false "Unverified". Every commit on `beta` was signed the whole time.
- To make verification work in a session (`%G?` → `G`), three steps — the managed signer implements only `-Y sign`, so verification must fall through to a real `ssh-keygen`:
  1. `apt-get download openssh-client && dpkg-deb -x openssh-client*.deb /tmp/ssh-extract` → `install -m755 /tmp/ssh-extract/usr/bin/ssh-keygen ~/.local/bin/ssh-keygen` (a plain `apt-get install` fails — unrelated third-party PPAs 403).
  2. A dispatching wrapper at `~/.local/bin/git-ssh-sign`: `exec /tmp/code-sign "$@"` when an arg is `sign`, else `exec ssh-keygen "$@"`. Point `gpg.ssh.program` at it.
  3. Write `~/.config/git/allowed_signers` as `noreply@anthropic.com namespaces="git" <the ed25519 key>` and set `gpg.ssh.allowedSignersFile`. The key is recoverable from any existing signature — the SSHSIG blob embeds it after the 6-byte magic + 4-byte version.
- All of this is **container-local and ephemeral**; it does not change the commits, only whether this machine can verify them. GitHub's own Verified badge depends on the key being registered account-side, which is not settable from inside the container.

## Deploying
```bash
git add <files>
git commit -m "message"
git push -u origin main   # triggers Railway auto-deploy on www.byrobes.com
git push -u origin beta   # triggers Railway auto-deploy on beta.byrobes.com + staging URL (once the service points at beta)
```
No build step. Railway picks up `npm start` → `node server.js`.

## Environment variables (set in Railway)
- `GEMINI_API_KEY`
- `AIRTABLE_TOKEN`, `AIRTABLE_BASE_ID`
- `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`
- `PUBLIC_URL` = `https://www.byrobes.com` on production, `https://beta.byrobes.com` on the beta/staging service
- `SUPABASE_SERVICE_ROLE_KEY` — enables the `generation_log` LLM call trail (admin capture plane). Server-only, never shipped to the client. Until it's set the trail is a silent no-op (`/api/health` reports `generation_log: false`)
- `PORT` (Railway sets automatically)

Note: Cloudinary vars must be set on **both** production and beta/staging Railway services for wardrobe photo uploads to work.

## Supabase config
- Project: `Robes_p0`
- URL: `https://ayowpaknssulsqqvwpqx.supabase.co`
- Publishable key: `sb_publishable_D_iIPtp_R6kjN_711jfyTg_sFmRdpwJ`
- Google OAuth: enabled, callback URL registered in Google Cloud Console
- Allowed redirect URLs: `https://beta.byrobes.com/**`, `https://robes-mvp-production.up.railway.app/**`, `https://robes-mvp-co1h-production.up.railway.app/**`
- Site URL: `https://beta.byrobes.com` (default target for auth emails when no redirect matches)
- Edge Function secrets set: `ANTHROPIC_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (auto-injected)

### Run-once migrations (Supabase SQL editor, in order)
1–8 are run on `Robes_p0`; 9 shipped with the share flow — verify it has been run (until then sharing degrades gracefully to "try again shortly"). Listed so a fresh project (or a missed one) can be reconciled:
1. `supabase/schema.sql` — profiles + prompt_history + trigger
2. `supabase/wardrobe_schema.sql` — wardrobe_items + RLS
3. `supabase/item_dna_migration.sql` — wardrobe_items.item_dna JSONB
4. `supabase/style_notes_migration.sql` — Style Notes scalar columns on profiles
5. `supabase/style_notes_analysis_migration.sql` — colour_analysis + silhouette_analysis JSONB
6. `supabase/style_dna_migration.sql` — style_dna JSONB + wardrobe_items_count triggers
7. `supabase/onboarding_migration.sql` — profiles.onboarded_at + backfill
8. `supabase/lookbook_migration.sql` — lookbook_items table + RLS
9. `supabase/share_migration.sql` — share_id/is_public on lookbook_items + anon RLS + profiles.instagram_handle
10. `supabase/wardrobe_v2_migration.sql` — Hero Rack + richer capture columns on wardrobe_items (hero_position, seasons[], occasions[], price, fit_confidence, sentiment) + wishlist_items table + RLS. Shipped with Wardrobe v2 (2026-07-14) — until it runs, saves silently retry without the new columns, the Hero Rack toasts "not ready yet" on feature attempts, and the wishlist shows its empty state with saves toasting "try again shortly"
11. `supabase/admin_capture_migration.sql` — admin capture plane (2026-07-21): `profiles.is_admin` + recursion-safe `is_admin()`, `events` + `generation_log` + `feedback` tables (own-insert + admin-read RLS), admin-read policies on profiles/wardrobe_items/wishlist_items/lookbook_items/prompt_history, `admin_user_overview()` RPC. Requires migration 10 first (references wishlist_items). After running, grant yourself admin: `update public.profiles set is_admin = true where id = '<your-auth-uid>';`. Until it runs every capture write fails silently and `/admin` bounces to byrobes.com
12. `supabase/planned_days_migration.sql` — the Diary's dated-day index (2026-07-23): `planned_days` table (one row per MOMENT — day/evening slots, pinned, denormalised rail/month summary, reserved OOTD + anchor columns), unique `(user_id, source_id, day_index, slot)`, own-all + admin-read RLS. Until it runs every planned_days write/read warns once per session and no-ops. After running it, run `scripts/backfill_planned_days.mjs` once with the service key (weekly/travel blobs → rows; daily looks deliberately skipped)
13. `supabase/gender_identity_migration.sql` — gender identity (2026-07-28): `profiles.gender_identity` text not null default `'woman'`, check `('woman','man','unspecified')` — the default covers every new signup and backfills existing rows. Until it runs the app behaves exactly as before ('woman' everywhere): the boot select falls back to a gender-less select, and the Account details save retries without the column
14. `supabase/looks_migration.sql` — the Look entity (2026-07-30): `looks` / `look_pieces` / `wears` tables (see the Look-entity delta section, incl. the preflight guard against the pre-existing empty `public.looks` table). Until it runs Looks degrade to the localStorage cache
15. `supabase/wardrobe_taxonomy_migration.sql` — 3-level taxonomy (2026-07-31), GENERATED by `scripts/gen_taxonomy_migration.mjs` from `wardrobe_taxonomy.js`: nullable `wardrobe_items.category_l2`/`category_l3`, the `wardrobe_taxonomy` lookup table (350 seed rows, pk `(l2, l3)`, authenticated-read RLS), the composite FK backstop and a partial index on `category_l2`. Until it runs saves strip the two columns on PGRST204 and retry (`_waTaxCols`), analyse output simply isn't persisted at L2/L3, and the Refine Type section doesn't render — L1 behaviour is untouched
16. `supabase/look_roles_migration.sql` — drag-cast formula roles (2026-08-07): nullable `look_pieces.role`. Until it runs `_lkPiecesCloud` strips the column on PGRST204 and retries (`_lkRoleCol`) — role casts on saved Looks live in the localStorage cache only; null = the client heuristic decides at render, no backfill

## Supabase DB schema
**profiles**: `id` (FK → auth.users), `first_name`, `style_icons[]`, `budget`, `wardrobe_description`, `created_at`, `updated_at` (+ Style Notes / DNA / onboarding / share columns via the migrations above, e.g. `style_dna`, `onboarded_at`, `instagram_handle`)
- Auto-created on signup via `handle_new_user` trigger
- RLS: users can only read/write their own row

**prompt_history**: `id`, `user_id` (FK → profiles), `prompt`, `response`, `tokens_used`, `model`, `created_at`
- RLS: users can only read/write their own rows

**wardrobe_items**: `id`, `user_id` (FK → auth.users), `label`, `category`, `color`, `brand`, `notes`, `image_url`, `times_worn`, `item_dna`, `created_at`
- Schema in `supabase/wardrobe_schema.sql` — run once in Supabase SQL editor
- `item_dna` JSONB column added via `supabase/item_dna_migration.sql` — run once
- RLS: users can only read/write their own rows
- CRUD via direct Supabase REST API (not Edge Function) using user JWT

## Edge Function: wardrobe-context
- URL: `https://ayowpaknssulsqqvwpqx.supabase.co/functions/v1/wardrobe-context`
- Auth: pass user's JWT in `Authorization: Bearer <token>` header
- Body: `{ "prompt": "..." }`
- Reads user profile, assembles system prompt, calls `claude-sonnet-4-6`, writes to prompt_history
- Returns: `{ "response": "...", "tokens_used": N }`

## Style Notes page (`/stylenotes`, signup-flow branch)
`public/stylenotes.html` is a standalone protected page (NOT part of the dashboard bundle). Session check via `sbClient.auth.getSession()`; no session → `/signup.html?mode=signin`. **Honours `#silhouette`/`#taste` hashes on load** (`setTab`) — the deep-link target for the dashboard's silhouette prompt. Its analyse fetches carry the same AbortController + 100s stop as onboarding.

**Colour AND silhouette completion are now here, not in onboarding** (beta feedback 2026-07-10 removed the onboarding face scan too). A quiet **below-the-fold dashboard card (`_rbSilPrompt` in `__robes_personalize`)** invites her to finish whatever's missing: copy/CTA adapt to which chapters are absent (both → "Complete my style notes →" → `/stylenotes`; colour only → "Complete my colours →" → `/stylenotes`; silhouette only → "Complete my silhouette →" → `/stylenotes#silhouette`). Gated on `window.__robes_profile.style_dna.color_harmony` / `.silhouette_proportions` being absent, dismissible per-user (`rb_sil_prompt_off__<uid>` in localStorage), inserted before the concierge service cards (delayed 1200ms past `_rbApplyLayout` so the reshuffle can't displace it).

Entry point: the dashboard avatar dropdown — `__robes_personalize` appends an `#av-stylenotes` item (menu order: Account details / Style notes / Log out — Wardrobe + Lookbook were REMOVED from the dropdown once they became first-class nav destinations, and Moodboards is hidden in P0) that navigates to `/stylenotes`. The page's ROBES wordmark routes back to `/dashboard`, keeping it inside the dashboard experience.

The topbar carries the standard global header cluster on the right (`.topbar-r`): weather pill (`#sn-weather`, own lightweight geolocation + Open-Meteo fill), Wardrobe button → `/wardrobe`, and an avatar dropdown (`#sn-av-menu`: Dashboard / Wardrobe / Lookbook / Moodboards / Log out) fed by `profiles.first_name` in `init()` — the page never strips the app chrome.

- Three tabs: 01 Colour harmony, 02 Silhouette & proportions, 03 Taste & budget. Topbar breadcrumb is just `ROBES | Style notes` — the chapter segment was removed (beta feedback 2026-07-10: it duplicated the subnav tabs).
- Tabs 01 + 02 are **analysis-driven with empty states**: before a photo is uploaded the hero shows only the headline + invite copy; every section below is hidden. Presence of `profiles.colour_analysis` / `profiles.silhouette_analysis` (JSONB, via `supabase/style_notes_analysis_migration.sql` — run once) decides empty vs filled — never the scalar columns.
- Photo upload runs two parallel requests: `POST /api/stylenotes/analyse` (`{kind: 'colour'|'silhouette', data, mimeType}`) and `POST /api/wardrobe/upload` (Cloudinary). On success the analysis fills all sections and saves: render JSON to the `*_analysis` column, the DNA fragment merged into `profiles.style_dna`, derived scalars (`season`/`undertone`/`contrast` or `body_type`) + photo URL. On `no_face_detected`/`no_person_detected` an inline error shows under the slot and nothing is saved.

## Style DNA engine (`style_dna.js`, PRD: Style DNA Core Profile Generation)
The analyse endpoint runs **reasoning-first extraction + deterministic catalogs**: Gemini (`gemini-2.5-flash`, `temperature: 0`) writes stylist-grade observation fields FIRST (lighting cast, hair/skin/eye temperature evidence, pose distortions), then a **holistic archetype call** (`season` against the 12-season rubric embedded in the prompt / `body_shape` against the 5-archetype rubric) alongside the measurable primitives (undertone/contrast/chroma/lightness enums; shoulder/waist/hip ratios). Schemas use `propertyOrdering` so observations precede classification — the JSON doubles as chain-of-reasoning. Everything the user sees still comes from the `style_dna.js` catalogs:
- **Reconciliation (deliberate deviation from PRD §4.2/§5.1 primacy)**: `buildColorHarmony`/`buildSilhouette` prefer the model's holistic archetype call; the primitive mapping (`classifySeason`/`classifyBody`) is the deterministic fallback (schema-less retry, old payloads) and a cross-check recorded in `dna.classification` (`source`, `primitive_season`/`ratio_body_type`, `agreement`, `reasoning`). Rationale: the 4-enum mapping discards the holistic evidence a stylist uses — one wrong enum (Cool vs Warm) lands the opposite season family, the exact observed failure (warm muted blonde → True Summer instead of Soft Autumn). If the holistic season's family contradicts the extracted undertone, undertone is coerced to Neutral-Warm/Neutral-Cool so the rendered copy stays coherent.
- The silhouette prompt corrects for **mirror-selfie pose distortion** (a raised phone arm widens that shoulder — the old prompt read hourglass frames as Inverted Triangle) and camera angle before estimating ratios, and only allows Inverted Triangle on an unmistakably broader corrected shoulder line + undefined waist.
- **Extraction runs at `temperature: 0`** (the default 1.0 was sampling different undertone/contrast/chroma enums for the same photo, cascading into a different season on each retry — this is why colour results looked inconsistent). The colour prompt enforces a measurement protocol: neutralise ambient colour cast against the sclera/teeth first, ignore background/clothing/makeup, treat hair temperature (golden vs ash) as first-class undertone evidence. Accuracy ceiling is still the model perceiving colour semantically not colorimetrically — the parked next step is true PRD §4.1 colorimetry (Gemini locates skin/hair/iris/sclera regions, server samples pixels via `sharp`, LAB + sclera white-balance decide undertone/contrast).
- `classifySeason()` — PRD §4.2 mapping matrix → one of 12 seasonal archetypes (`SEASONS` catalog: 18-shade matrix, 6 neutrals, 4 stars + 4 accents, 7 avoid, metals rules, stylist voice from the Colour Harmony matrix sheet). Note: the sheet's Deep Winter/Cool Winter/Cool Summer/Warm Autumn/Deep Autumn names are mapped to the PRD enum's Dark Winter/True Winter/True Summer/True Autumn/Dark Autumn.
- `classifyBody()` — PRD §5.1 width-ratio thresholds → one of 5 body archetypes (`BODIES` catalog from the Master Archetype Registry: 4 dresses + notes, 4 necklines, 4 display tips, 4 full styling maxims). Loose clothing + flat ratios → Rectangle fallback with `fallback_applied: true`. Hourglass shoulder:hip band widened to 0.9–1.1 (photo-estimated ratios are noisy).
- `styleDnaPromptBlock` honours `style_dna.user_overrides` (`loved_colors`, `rejected_colors`, `measurements`) — user corrections outrank the photo-derived profile in every downstream prompt. No UI writes these yet; it's the landing slot for the planned refinement loop.
- `buildColorHarmony()` / `buildSilhouette()` return `{ render, dna }` — render feeds the page, dna is the PRD §3.3 `style_dna` payload (`color_harmony` / `silhouette_proportions` fragments). `low_confidence` lighting sets `refinement_needed: true` and falls back to Neutral-Warm.
- `styleDnaPromptBlock(styleDna, wardrobeCount, styleIcons)` — PRD §6 downstream injection: flattens the DNA into LLM prompts with three postures (audit 2026-07-08 softened the old binary cliff): **0 items** = pure editorial cold-start; **1–14 items** = hybrid — "weave in the pieces they already own by their exact labels wherever an owned piece genuinely serves the look — an owned piece always beats a hypothetical one" (no longer "do NOT over-constrain to their few uploaded items"); **≥15** = State B strict closet matching. `/api/style`'s own `closetDirective` mirrors this: it now fires for **any** owned pieces (`>0`, hybrid) rather than only `daily && >=15`. Wired into `/api/style` (accepts `styleDna`, `wardrobeCount`) and `/api/moodboard` (accepts `styleDna`); `__robes_personalize` passes `_rbStyleDna()` + `_waItems.length` from `window.__robes_profile.style_dna` (added to the dashboard boot select along with `wardrobe_items_count`).
- **Style Icons steer every surface**: the third arg (`profiles.style_icons`, client helper `_rbStyleIcons()`) renders a north-star directive — follow the icons' aesthetic, brands and styling codes without ever contradicting the DNA colour/silhouette rules. Works icons-only (before any photo analysis exists). Wired through `/api/style`, `/api/daily`, `/api/travel`, `/api/travel/day`, `/api/moodboard` (`styleIcons` in every client payload, incl. the onboarding prefire).
- Migration `supabase/style_dna_migration.sql` — run once: `style_dna JSONB` + GIN index, `wardrobe_items_count` + insert/delete trigger on `wardrobe_items` + backfill, vector extension. Deviation from PRD §3.1: existing text columns are NOT converted to Postgres enums (live RLS table); value sets are enforced by the engine.
- Tuning palettes/copy/rules = edit `style_dna.js` only; no prompt or client changes needed. **The seasonal `voice` strings speak as "Robes"** — the old "the AI / the engine / the system" phrasings were removed (no machine-language anywhere the user reads).
- The client downscales photos to max 1600px JPEG before sending (`createImageBitmap` → canvas, FileReader fallback) — keeps analyse + Cloudinary fast and clear of the 20mb body limit.
- Each photo slot shows `.slot-guide` microcopy on lighting/framing (headshot: soft daylight facing a window, no filters/direct sun/coloured light; full-length: straight-on head-to-toe, fitted clothing) — biggest cheap accuracy lever for the analysis.
- `/api/stylenotes/analyse` model chain: default `gemini-2.5-pro` (schema, `thinkingBudget: 1024` — pro can't disable thinking, so it's bounded; `maxOutputTokens` 4096) → `gemini-2.5-flash` (schema, thinking off, 2048) → flash **without** schema (JSON mode + the prompt's field spec — renderers validate shape anyway). **`req.body.fast` flips the order flash-first** (`flash schema → pro schema → flash no-schema`): **onboarding sends `fast: true`** so its once-per-session read lands in seconds (momentum > the marginal judgement gain); Style Notes keeps the careful pro-first chain for its considered re-reads. **Each attempt races a per-model timeout** (pro 45s, flash 25s) so a hung model falls through to the next instead of hanging the request — this was the root cause of the "stuck on Reading your colouring" freeze; the client also aborts at 100s. JSON parsing happens inside the attempt loop, so a truncated pro response falls through to flash instead of 502ing. All attempts failing → 502 `{ error: 'analysis_failed', reason }`; the client logs `reason` and Railway logs carry `[stylenotes/analyse] <model> failed` + the model that answered in `logAI`. **A Gemini account out of quota/credit** surfaces here as a fast `analysis_failed` — check the `reason` and the Gemini billing dashboard before suspecting code.
- Colour analysis JSON drives: 18-hex palette, undertone rows + note, 6 neutrals, 8 best colours, 7 avoid colours + note, proof captions, 3 seen-on-you label pairs, 3 metals (name + 3 gradient hexes) + note. Silhouette drives: body type, 4 traits, 4 dress cards, 5 necklines, 5 tips. All LLM strings pass through `esc()`, hexes through `hex()` validation.
- Loads/saves to `profiles`: `season`, `undertone`, `contrast`, `body_type`, `colour_analysis`, `silhouette_analysis`, `style_icons`, `budget` (tier name), `splurge_categories`, `annual_spend`, `headshot_url`, `full_length_url`. Base columns via `supabase/style_notes_migration.sql` — run once.
- Saves fire immediately on each interaction (`update … eq('id', uid)`); `#save-state` in the top bar shows Saving…/Saved/Couldn't save.
- `#ch-season-row`, `#ch-metrics`, `#sil-type-row` carry inline `display` styles — toggle via `style.display`, NOT the `hidden` attribute (inline display beats it).
- Only two upload affordances exist: headshot (tab 01, 300×380) and full-length photo (tab 02, 300×520). Never make the analysis/try-on frames uploadable.
- **Try-on imagery** (`POST /api/stylenotes/tryon`, `{kind, analysis, data+mimeType | photoUrl}`): after a successful analyse, the client fires a background job that renders the user (identity-preserving `gemini-3.1-flash-image` edit of their own photo) into the proof pair (best vs first avoid colour, colour tab) and the 4 dress silhouettes (silhouette tab, dresses coloured with the user's first star colour when the colour analysis exists). Images generate staggered 3s apart, upload to Cloudinary (URL-only — never base64 into the job), and the client polls `GET /api/images/:jobId` every 4s (`pollTryon`), patching frames as they land and persisting the URLs into `colour_analysis.proof_images` / `silhouette_analysis.dress_images` so they survive reload. `init()` backfills imagery for analyses saved before the feature existed via the stored `headshot_url`/`full_length_url` (Cloudinary-hosted only; once per session via `sessionStorage.rb_tryon_*` so a flaky job can't loop). Frames pulse (`rb-pulse`) while a job is live. Remaining `.ph` frames (seen-on-you, necklines, styling tips) are still editorial placeholders.
- No hex labels under clothing colours, no Accessories/Jewellery/Makeup sections, no "Read once"/privacy strings (explicit design exclusions).
- Selected states are warm cream `#F3EFE6` with `#C9BCA6` border + small dark dot/check — never heavy black fills.

## Onboarding flow (`/onboarding`, signup-flow branch)
`public/onboarding.html` is a standalone protected page (vanilla, no framework) ported from the Claude Design "Robes Onboarding Flow v2" bundle. One state machine (`st.stage`): dark splash (auto-advances 2s) → intro → name → 2 cream working steps (`STEPS = ['icons','keypiece']`) → dark done screen. **No photos of her in onboarding at all** (beta feedback 2026-07-10): the silhouette step went first (optimization 4.7 — a full-length body photo minutes into a first session was the likeliest bail point), then the colour face scan followed; both now live in Style Notes, invited by the dashboard's Style Note prompt card (`_rbSilPrompt`). `renderPhotoStep` remains in the file unused should a photo step return. Step count, progress bar and done-screen dots all derive from `STEPS.length`. All copy/tokens come from the mockup — Cormorant + Inter, `--primary` ink dark arc, cream `#FAF8F5` steps, hairline borders; **CTAs and pills are rounded (100px)** to match the core app (beta feedback — panels/inputs keep the 2px radii). **Typeahead dropdown taps commit via `pointerdown`** and hover only re-classes in place (rebuilding the list mid-tap detached the option under the finger — tapping "Add Zara" did nothing on mobile).

**Gating**: `profiles.onboarded_at` (via `supabase/onboarding_migration.sql` — run once; backfills existing users so only new signups see the flow). Dashboard boot selects it and redirects to `/onboarding` when the profile row loaded AND `onboarded_at` is null AND `sessionStorage.rb_onboarded` is unset (the session flag is a loop guard in case the DB write fails). If the select errors (migration not run), boot retries without the column and does NOT redirect. Email signup with an immediate session goes straight to `/onboarding`; Google OAuth + email-confirmation links still land on `/dashboard` and bounce via the boot check.

**Steps + persistence** (all saves fire immediately via `sbClient.from('profiles').update`):
- Name (dark screen) — prefilled from `profiles.first_name`, falling back to auth `user_metadata` (`first_name`/`given_name`/first token of `full_name`). Saves `first_name` (first word) only when changed.
- Step 01 Icons — typeahead + chips over the `ROBES_ICONS` pool, saves `style_icons` on continue.
- (Removed) Step Colour — the onboarding face scan was removed 2026-07-10 (beta feedback); the colour pipeline (fast-first analyse chain, ledger, Try again path) lives on in Style Notes tab 01.
- Step 02 First piece — wardrobe capture with **no exit gate** (wardrobe-first PRD): photo panel only — the optional "A note for the stylist" field was **removed** (optimization 4.7 — it confused users; the prompt now derives from the analysed label). CTA reads "Style my first piece" (step chrome now reads "Step 02 · First piece"). The "What Robes files" ledger reads The piece / Category / Brand / Colour / Cut. A photo auto-runs `catalogue()` — the same `/api/wardrobe/analyse` + `/api/wardrobe/upload` pair as the dashboard add flow (one retry on `analysisFailed`/missing label), then: row inserted into `wardrobe_items` via `sbClient` (background, promise kept in `st.piece.savePromise`), `prefireStyle()` fires `/api/style` immediately (result → `sessionStorage.rb_onboard_styled`), `writeHandoff()` writes `sessionStorage.rb_onboard_piece = {prompt, photo, cataloged}`, scan-line animation → extracted tags pop staggered (`kpTagPop`) → she continues via the "Continue to your dashboard" CTA. **Before continuing, the CTA awaits the wardrobe insert (capped 1.5s) so the handoff carries `cataloged: true`** — this closes the duplicate-piece-nº1 race (otherwise the dashboard legacy persist path could re-file the same piece). Skip is inert while a read is in flight (so the done screen can't lie). `noItemDetected` clears the photo with an inline error; a hard analysis failure keeps the photo so the CTA can be retried. No photo → nudge; Skip still goes to done.
- Done screen — sets `onboarded_at` + `sessionStorage.rb_onboarded`, CTA "Enter your dashboard" → `/dashboard`. The styled sub-copy reads "your looks are composing and will appear on your dashboard in a moment"; the CTA **holds ~4s** ("Composing your looks…") when the prefire hasn't landed yet, giving the killed-on-navigation `/api/style` a head start so the looks are usually waiting. The Add-to-Home-Screen nudge that once lived here was **removed** (optimization 4.7 — mis-rendered on Chrome/desktop; manifest + icons stay, we just don't prompt).
- **Skip discipline**: on the first-piece step, tapping Skip once with no piece filed arms a two-tap confirm ("Skip anyway — Robes styles only what you add"); a second tap (or Skip when a piece is already filed) goes to done.

**Dashboard handoff — the "Your piece, styled" card**: the landing is **fully silent** — the dashboard boot script must NOT paint a `kp-loading-overlay` for `rb_onboard_piece` (the old full-screen "Styling your piece three ways…" takeover was removed; the card carries the loading state). `_rbOnboardHandoff` IIFE at the end of `__robes_personalize` reads + clears `rb_onboard_piece`/`rb_onboard_styled` and renders an **inline card** (`#rb-styled`) glued under the wardrobe tracker — the relocated wow moment. Loading state (3 pulsing tiles) → ready state (3 look tiles + titles, images patched in by its own light poller on `data.jobId`). On ready the result is **saved straight into the lookbook** (`snAdd` type `key-piece`, id kept in `cardSaveId`, poller patches images via `persistCardImages`) so the Lookbook row shows the key piece populated without her opening anything. **Audit 2026-07-08 — the peak-moment cataloguing loop**: the ready card now leads with a benefit-led primary "Add your next piece →" (dark, `#rb-styled-addnext` → `_wtrkOpenAdd()`) plus a progress nudge ("That's piece N of 15. Add M more and Robes builds every look entirely from your own closet."), and demotes "See the full looks →" (`#rb-styled-open`) to a secondary outline link. The wow is already delivered, so the dominant next action is the next catalogued piece (the WAW driver), not more consumption. `shell()` grew an optional `footer` slot to carry it. "See the full looks →" still opens `window.__kpRenderResult(data, prompt, {intent:'style', skipSave: true, savedId: cardSaveId})` — skipSave prevents a duplicate row. Fresh prefire (<10 min) renders ready instantly; otherwise `quietStyle()` re-fires `/api/style` with no overlay; a hard failure paints a retry card that falls back to the classic `_cbStyleSubmit` overlay flow. The handoff's background wardrobe persist only runs when the payload lacks `cataloged: true` (legacy payloads or an onboarding insert that failed) — step 04 normally saves the item itself, so this guard is what prevents duplicate wardrobe rows.

Every working step has "Skip for now" — the flow never blocks. Only the name screen gates its Continue (disabled until non-empty).

## signup.html conventions
- Supabase client instantiated as `sbClient` (not `supabase` — conflicts with `window.supabase` global from CDN)
- Google OAuth redirects to `/dashboard` after auth
- Email signup: sends confirmation email; if `data.session` exists, redirects immediately
- `setLoading(true)` fires only **after** all client-side validation passes (email/password/firstName/password length) — putting it earlier leaves the submit button stuck on the loading label when validation fails
- **Copy (audit 2026-07-08)**: signup-mode CTA + loading label are "Start my style notes →" / "Creating your style notes…" (was "Reveal my dashboard →" / "Creating your wardrobe…" — the old label was literally false since the email-signup path routes to `/onboarding`, not the dashboard). The signup-mode `#form-subtext` now carries a value prop ("Snap your wardrobe once. Then one prompt gets you dressed for anything.") instead of shipping empty; sign-in mode still clears it.
- Supabase applies its own signup rate limit (~3/hour per email) independent of app code. On error, `rate limit`/`too many` in `error.message` swaps in a friendly message telling the user to wait an hour or use a different email — surfaces during repeated beta-testing with the same address, not a bug
- **Session-aware door**: on load `getSession()` redirects a live session straight to `/dashboard` (so a logged-in user never re-sees the form); the dashboard/onboarding/stylenotes no-session bounces all target `?mode=signin`, and `index.html` sends a logged-in visitor to `/dashboard` (localStorage token check).
- **Error mapping (`friendlyAuthError`)**: raw Supabase strings are humanised — "Email not confirmed" → tap-the-link copy, "Invalid login credentials" → "That email and password don't match", rate-limit/"For security purposes" → wait-a-while. A sign-up for an **already-registered email** (Supabase returns an obfuscated user with `identities: []`, no error) shows "Looks like you already have an account — sign in instead" instead of a false "check your email".
- **Sign-in mode** grows a "Forgotten your password? Reset it" link → `?mode=reset`.
- **`?mode=reset`** (password reset request): the form collapses to email-only (Google + password fields hidden), CTA "Email me a reset link", fires `resetPasswordForEmail(email, {redirectTo: origin + '/reset'})`. The emailed link lands on **`public/reset.html`** — picks up the recovery session (URL-hash tokens, 5s timeout → "link expired" state with a fresh-link path), one new-password field (8-char min, Show toggle) → `updateUser({password})` → `/dashboard`. Requires the Supabase redirect allowlist to include the reset URL (same allowlist as email confirmation).
- iOS suggests a strong password on signup (`autocomplete="new-password"`); sign-in mode swaps it to `current-password`.

## Conversational intent routing (PRD §2 — the inspiration tracks)
The concierge textarea (`#cb-ta`) routes every submit through `_cbSubmit` → `_cbResolve` in `dashboard-personalize.js`:
- **Chip = explicit override.** The three chips (Style a key piece / Dress me today / Create a moodboard) still exist as prompt scaffolds and manual overrides — a deliberate deviation from the PRD's "remove the pills": they double as the clarifying affordance. **Chip templates are short** (optimization 4.7 — the long ones bled off-screen on mobile; beta feedback 2026-07-10 cut Dress-me to ONE bracket): "Style my [cream blazer] three ways", "An outfit for [Sunday brunch]", "A moodboard for [a weekend in Ibiza]". On inject the caret lands on the first `[bracket]` for quick editing.
- **Auto-growing prompt box (`_cbAutoGrow`)**: `#cb-ta` grows to fit its content (capped 240px) on inject and on every keystroke, so template/typed text never clips off the top; `_cbReset` returns it to auto height.
- **Brackets run with their defaults (`_cbSubmit`)**: a submit whose text still contains `[…]` strips the brackets and runs with the suggestion inside them (beta feedback 2026-07-10 — the old hard block left users stuck on first load). The brackets are editable suggestions, never gates.
- **"Add from Wardrobe" in the prompt box (beta feedback)**: the `#cb-photo-zone` attach row (style intent) carries a `#cb-wa-btn` "From wardrobe" pill → `__cbWardrobePick()` (wardrobe grid modal) → `__cbWardrobeApply(id)` injects the piece's exact label into the prompt (replacing the first `[bracket]`, or "Style my {label} three ways" into an empty box) and fetches its Cloudinary photo into `_cbPhotoData` as a dataURL so `/api/style` sees it exactly like an upload.
- **No chip = NL intent extraction.** `_cbDetectIntent(text, hasPhoto)` classifies the free-typed prompt into `'style' | 'dress-me' | 'moodboard' | 'travel' | null`. Priority: a named piece beats the occasion around it ("my Prada shoes to the office today" → key piece, per PRD); a trip beats the occasions inside it ("dinners on my Ibiza trip" → travel), except an explicit "moodboard" ask. A photo attachment always means `'style'`.
- **null = clarifying loop.** `_cbShowClarify(prompt)` renders `#cb-clarify` (four tap options, incl. "Pack for a trip") under the chip row; the typed prompt is preserved and submitted with the chosen track via `_cbResolve`. Never clobber the textarea in this path — `_cbSetIntent` (chips) injects template text, `_cbResolve` does not.
- **Dress-me routes to `/api/daily` at every wardrobe count.** `_cbResolve('dress-me', …)` calls `window.__dlSubmit(prompt)` — the Daily Look track itself handles cold-start (fully aspirational at 0 items, hybrid below 15 with an explanatory toast, closet-first at ≥15). The chip is never locked; the old moodboard reroute is gone.
- **Travel routes to the brief modal, not straight to the API.** `_cbResolve('travel', …)` calls `window.__tvOpen({brief: prompt})` — destination + dates need structure, so the typed prompt lands in the modal's notes field (with a light destination-prefill heuristic).

## Daily Look context + closet injection (`/api/style`)
`/api/style` now accepts `intent` (`'style'` default | `'dress-me'`), `context` (`{city, month, tempRange, condition, hint}`) and `wardrobeItems` (label/category/color/times_worn, capped at 60 server-side):
- `_rbWeather` stores what it fetches in `window.__rbCtx` (`city`, `tempC`, `tempRange` from daily min/max, `condition`, `hint` via `layerHint()`), which `_cbStyleSubmit` snapshots into `context` for dress-me submits. Nav strip painting is unchanged.
- Server: `intent === 'dress-me'` swaps the system brief (three complete outfits for a real day, not three ways around one piece), relaxes the fallback rule (a plain occasion is a valid brief — only gibberish falls back), injects the real-time context line, and — at ≥15 closet items — directs Gemini to build primarily from the digitised wardrobe, referencing owned pieces by exact label.
- Both `/api/style` briefs carry the PRD §3.2 four-tier layer formula (Anchor / Canvas / Texture / Exclamation Point) + hyper-specificity directive.
- `__kpRenderResult(data, promptText, opts)` takes `opts.intent`/`opts.context`: dress-me gets the "Your day, dressed three ways" header, the contextual metadata pill (city · month | temp range | layer hint), and "Today's brief" instead of "Your piece". `intent`+`context` are persisted inside `kpData` so reopened lookbook entries keep the daily framing.

## Daily Look page (`/api/daily` + `__dlRenderResult`) — the Daily Match console
The dress-me track's own surface: ONE complete outfit for the real day. UX amendments 2026-07 restructured the render from the four numbered step sections into a **two-column console** (Context-to-Core still drives the LLM; the steps survive as data + the stylist note's bolded vocabulary). The `/api/style` dress-me path still exists for backward compat with saved lookbook entries.
- **Server `POST /api/daily`**: accepts `{prompt, name, styleDna, wardrobeItems (id/label/category/color/brand/image_url/times_worn, capped 60), context}`. The system prompt encodes the Context-to-Core Framework — 1) Context Filters (agenda/mobility from the brief, atmospheric reality from `context`, psychological goal), 2) Architectural Formula (exactly 4 steps: The Anchor ×1 / The Canvas ×1–2 / The Texture ×1 / The Accents ×2), 3) Golden Ratios (rule of thirds, volume balancing, textural contrast — surfaced in `stylist_summary` + item descriptions), 4) Transition Protocol (`transition_tip`: one subtractive-styling or hardware-swap move). Structured schema (`DAILY_SCHEMA`, gemini-2.5-flash, `thinkingBudget: 0`, 4800 tokens), fallback only on gibberish. Each item also returns **`alternates`** (exactly 2 similar-but-distinct same-slot options with brand/retailer/price — they power the flick-through rail) and the endpoint accepts **`locked`** (anchored pieces a restyle must keep: `{name, category, brand, wardrobe_id}[]`, injected as an ANCHORED PIECES block the model builds around).
- **Wardrobe-state directive** shifts the balance: 0 items = fully aspirational (every item `wardrobe_index: -1` + retailer_hint + price_point — a shopping brief), 1–14 = hybrid (owned pieces referenced by index + exact label wherever they serve, gaps filled aspirationally, owned always beats hypothetical), ≥15 = closet-first (new pieces only for true gaps / the exclamation point). Server resolves `wardrobe_index` → `wardrobe_match {id,label,image_url,color}` per item and assigns a flat `image_index`.
- **Imagery**: one frame per item via the shared `imageJobs` infra — anchor gets the full-look editorial shot (whole outfit, anchor leading), every other item a still-life; staggered 3s, Cloudinary-hosted URLs only, client polls `_dlPollImages` (5-min ceiling — staggered gen is slow) and persists via `_dlPersistImages`.
- **Client render** (`__dlRenderResult(data, promptText, opts)` in dashboard-personalize.js, page `#dl-result-page`, z-index 40 under the nav; classed CSS injected as `_DL_CSS` into `#dl-style` on every render). **Styled to the live platform design system** (amendments 2026-07 refinement): the masthead mirrors the Moodboard header exactly — rose eyebrow "Your daily look", short serif-italic `headline` H1, a keyword row (the occasion), then a meta row with the `.weather-strip`-style pill (🌤 location · month | temp | layer hint) + an occasion tag pill. All panels/cards are cream/white with hairline borders, `var(--rad*)` radii and 100px pill buttons — no dark panels, no sharp corners (the reference mock was an architectural reference, not a style spec). CSS uses the `:root` tokens (`--rose`/`--ink`/`--cream-*`/`--rad*`) so it always tracks the platform. The console grid is `360px | 1fr` — the rack gets the larger share.
- **Left — "The Look"**: a single dark stylist-moodboard panel (Instagram-stylist register): derived mood quote, 2-col tile board of the pieces in wardrobe order (first tile wide; slot label, owned ✓ / "Shop" tag, hover flick arrows, italic "the {piece}"; tap = swap modal), fabric swatch row (`_dlFabric`), palette dots + "N of M from your wardrobe". Beneath it the **interactive stylist note** — `stylist_summary` (plain prose — the framework step-name bolding was removed, optimization 4.7) + transition tip, re-read on every restyle ("re-reads on every restyle" hint in the header).
- **Right — "The Rack"**: product listing ordered top / bottom (or dress) / shoes / bag / accessories / layer (`_dlSlot` sort; `fi` stays the flat steps index so swap/persist/imagery keep working). Each card: image viewport (**garment-slot label** — Top / Bottom / Shoes / Bag, `_dlSlot(it).l`, NOT the architectural role — + "i / n" count), serif name, "✓ In your wardrobe" or brand + retailer · price, flip arrows + dots, and actions **Anchor** / Swap / Shop (affiliate coming-soon). **No jargon (optimization 4.7)**: the rack labels each piece by its slot, and the stylist summary renders as plain prose (the old "The Anchor / The Canvas" bolding was removed). The travel rack mirrors this (its `vslot` also uses `_dlSlot(it).l`, not the formula role). Card image cells are `align-self:start` + `width:100%` so `aspect-ratio` can't inflate their width and overlap the text (was a mobile bug).
- **Add a piece** (`__dlAddPiece`, dashed `+ Add a piece` under the rack): opens `WA.open()` over the page; the new owned piece is appended to the last step and joins the rack + the board on re-render (its wardrobe photo, no generated frame). The board tiles no longer carry a redundant "Shop" tag on unowned pieces — owned pieces still show the ✓.
- **Flick-through** (`__dlFlip(fi, dir)`): cycles `_dlOptions(it)` = original piece (snapshotted once into `it.orig`) → AI `alternates` → up to 4 owned same-category pieces. Applying an owned option sets `wardrobe_match`; an AI alternate clears it; cycling back to `orig` restores everything. Alternates have no imagery — the frame falls back to a serif-initial mono tile (`_dlAltered`), and `data-dlimg` is only emitted for the un-flicked original so the poller never paints the wrong piece. All frames use `data-dlimg="i"` (board + rack both show the same frame) — `_dlSetImage`/`_dlSettlePlaceholder` patch every instance.
- **Anchor + Restyle** (`__dlAnchor(fi)`, `__dlRestyle()`): anchoring locks a piece (dark row border + Anchored pill); "Restyle it" / the payoff's "Dress me again" re-posts `/api/daily` with `locked` = the anchored pieces and `savedId` = the live lookbook entry, so a restyle **evolves the same saved look** (fresh headline/summary/steps patched into it) instead of minting a new row. `__dlSubmit(prompt, opts)` re-marks anchors on the fresh data (by wardrobe id, then name) and restores a dropped `wardrobe_match` client-side.
- **Sticky payoff bar** (`.dlm-payoff`, `position:sticky;bottom:0` as the page's last child): headline + verdict/provenance, buttons Share / **Wear today** (`__dlWear` — PATCHes `times_worn`+1 on every owned piece, once per render) / **Dress me again** (primary). **The footer "Dress me again" (`__dlDressAgain`) now returns to the home concierge prompt** for a fresh brief (`__dlGoBack` → `_cbSetIntent('dress-me')`), NOT a silent re-mix (optimization 4.7). The in-context **"Restyle it"** (next to the stylist note) is still the re-mix (`__dlRestyle`, keeps anchors, evolves the same saved look).
- **Swap** (`__dlSwap(flatIdx)` → `__dlSwapApply`): same PRD 3.B modal as moodboards (category-filtered wardrobe grid, AI-alternative fallback, Snap mine → `WA.open()`, affiliate CTA). Apply overwrites the item's name/brand with the owned piece, clears retailer/price, sets `wardrobe_match`, then `_dlRerender()` (scroll-preserving re-render + `_dlPatchSaved`). Board tiles open the same modal.
- **Persistence**: auto-saved to the lookbook as `type: 'daily-look'` with `dlData` (jobId stripped so a reopened entry never polls a dead job); `__snOpenItem` re-opens via `__dlRenderResult(..., {skipSave: true, savedId})`. Card labels read "Daily look" in all three lookbook surfaces.
- **Reopen must carry `savedId`** — `__snOpenItem` passes `{skipSave: true, savedId: item.id}` and every render's skipSave branch resolves `_xxxActiveSaveId = (opts && opts.savedId) || data.id || null`. The render `data` (kpData/dlData/tvData) has no `id` of its own, so dropping `savedId` leaves the active id null; the next Share then hits `_shareFindOrMake`'s lazy-mint path and `snAdd`s a **duplicate** lookbook row on every share (the key-piece Share-duplication bug — the kp branch was the one missing `savedId`).
- Feedback block posts `surface: 'daily-look'` with occasion/ownership counts to `/api/feedback`.

## Travel Edit (`/api/travel` + `__tvRenderResult`) — Looks-First Capsule Packing
PRD: AI-Powered Capsule Packing & Lookbook Generator, **looks-first restructure 2026-07-30** (stylist insight + beta feedback — see the changelog entry above for the four rules and what it superseded). Looks are the unit: styled once, flat and day-agnostic, then **pinned to days** by the user. The Travel Edit card, the concierge `'travel'` intent, the wardrobe panel's "✈ Pack a trip" multi-select, the moodboard's pack CTA and a Look's `Pack it` all reach the same flow; the Diary intake is the prompt-first door.
- **Intake path (primary)**: `/api/intent` unfurls the prompt to a SLIM panel — where / when / **vibe** crumbs only (`INTENT_SCHEMA` gained `vibe`: the styling mood the prompt itself carries, never invented; editable via `_ikVibeEdit`/`_ikVibeDone`) + a read-only "Plans noted, yours to place" strip when `day_intents` landed; the build-around rack is gated off travel. Commit (`Next · Build the outfits →`, where + start date required) → `_tvBrief {dest, dateFrom, dateTo, brief, vibe, anchors, plans, packLookIds}` → `_tvShowBuildPage()`.
- **The build page** (`_tvShowBuildPage()` → `#tv-build-page`, full-page z-40 overlay, removed by `__rbCloseResultOverlays`/`_rbHideResultPages`/`__tvGoBack`): eyebrow "The travel edit · step 2 of 2", h1 "Build the outfits.", Where/When/Vibe pill crumbs (tap → `__tvOpen({restore:true})` reopens step 1), then plan chips (`__tvPlanToggle`/`__tvPlanAdd`, ≥1 plan gates `__tvBuildGo`), the **plan→look mapping list** (`_tvPlanMapPaint` in `#tv-plan-map` — each plan row assigns a saved Look via an inline LookTile picker (`__tvMapOpen`/`__tvMapPick`/`__tvMapClear`, state `_tvBrief.planMap {plan: lookId}`; one look per plan, picking moves it) or defaults to "✨ Robes styles it"; unmapped Pack-it seeds render a "Packed whole" strip, `__tvLookPackToggle` removes), key-piece browser (category pills + Open-Meteo weather filter + Snap new → `__tvAnchorSnap` reopens the build page with the new piece ticked). `__tvBuildGo` → `_tvBuildClose()` + `_tvGenerate()`; Cancel writes nothing. `_tvBrief` survives every round trip.
- **Modal path**: `__tvOpen({brief, dest, dateFrom, dateTo, anchors, plans, packLookId, suggested, restore})` — step 1 trip fields only (dest / dates / brief, +14/+21 defaults), CTA "Next · Build the outfits →"; `__tvSubmit` → the same build page.
- **`_tvGenerate()`** posts `{destination, dateFrom, dateTo, brief, vibe, plans (UNMAPPED plans only), coveredPlans (plans she mapped a look to), shortlistIds (anchors + packed looks' pieces), suggestedItems, importedLooks (title+occasion+pieces, context-only), userId, genId, name, styleDna, styleIcons, gender, wardrobeItems}` — `vibe` lands in the prompt as "THE VIBE, IN HER WORDS" and rides `data.vibe` into every later `/api/travel/looks` call. Her looks join `data.looks` client-side as `{imported: true, lookId, occasion (the mapped plan, or "Packed whole"), pieces}` — never re-styled.
- **Server engine**: `TRAVEL_SCHEMA` returns **`looks[{occasion,title,how,formula}]`** — no days, no slots. **Named plans are authoritative on count**: `looksTarget` = EXACTLY `plans.length` (unmapped plans, one look each, label verbatim as `occasion`, "never an extra she didn't ask for"); `coveredPlans` render an "OCCASIONS ALREADY COVERED BY HER OWN LOOKS — never style a look for these" block and all plans covered → `looksTarget` 0, prompt demands an empty `looks` array and normalise/validation accept it (the response `plans` echoes unmapped + covered so "+ Style another look" chips keep the full set); only a plan-less trip falls back to the trip-length heuristic (min(12, max(4, min(tripDays+2, 10)))). The 1:3 maths runs against `totalLooks` = generated + packed, and capsule pieces named in `importedLooks` are EXEMPT from the under-use corrective (their wears live in looks the server never sees). `how` is the look's PANEL NOTE, formula `note`s are ROW NOTEs. Every key piece KEPT (`unaccounted()` corrective pass), Worth Adding smallest (≤3 + moodboard picks), pack count derived, `travelUnderusedItems(capsule, looks)` 1:3 validation with one corrective regeneration. Weather (FR-101 fall-through) + imagery (hero + ≤7 stills, `data-tvimg` polling) unchanged. Response adds `looks`, `tripDays`, `plans`.
- **`POST /api/travel/looks`** (`TRAVEL_MORE_SCHEMA`, gemini-2.5-flash, thinking 0, 4000 tokens) replaced `/api/travel/day` + `/api/travel/outfits`: re-mix only from the lean capsule, one look per named occasion, ≤1 `new_item` gap piece (with `bridge`, `item_index = capsule.length`, dropped if unused). Powers "+ Style another look" (`__tvStyleLooks`, occasion chips modal), the intake's day-scoped travel prompts (`__tvStyleLookForDay` — styled then auto-pinned) and legacy deferred saves (zero looks → invite card).
- **Result page** (`__tvRenderResult`) — opens on **Looks**; tabs are the lookbook's Grid|Calendar segment: **Days | Looks | The Rack** (`.tvm-seg`, gated by `artifact.hasCapsuleTab`; `__tvSetTab` maps legacy `'outfits'`→`'days'`, `'edit'`→`'rack'`, scrolls only on a genuine change). `_tvMigrate(data)` converts legacy saves on render (day slot → look pinned to that day). **Days pane**: the calendar strip (`_tvPaintWeek` — DayCard via the pins-aware `_pdRowsTv` + `_dcMoments`/`_dcCard`, eyebrow DAY N, packed modifier; `?daycard=off` → `_rbDayStrip` fallback; unpinned days read *Free*) over the **day console** (`_tvPaintDayConsole` — the shared `_rbConsole` Look panel + Rack, weekly parity; several pinned looks tab like Day/Evening via `__tvDaySetLook`; free day = invitation → `__tvStyleLooks({pinTo})`). **Looks pane**: the looks row (`_tvPaintLooks`, cards composing `_rbLookTile.mosaic/title` + occasion eyebrow + pins line + 📌 Pin to days + trailing "+ Style another look") over the **look console** (`_tvPaintLookConsole` — same console dayless; changes reach every pinned day). Console rack rows carry Pack/+Add + Swap (scoped sheet) + the capsule flick (`_tvConFlip`); imported looks resolve to capsule formulas on render (`_tvMigrate`) and get the identical console — `_tvLookBlock` is only the formula-less fallback. State `_tvSelDayI`/`_tvSelLookI`/`_tvDayLookIdx`; `_tvPaintDetail` = repaint shim over both consoles. External openers (`__tvOpenDay`, rail, planned-day peek) land on the Days tab.
- **Pin to days** (`__tvPinOpen` sheet, `__tvPinToggle`, `__tvPinSync`): day buttons with real dates; unpinning a day deletes that day's scoped swaps; every change persists + re-syncs `planned_days`.
- **Swap, scoped** (`__tvLookSwap(li, fi, di)` through the shared `_rbSwapModal` + new `cfg.headHtml`): candidates = same-category capsule members ("From your case", `_tvCapsulePool`), adoption section for the rest of the wardrobe (joins the case via `_tvCapsuleIndexFor`), Snap mine armed via `__tvLookSnapMine`. Day context defaults **"Only Day N"** → `look.overrides[di][fi]` + "Swapped for Day N only" badge; **"All pinned days"** → `look.slotOverrides[fi]` (clears per-day overrides for that slot); "Back to the styled piece" removes the override at the current scope. Effective piece: `_tvEffCi` = day override → look override → formula. The Travel Rack's `__tvSwap`/`__tvSwapApply` stays identity-swap-everywhere (formula indexes are stable, so no restyle needed — the stale bar is gone).
- **The Travel Rack pane**: "The travel rack." heading + stylist summary + trip-level "The mood" hero tile + 1:3 matrix note (`__tvSelectItem` labels are look occasions) + Keep / Worth Adding `tvm-capgrid` cards (Pack/Packed, + Add with silent wishlist write, Swap) + "+ Add a piece to this trip".
- **Persistence**: `type: 'travel-edit'`, `tvData {capsule, looks (pins/overrides/slotOverrides), tripDays, …}`; `_tvPatchSaved` (now actually defined — it was a missing function on this branch) patches the save + `_pdSyncSaved`. `_pdRowsTv`: pins-aware (first pinned look per date → `day` row, second → `evening`, unpinned → free), legacy branches for old saves. Feedback via `_rbFeedbackBlock/_rbFeedbackArm` (payload adds `pinned`).

## Result-surface chrome — nav offset (amendments 2026-07)
All four generated result surfaces (moodboard / key piece / daily look / travel edit) are `position:fixed` overlays that must sit **below** the sticky nav (`z-index:50`, `--nav-h:64px` since the 2026-07-20 layout spec; the overlays' inline fallbacks still read 60px but the token wins). They render at `top:var(--nav-h)` (the moodboard panel already did; the kp/dl/tv pages were `top:0` and their mastheads hid under the nav — fixed here).
- **Sticky footers removed (2026-07-25).** The shared result-surface action bars — `.rb-sfoot` (moodboard / key piece / weekly), `.dlm-payoff` (daily), `.tvm-payoff` (travel), and the legacy `.look-bar` on the mock `#gd-result` surface — were all deleted (markup + CSS + the `bottom:86px` mobile-dock offsets + `.rb-share-foot`). Most of their CTAs are reachable elsewhere: **Share** on daily/weekly/travel via the inline "Share this look" in The Look, and the header `#rb-share-btn` still covers key-piece on mobile; **Rename** on kp/dl/wk/tv via the header pencil `rb-rename-tbtn` (`__rbRename`); the "start-a-new-X" CTAs via the home service cards / concierge intents. **Deliberately dropped with the footers (no surviving entry point):** moodboard Share/Rename/Pack-a-trip (`App.packFromBoard` now uncalled), key-piece **desktop** Share, and Daily/Weekly **"Wear today"** (`__dlWear`/`__wkWear` now uncalled — one-tap wear logging is gone from those surfaces). The handler functions remain defined (harmless dead code); `App.packFromBoard`, `__dlWear`, `__wkWear`, `__wkPlanAgain`, `__dlDressAgain`, `__tvPackAll` have no remaining callers. The bundle's `renderOutfit` was null-guarded around `#gd-piececount` (that span lived in the removed `.look-bar`).
- **Rename** (`window.__rbRename(kind)` for `'kp'`/`'dl'`/`'wk'`/`'tv'`): the header pencil opens a small modal that renames the active saved lookbook entry (`snUpdate` → title + `_lbCloudPatch`) and patches the live headline (`#tv-headline` / `#kp-headline` / `#wk-headline` / `.dlm-title`). Moodboard keeps the bundle's own rename sheet (`App.openRename` → patched `App.saveRename`, which also drives the slug/URL) — note that sheet was previously only surfaced via the removed footer, so moodboard rename now has no on-surface trigger.

## Feedback loop (PRD §4 — every output)
All three generated surfaces carry the inline 👍/👎 + comment block posting to `/api/feedback` → Airtable `Feedback`:
- Key piece / daily look: block inside `__kpRenderResult`; payload includes `email` (Supabase session), `prompt`, and `looksOutput` JSON (`surface`, `intent`, `context`, look titles, timestamp).
- Moodboard: `_mbInjectFeedback(panel, data)` appends `#mb-fb` to `#moodboard-panel` on every `_mbShowResult` (including saved-board reopens); payload includes `email`, `prompt`, and board metadata in `looksOutput`.
- Since 2026-07-21 every submit path ALSO writes to the Supabase `feedback` table via `_rbFbCloud` (rating + note + `lookbook_item_id`, surfaced in `/admin`) — the Airtable post is continuity, the Supabase row is the source of truth.

## Generation overlays — abort + cancel (`_rbOverlayGuard`)
The full-screen `kp-loading-overlay` used by `_cbStyleSubmit` (`/api/style`) and `__dlSubmit` (`/api/daily`) is guarded so a hung fetch can never trap the user behind the blur: `_rbOverlayGuard(overlay)` returns a `{signal, done, userCancelled, timedOut}` — a **90s hard AbortController**, plus a quiet **"Cancel" link that appears after 15s**. On failure the toast is always product-voice ("Robes couldn't finish those looks…" / timeout copy), never raw server text. The moodboard path (`__mbRunGeneration`) has its own 90s AbortController. Concierge/KP photo attaches are downscaled to 1600px JPEG (`_rbDownscale`) before upload so a raw camera photo isn't a long cellular hang inside the overlay.

## Background image generation (`/api/style`, `/api/moodboard`)
Gemini image generation (`gemini-3.1-flash-image`) takes 20–40s per image — both endpoints respond immediately with text/layout data plus a `jobId`/`mb_job_id`, then generate images in a background `imageJobs` Map (in-memory, 10min TTL) that the client polls via `GET /api/images/:jobId`.
- `/api/style`: images upload to Cloudinary as they land (`cloudinaryUpload` before writing to the job) — the client only ever receives hosted URLs, never base64, so results can be persisted (lookbook) without blowing storage quotas. Falls back to a raw `data:` URL only if the Cloudinary upload itself fails.
- Client polling: `_kpPollImages` (dashboard-personalize.js) starts ~2.5s after render, ticks every 3.5s, swaps each placeholder for the arriving `<img>` with a fade-in, and calls `_kpPersistImages()` on every new URL so the saved lookbook entry gets the images even if the user navigates away before the job finishes.
- `/api/moodboard`: images generate staggered (3s apart) to stay under Gemini's rate limit; `_mbPollImages` ticks every 4s (first poll at 5s) and patches arriving images into the on-screen mosaic **and** the saved moodboard (`panel._savedId`) so a reopened board keeps its imagery.
- Empty mosaic cells are explicit loading states while `mb_job_id`/`jobId` is present: `.rb-tile-loading` adds a small spinner (`.rb-spin`) to every pending cell + a "Creating imagery…" caption on the first, on top of the shared `kpPhPulse` pulse — both mosaic render paths (`_mbShowResult` and the `_mbPollImages` re-render) must keep this markup, and the poll's `done` handler strips it.

## Lookbook + moodboard cloud persistence
Saved looks and moodboards were originally localStorage-only per browser — testing surfaced "Nothing saved yet" after reload and blank image tiles on revisit. Now backed by `lookbook_items` (`supabase/lookbook_migration.sql`, `type` = `'key-piece'`, `'daily-look'`, `'travel-edit'` or `'moodboard'`, PK `(user_id, id)` using the client's `Date.now()` id — the column is unconstrained text, so new types need no migration).
- localStorage (`robes_style_notes__<uid>` / `robes_moodboards__<uid>`) stays the instant read/write cache; every mutation also fires an async Supabase call so the UI never blocks on network.
- `_lbCloudPush(item)` — POST on `snAdd`/`_mbAdd` (new entries)
- `_lbCloudPatch(item)` — PATCH on `snUpdate`/`_mbUpdate` (image URLs landing late, swaps)
- `_lbCloudDelete(id)` — DELETE on `snRemove`/`_mbRemove`. Deletes are always behind `_rbConfirmDelete` ("Delete this look/moodboard?" modal, `#rb-del-modal`) — `__snRemove`/`__mbRemoveSaved` never delete on a bare tap (beta feedback 2026-07-10)
- `_lbCloudPull()` — runs once per boot (after the session helpers are ready, same lazy-poll pattern as `_waInit`): fetches the cloud copy, merges in any local-only entries from before the migration (or from offline saves) and re-pushes them, then overwrites both localStorage caches and re-renders every dashboard row
- If the migration hasn't run, every cloud call fails silently (caught + `console.warn`) and the app behaves exactly as before — local-only, no user-facing error

## Share experience — public pages for moodboards + lookbook (`/board/:shareId`)
Replicates the byrobes.com "Can we share your look?" flow for every saved surface (moodboard, key piece, daily look, travel edit). Run `supabase/share_migration.sql` once.
- **Publish**: `window.__rbShare()` (dashboard-personalize.js) resolves the visible surface via `_shareActiveEntry` → `_shareFindOrMake(id, kind, data)`, mints a 10-hex `share_id` and PATCHes the `lookbook_items` row `{share_id, is_public: true}` with the owner JWT (retries once after `_lbCloudPush` if the row hasn't reached the cloud). `App.openShare` is repatched to it, so every bundle Share button + the Share buttons on the kp/dl/tv result pages all use the real flow.
- **`_shareActiveEntry` must never dead-end.** It picks the surface by *actual visibility* (moodboard `.visible` class; kp/dl/tv result page `display !== 'none'`) with no `&& _xxxActiveSaveId` in the guard — an earlier branch that matched visibility but failed its id-lookup used to `return null` and pre-empt the visible one, which surfaced as the "Style something first — then share it" toast on a fully-rendered key-piece result. `_shareFindOrMake` returns the stored entry, or (id set but dropped from the local cache) a thin `{id, …}` so the DB PATCH still targets the cloud row, or (no id at all) lazily `snAdd`/`_mbAdd`s a reconstruction from the live render data (`window.__lastKpData` / `__lastDlData` / `__lastTvData` / `panel._currentData`) so share always has something to publish. `_shareBuild` strips non-http images so a lazy moodboard mint can't blow the localStorage quota.
- **Modal**: IG-handle capture (`@` input prefilled from `profiles.instagram_handle`; save PATCHes profiles + POSTs `/api/instagram` → Airtable Contacts), "Share my look" (native `navigator.share`, else copy), copy-link row.
- **Public page**: `GET /board/:shareId` (rate-limited) queries Supabase REST with the **anon key** — RLS (`lookbook_select_shared`, `to anon using (is_public = true)`) means only explicitly shared rows are readable. Server injects OG/Twitter meta (crawlers don't run JS) + a whitelist-sanitized payload (`publicSharePayload` — title/subtitle/images/pieces/tags/editorial only, never ids or emails) into `public/board.html` — one REST round trip, no app shell, so it loads fast. Missing/private rows → same template with a 404 not-found state. CTA links go to https://www.byrobes.com.
- `_lbRowToItem`/`_lbItemToRow` carry `share_id`/`is_public` as real columns so cloud pulls never orphan a published link. Share links use `window.location.origin`, so beta links live on beta.byrobes.com.

## Admin panel + capture plane (`/admin`, migration 11 — 2026-07-21)
Internal record panel for tracking beta users, per the admin architecture doc (ADR-1: custom in-repo page, not a SaaS; ADR-2: record-browsing before analytics). Environment-agnostic by design — beta + production share one Supabase project, so the same panel covers both; the `environment` column ('beta' | 'production') separates traffic (client: hostname is www.byrobes.com/byrobes.com → production, else beta; server: `PUBLIC_URL` contains www.byrobes.com → production, else beta).
- **`public/admin.html`** — protected page, same lifecycle as the other standalone pages (`sbClient`, session check). Gate = session AND `profiles.is_admin === true`; anything else redirects to `https://byrobes.com` (stable across the beta→production cutover, doesn't advertise the route). Screens (hash-routed): **Users** (via the `admin_user_overview` RPC — email + last sign-in come from auth.users, per-user wardrobe/lookbook/feedback counts + last event in one call; paginated 50/page + search; every column header click-sorts asc/desc server-side via the RPC's `p_sort`/`p_dir` — if the deployed function predates those params the panel falls back to sorting the visible page and says which SQL block to re-run), **User detail** (`#user/<uuid>` — profile/Style DNA summary, wardrobe + wishlist thumbnail grids, lookbook grid with `/board/:shareId` links on shared entries, feedback list, event timeline), **Generation health** (`generation_log` last 7 days: per-endpoint calls/ok/partial/failed/median latency/tokens, not-OK table, click a row → full prompt/response modal), **Feedback feed** (all users, newest first, paginated). Env filter select in the topbar (persisted `rb_admin_env`) applies to generation health + the detail event timeline. No service key in the browser — all reads ride the admin's own JWT through the `is_admin()` RLS policies; only the users list uses the security-definer RPC (PostgREST can't join auth.users or aggregate counts). Every screen degrades to an explanatory message until migration 11 runs.
- **`events` (client-written)**: `_rbTrack(type, meta)` in dashboard-personalize.js (also `window._rbTrack`), fire-and-forget POST with the user's JWT (own-insert RLS) via `_rbCapturePost` — which MUST send `Prefer: return=minimal`, never `_waFetch`: events/feedback are insert-only for ordinary users (select is admin-only), and `return=representation` triggers `INSERT…RETURNING`, whose returned row is checked against SELECT policies → the whole insert fails 42501 for every non-admin (the launch-day "feedback isn't logging" bug). Silent on failure. Vocabulary (specifics go in `metadata`): `prompt_submitted` (_cbResolve), `photo_attached` (#cb-file listener), `wardrobe_pick` (__cbWardrobeApply), `look_generated` (kp + dl fresh-save branches; meta.track 'key-piece'/'daily-3way'/'daily'), `weekly_generated`, `travel_generated` (wk/tv fresh-save branches), `day_restyled` (inside `_rbDayPost` — covers weekly + travel day restyles), `piece_swapped` (all four SwapApply fns, meta.surface), `wardrobe_added` (WA.submit, non-edit), `wishlist_added`, `share_created` (_shareEnsurePublic first publish only), `feedback_given` (inside _rbFbCloud).
- **`generation_log` (server-written)**: `server.js` wraps `ai.models.generateContent` ONCE (only when `SUPABASE_SERVICE_ROLE_KEY` is set) — an `AsyncLocalStorage` middleware on `/api` carries `{endpoint, userId}` into the background image loops, so every Gemini call (text + image, all endpoints) logs endpoint/model/latency/tokens/status + the FULL prompt (system instruction + text parts, inline images counted not stored) and response (parsed JSON for text models; `{has_image}` for the image model — never base64). Status: `ok` / `partial` (image call returned no frame — the "2 of 3 images" case) / `timeout` / `error`. `userId` rides in the generation POST bodies from the clients (dashboard-personalize `_waUid()`, onboarding/stylenotes `st.uid`) — absent → row logs with null user. Inserts go straight to Supabase REST with the service key, fire-and-forget. **Prompt → calls → artifact correlation (`gen_id`)**: each client generation submit mints `_rbGenId()` and sends it as `genId`; the ALS middleware copies it (+ the user's raw `prompt`/`brief`/`activity` as `user_prompt`, 400 chars) into every row's `detail` jsonb, and the same id is stamped into the saved lookbook entry's data (`kpData.genId`, `dlData/wkData/tvData/moodboard` via `data.genId` spread) — no schema change. The admin user-detail **Generations** section groups rows by `detail.gen_id`, shows her typed prompt, one clickable status chip per LLM call (→ full prompt/response modal), and the lookbook artifact carrying that gen_id (+ its /board link).
- **`feedback` (client-written)**: `_rbFbCloud(track, itemId, rating, note)` — rating 1=👍 0=👎 null=none, `lookbook_item_id` soft-references the artifact (`_kpActiveSaveId`/`_dlActiveSaveId`/`_wkActiveSaveId`/`_tvActiveSaveId`/`panel._savedId`). Wired into all five feedback submit paths (kp, shared dl/wk/tv, legacy sn, moodboard) ALONGSIDE the existing `/api/feedback` → Airtable post (kept for continuity).
- Route: `GET /admin` serves admin.html. `/api/health` gained `generation_log: true|false` (service key present) for verifying a deploy.
- Verified 2026-07-21 with a Playwright harness (Supabase shim): 23 checks green across all four screens incl. the prompt/response inspect modal, the gen_id-grouped Generations trail with artifact linking, and the lookbook-thumb extraction from the `data` jsonb.
- **Analytics stays bought, not built** (ADR-3): when funnels matter, add PostHog autocapture + session replay and mirror the events vocabulary with explicit captures; the in-DB tables keep owning outcomes/tokens/cost.

## URL routing (`window._rbNav`)
Lookbook, moodboards and wardrobe are `position:fixed` overlays, not real navigations, so the address bar needs to be driven manually. `window._rbNav(path)` calls `history.pushState` when the pathname differs; a single `popstate` listener closes whatever overlay is open and re-opens the one matching the new path (or clears the crumb for `/dashboard`). A `_rbRouting` guard stops the popstate handler's own view changes from re-triggering `_rbNav` pushes.
- A populated moodboard lives at `/moodboard/[slug]` (`_mbSlug(title)`; server serves dashboard.html for `/moodboard/:slug`). `_mbShowResult` pushes it, `__mbCloseResult` restores `/moodboards` or `/dashboard`, popstate + the boot deep-link (retries until `_lbCloudPull` lands) reopen the board by slug via `window._mbFindBySlug`.

## Dashboard wardrobe feature (signup-flow branch)
The dashboard was originally a ~4MB self-contained Claude Design bundle; it has been **ejected** into plain files:
- `public/dashboard.html` (~150KB) — plain HTML page. Head carries `window.__resources` (id → asset path map the app JS reads for runtime imagery) and a `#rb-boot` style that hides the body until boot completes.
- `public/dashboard-assets/` — 43 extracted assets (fonts, images, the 6 app JS files), named by their original bundle uuid. Fetched on demand and cached individually.
- `public/js/dashboard-personalize.js` — the entire `window.__robes_personalize` customisation layer.
- Boot sequence (inline script at end of body): load supabase-js CDN → auth guard (no session → `/signup.html`) → set `window.__robes_session`/`__robes_sb`/`__robes_profile` → load the 6 app scripts **sequentially in order** → call `__robes_personalize()` → remove `#rb-boot`. Do not reorder; the app scripts depend on session being set first and on each other's order.
- `window.__robes_sb` is the authenticated Supabase client — used by the avatar dropdown's Log out item (`#av-logout`: `signOut()` then redirect to `/signup.html?mode=signin`). Dropdown order: Account details / Style notes / Log out (the bundle's "My wardrobe" + "Style notes"→Lookbook items are removed in `__robes_personalize` — Wardrobe/Lookbook live in the nav now; Moodboards hidden in P0).

"The bundle" below refers to the minified app JS in `dashboard-assets/` — its functions are still private closures, so the whole interception layer is unchanged. All customisation runs via `window.__robes_personalize`, called after all bundle scripts execute.

### Wardrobe wiring (inside `__robes_personalize`)
- `_waUid()` / `_waToken()` — read `window.__robes_session` lazily on each call (session loads async after bundle auth)
- `_waFetch(method, path, body)` — direct Supabase REST with user JWT
- `_waLoad()` — fetches `wardrobe_items`, sets `_waLoaded = true`, rebuilds pills, re-renders grid
- `_waRender()` — builds grid from `_waItems`; shows "Loading…" until `_waLoaded` is true
- `_waBuildFilters()` — builds category pill buttons; skips rebuild if our pills (no `onclick` attr) already exist; marks current `_waCat` as active
- `_waSyncCounts()` — updates nav badge (`.nav-wbtn-count`), `#wg-count`, and the `.tracker-*` dashboard widget
- `_waObserver` — MutationObserver on `#wg-grid`: any time the bundle's `renderWardrobe()` overwrites the grid with mock data, we immediately restore real items. Disconnected during our own renders to avoid re-entrancy.
- `_waInit()` — polls every 250ms until `_waUid()` is truthy, then calls `_waLoad()`
- **Layout**: `.wardrobe-panel` sits on the centred shell (`max-width:var(--shell)` + `--s6` gutter, matching `.dash` — superseded the old 1148px cap, see the 2026-07-20 layout spec); under 768px `#wg-filters` becomes a single horizontal scroll row (nowrap, hidden scrollbar, `flex-shrink:0` pills) instead of wrapping into a cluttered block.
- **Pack-for-a-trip multi-select**: a "✈ Pack a trip" pill at the end of `#wg-filters` toggles `_wgPackMode` — cards then toggle selection (`_wgDecorate` outline + ✓ badge) instead of opening edit, and a floating dark bar (`#wg-packbar`, fixed bottom, z-60) shows the count + "Pack for a trip →" (enabled at ≥2) + Cancel. Go closes the panel and calls `__tvOpen({anchors: sel})` with the selection pre-checked in the Travel Edit shortlist browser. A MutationObserver on the panel's `.visible` class cancels select mode whenever the panel closes, so the bar can't outlive its grid. (Pack mode hides the grid-card stars — the pack ✓ occupies the same corner.)

### Wardrobe v2 internals (2026-07-14 — see the dated delta at the top for the UX summary)
- **State** (`var` on purpose, TDZ guard): `_waView` ('all'|'wishlist'), `_waV2Cols` (v2 columns exist; flipped false on PGRST204), `_wlItems`/`_wlLoaded`/`_wlTableMissing`, `_waRefine` `{seasons,colors,fits,worn,brand}`, `_waRefineOpen`. Constants `WA_SEASONS`, `WA_OCCASIONS`, `_WA_HERO_CAP=10`.
- **Setup** (`_waV2Setup` IIFE): injects `#rb-wa2-style` + into the static panel: `#rb-wsub` sub-tabs AFTER `.wg-header` (title leads — 2026-07-20 second pass), `.rb-wg-actions` (the wishlist-only `#rb-wg-cta-head` CTA, hidden on All pieces + relocated `#wg-count`) into the header, the `#rb-wa-fab` mobile FAB onto `document.body`, `#rb-hero` before `#wg-filters`, `#rb-refine` after it, `#rb-wl-grid` after `#wg-grid`. All view flips go through `_waV2Sync()` (called at the end of every `_waRender`); `__waSetView(v)` also sets crumb + `_rbNav('/wardrobe'|'/wishlist')`. **`#rb-wl-grid` needs `display:grid` inline when shown** — its stylesheet default is `none`, so clearing the inline style hides it.
- **Hero**: `_waHeroAll()` (sorted by `hero_position`), `_waHeroNextPos()`, `_waInSeasonNow(it)` / `_waItemSeasons(it)` (untagged ⇒ Year-round ⇒ always in season), `__waHeroToggle(id)` (optimistic PATCH + revert on error), `_waHeroRender()`, `__waHeroPicker()`/`_waHeroPickerPaint()`.
- **Refine**: `_waMatchRefine(it)` + `_waFilteredItems()` drive `_waRender`; `_waRefineRender()` rebuilds the drawer per interaction; `_waRefinePillSync()` keeps the pill badge (pill is always ink-dark with the funnel icon). Categories render as `.wg-tab` text tabs — active-class toggles target `.wg-tab` only, so the Refine/pack pills are naturally excluded. Colour = full palette + `.rb-sw-wheel` colour wheel (`__waRefWheel` nearest-palette mapping via `_waHexDist`, fires on `change` not `input` so drag-picking doesn't re-render mid-interaction).
- **Wishlist**: `_wlLoad()` rides along `_waLoad()`; `_WL_SRC` maps `source_type` → chip label/style; `__wlBought`/`__wlRemove` (shared `_rbConfirmDelete`)/`__wlOpenAdd`. Server route `GET /wishlist` serves the dashboard shell.
- **Detail expander**: `__rbWaDetailInject(itOrNull)` resets `window.__rbWaDetail` + mounts `#rb-wa-detail-host` (step 3 has the host in its markup; edit mode inserts before `#wa-cta`). `_rbWaDetailPaint()` re-renders on every chip toggle. The custom-tag input guards re-entrancy: Enter commits + repaints, and the orphaned input's blur must early-return on `!d.addingTag` (repainting mid-remove threw NotFoundError).
- **Verified 2026-07-14** with a Playwright harness (Supabase CDN shim + REST route stubs): 46 populated-path checks + 7 pre-migration degradation checks, all green, desktop + 390px mobile.

### Account details modal
All user-supplied/profile values injected into `acct-modal`'s template literal (`prof.first_name`, `userEmail`, etc.) must pass through `_acctEsc()` — the raw template literal previously leaked as literal `${userEmail}` text when values contained characters that broke the interpolation. `window.__saveAcctDetails()` PATCHes `profiles`, syncs `window.__robes_profile` + the visible avatar name/greeting, then closes the modal ~900ms after showing "Saved." (it used to stay open indefinitely after a successful save).

### `window.App` bridge (critical — `App` is a lexical `const`, not a window prop)
The main dashboard bundle declares its API as `const App = (function(){…})()` — a **global lexical binding**, so it's reachable by the bare name `App` from other classic scripts but is **`undefined` as `window.App`** (unlike `window.KP` / `window.WA`, which are real window props). Every `if (window.App && App.xxx)` guard in `__robes_personalize` therefore silently skipped — the wardrobe patches (which only survived via the `#wg-grid` MutationObserver), and the moodboard `App.openShare` / `App.packFromBoard` / `App.chooseInspire` / `App.saveRename` / wordmark-`App.goHome` patches (which just didn't apply — Share fell through to the bundle's mock "Wimbledon" sheet, etc.). Fix: the personalize body opens with `if (typeof App !== 'undefined' && App && !window.App) window.App = App;` so all those guards resolve. When adding a new `App.*` patch, keep the `window.App` guard — the bridge makes it valid.

### Bundle interception
The dashboard bundle has private functions (`renderWardrobe`, `showView`, etc.) that can't be patched directly. Instead:
- `App.showWardrobe` is patched to FIRST call `window.__rbCloseResultOverlays()` (drops the kp/dl/tv result pages, moodboard result + list and the lookbook page — they're fixed overlays ABOVE the in-flow panel, so without this the nav "Wardrobe" pill animated but the panel rendered invisibly underneath; beta bug 2026-07-10), then call original (for view switching), then immediately restore our grid + pills
- `App.filterWardrobe` is patched to update `_waCat` and call `_waRender()`
- `App.addPiece` is patched to no-op (prevents bundle's mock-data path after WA modal submit)
- `WA.submit` is replaced entirely — saves to Supabase, uploads photo to Cloudinary, then reloads grid
- `WA.open/close/onFile/pickColour/hover/validate` are kept from the bundle untouched

### Wardrobe photo upload
- Client: `WA.submit` reads data URL from `#wa-tile-img` (set by bundle's `WA.onFile` via `FileReader.readAsDataURL`)
- Detection: checks `#wa-tile.filled` class (added by bundle when photo is selected)
- Server: `POST /api/wardrobe/upload` — accepts `{ data: base64, mimeType }`, uploads to Cloudinary, returns `{ url }`
- Saved as `image_url` on the `wardrobe_items` row

### `/wardrobe` URL
`server.js` serves `dashboard.html` for `GET /wardrobe`. On load, `__robes_personalize` detects `pathname === '/wardrobe'` and calls `App.showWardrobe()` after 100ms.

### Wardrobe dashboard tracker widget (wardrobe-first redesign)
`section.tracker#wtrk` in dashboard.html — headline "The more you catalogue, *the more Robes can style.*" over a sage card. `_waSyncCounts()` fills: `#wtrk-num` (n / 15), `#wtrk-kicker` + `#wtrk-copy` (milestone copy via `_wtrkCopy(n)`), `#wtrk-fill` (progress bar), and `#wtrk-items` — the first 12 catalogued pieces as thumbnail tiles (click → `window.__wtrkEdit(id)` → edit modal), a "+N more" tile → `App.showWardrobe()`, and a dashed Add tile.
- **Copy is benefit-first (audit 2026-07-08)**: `_wtrkCopy(n)` leads with the reason to add the next piece, count trails as proof (e.g. "Add 11 more and Robes can dress you head to toe from your own closet — **4 of 15 catalogued**."). It no longer claims present-tense "styles only from what you own" below 15 (honest cold-start — see 2.1A).
- **Hidden ≥15 state (beta feedback 2026-07-10, supersedes the audit 2026-07-08 graduated strip)**: at the 15-piece threshold `_waSyncCounts` sets `display:none` on `#wtrk` — the module disappears from the homepage entirely to cut clutter. The section STAYS in the DOM (it's `_rbApplyLayout`'s anchor and `#rb-styled` glues after it) and reappears automatically if the count drops below 15. The old `wtrk-complete` class + `#rb-wtrk-style` CSS still toggle underneath (harmless). `#wtrk-cta` ("Add your first piece" / "Add pieces") and the `#wtrk-snap` "Catalogue what you're wearing now" fastest-add both open the wardrobe add modal directly — they do NOT navigate to `/wardrobe`. The section keeps the `.tracker` class (it's `_rbApplyLayout`'s anchor); `_rbApplyLayout` moves it above the moodboards/lookbook rows so the wardrobe leads the page, and re-glues `#rb-styled` (the onboarding card) after it.

### Dashboard v2 — Styling Concierge cards
The bundle ships with the **old** card order: `[Weekly Planner(01), Travel Edit(02), Key Piece(03)]`. `__robes_personalize` transforms this at runtime:
- Destructures as `const [weekly, travel, keyPiece] = svcs`
- Relabels `keyPiece` → "Daily outfit" (title + description + adds `.svc-daily` class, clears onclick)
- Applies inline SVG data URLs: `calSvg` → Weekly Planner image, `suitSvg` → Travel Edit image
- Rewires the Travel Edit card's onclick from the bundle's `KP.comingSoon` to `window.__tvOpen()` (the feature is live — see its section)
- Reorders DOM to `[keyPiece, weekly, travel]` and renumbers badges 01→02→03
- `_rbUpdateDailyOutfitLock()` then adds the lock pill overlay and manages the CTA state

**Critical**: do NOT use XML comments (`<!-- -->`) inside SVG data URLs — they break the URI encoding and cause the image to fail silently.

### Masthead greeting (`_rbUpdateMasthead`)
**One masthead for everyone (optimization 4.7)**: time-of-day greeting (`#dash-greet` — "Good morning/afternoon/evening, {name}.") + the static echo (`.dash-echo` — "What are we dressing for today?"). The earlier persona-aware "Welcome back, {name} · N pieces catalogued · style today, or add what's new since {weekday}" returner variant was **removed** — it read as clutter above the prompt. `_rbUpdateMasthead()` now only sets the greeting; the echo stays as its HTML default. Runs on personalize boot and at the end of `_waSyncCounts`. (`_rbIsReturner` / `rb_last_visit__<uid>` are now dead but left in place — harmless, and the `var` declarations avoid the TDZ trap noted in Common gotchas.)
- **Mobile above-the-fold (optimization 4.7)**: under 640px the greeting shrinks to 34px and `.concierge` margin tightens so the prompt box + suggestion pills lead the page.

### Daily Outfit progress pill (`_rbUpdateDailyOutfitLock`)
- Targets `.svc-daily` card (the relabelled Key Piece card after reorder). The card is **never locked** — the Daily Look track serves an editorial build below 15 items.
- Below 15 items: `.rb-lock-wrap` pill reads "✦ n more pieces unlock closet-only looks · n/15" (information, not a gate; audit 2026-07-08 rewrote it benefit-first from "Editorial until 15 pieces"); at 15 the pill is removed
- CTA is always "Style today →"; onclick always `_cbSetIntent('dress-me')` (prefills `#cb-ta`, scrolls/focuses)
- Called on every `_waLoad()` completion and wardrobe item add/delete (function name kept for history)

### 3-step wardrobe add flow
**See the 2026-08-05 add/edit redesign delta for the current flow** (one confirm screen, custom pickers, edit modal on the same form) — the step ownership, batch mechanics, `#wa-rb-file` id, `__waSawSubmit` bridge and `_origStepHTML` rules below still hold.
We own all 3 steps — the bundle's `.fm-step` content is replaced entirely on `WA.open` for new items.

- **Step 1** (`_showStep1`): our own photo capture UI (file input + drop zone). No bundle involvement. **The input is `multiple`** — see batch add below.
- **Step 2** (`_runStep2`): "Reading your piece…" spinner + progress text while `POST /api/wardrobe/analyse` runs.
- **Step 3** (`_runStep3`): "Here's what Robes saw." — since 2026-07-29 this is the onboarding-style REVEAL (photo with popped tags + label banner, "What Robes files" ledger filling row by row), with the old review form (extracted fields, colour swatches, silhouette pills, notes, progressive-capture expander) intact but collapsed behind "✎ Adjust the details" (`#rb-saw-edit` / `window.__rbSawExpand`). See the dated delta at the top.

**Batch add (`_waBatchQueue`/`_waBatchTotal`/`_waBatchDone`/`_waBatchAdvance`)**: step 1 accepts several photos at once; each saved piece pulls the next queued photo straight back into step 2 — the modal never closes between pieces. Steps 2/3 carry a "PIECE n OF m" tag (`_waBatchTag`), each save toasts "Piece n of m added", the last toasts "All m pieces in your wardrobe". Closing the modal (`WA.close`) clears the queue; a mid-batch Retake replaces the current photo and keeps the queue. Biggest cold-start lever — cataloguing 15 pieces becomes 2–3 passes, not 15 modal round-trips.

**Empty-label guard (`__waSawSubmit`)**: the bundle's `WA.submit` silently returns on an empty label, which used to freeze the CTA on "Saving…" forever. `__waSawSubmit` now catches a blank `#wa-saw-label` first — re-focuses it with an inline hint — and step-3 copy flips to "Robes couldn't quite read this one — give it a name" when the analyse came back label-less.

On step 3 submit (`window.__waSawSubmit`): restores `_origStepHTML` (bundle form) into the DOM, populates its hidden fields (`#wa-label-in`, `#wa-cat`, `#wa-brand`, `#wa-notes`, `#wa-sw-name`), then calls `WA.submit` after 50ms.

`_origStepHTML` is captured once on first `WA.open` call (add mode). It is **never** cleared on `WA.close` — preserving it means edit mode can always restore the bundle form even after an add flow.

Edit mode (`_waEditId !== null`): `WA.open` skips step 1 and instead restores `_origStepHTML` at 50ms if `#wa-label-in` is missing from DOM (handles the case where step 1 replaced it).

### Colour picker (`_ALL_SWATCHES`)
**Presentation superseded 2026-08-05**: colour renders as a single swatch + Change popover (`__waColourPop`, `_waSwatchBg`); `_buildSwatchRows`/`__rbPickSwatch`/`__rbInjectSwatches` are gone. The palette data below is unchanged and still the one source for the form, the popover and Refine.
Editorial tri-tier swatch system defined at IIFE scope.

- `_ALL_SWATCHES` — 21 swatches: Foundations (6) → Dimension Builders (7) → Exclamation Points (6) → Multi + Print
- `_buildSwatchRows(selectedColor)` — returns two-row HTML; the selected swatch gets outline + checkmark
- `window.__rbPickSwatch(el, name)` — global click handler; updates outline/checkmark on all swatch buttons, writes to `#wa-sw-name` and `window.__waSawColor`
- `window.__rbInjectSwatches(selectedColor)` — replaces `#wa-swatches` (bundle's swatch container) with our rows; used by edit modal. Also sets `#wa-sw-name` textContent so `WA.submit` reads the right colour
- Colour is submitted via `#wa-sw-name` textContent — `WA.submit` reads this directly
- `Multi` swatch: `conic-gradient(#FF1493,#FF4500,#E1FD2E,#00A86B,#0047AB,#4B0082,#FF1493)`
- `Print` swatch: diagonal SVG stripe on `#EDE8E0` background
- White swatch uses inset border shadow to show its edge
- Hover reveals colour name in `#rb-sw-label`; click also updates it (covers mobile)

### Gemini wardrobe analysis (`POST /api/wardrobe/analyse`)
Returns structured JSON used to populate step 3 and saved to `item_dna`:
```json
{
  "label": "...",
  "category": "...",
  "color": "...",
  "brand": "...",
  "notes": "...",
  "item_dna": {
    "display": {
      "title": "...",
      "editorial_color_name": "Washed Slate",
      "primary_color_hex": "#6B6B6B",
      "brand_raw": "..."
    },
    "structural_dna": {
      "silhouette_fit": ["Relaxed", "Single-breasted", "Unlined"]
    },
    "llm_styling_context": {},
    "ai_generated_notes": "..."
  }
}
```

Guardrail taxonomy enforced in prompt — `silhouette_fit` values map to controlled terms per category (Tops/Bottoms/Outerwear/Dresses/Shoes/Bags/Accessories). `maxOutputTokens` is 600 (originally 200 — raised to fit the richer response). Runs at `temperature: 0` + `thinkingConfig: { thinkingBudget: 0 }` — same truncation/nondeterminism trap as stylenotes analyse (see Common gotchas). On any Gemini error the response still resolves 200 with `analysisFailed: true` and empty fields (never blocks the caller); onboarding step 04's catalogue flow (`onboarding.html`) and the legacy dashboard handoff persist (`_rbOnboardHandoff` in `dashboard-personalize.js`) both check for `analysisFailed` or a missing `label` and retry the analysis once after a 1.5s pause before saving to `wardrobe_items` — a first-attempt truncation used to silently save the key piece with blank category/brand.

### item_dna JSONB column
- Added via `supabase/item_dna_migration.sql` (backward-compatible — existing rows get `{}`)
- GIN index: `idx_wardrobe_item_dna` for efficient JSONB queries
- `WA.submit` merges current colour + notes into `item_dna` before saving
- `window.__waSawItemDna` holds the in-flight item_dna object during the add/edit flow — always a deep copy

### Silhouette & Fit pills
- Rendered from `item_dna.structural_dna.silhouette_fit` (array of strings)
- Dismissible with × button — `window.__rbRemovePill(i)` splices the array and re-renders
- Shown in step 3 (add flow) and in the edit modal (loaded from saved `item_dna`)
- `window.__waSawFit` tracks the current pill state during the modal session

## App flow (modal path — primary)
1. **Landing** — email capture → `submitLandingEmail()` → Airtable `Contacts`
2. **Flow modal opens** → Step 1: prompt/photo input
3. `submitStyle()` fires `callStyle()` early (parallel with name entry), stores in `st.stylingPromise`
4. Step 2: name entry
5. Step 3: generating animation → `runGenModal()` picks up `st.stylingPromise`
6. Result → `saveLook()` hits `/api/look` → gets shareable ID → `go('result')`

## State object (`st` in app.js)
```js
{ name, email, pieceName, prompt, link, photo,
  photoUrl,          // Cloudinary URL for uploaded photo
  ways,              // array of 3 look objects from Gemini
  generatedImages,   // array of 3 base64 images from Gemini
  fallback,          // true when input wasn't recognisable as a fashion piece
  history,           // previous styling sessions (localStorage)
  stylingPromise,    // pre-fired callStyle() promise
  styleController,   // AbortController for in-flight /api/style fetch
  lookId,            // share URL id from /api/look
  resultLayout, shareIdx, idx }
```

## Key conventions
- **No framework, no build** — edit HTML/CSS/JS directly, changes are live on push
- **No comments** unless the WHY is non-obvious
- **Fallback piece**: `black Balmain waistcoat with gold buttons` — shown when Gemini can't identify a fashion item
- **Airtable logging**: every prompt auto-logged to `Feedback` table via `POST /api/look` (fires async after responding to client)
- **AbortController**: always abort `st.styleController` in `openModal()` and `closeModal()` to prevent orphaned server requests
- **Image fallback**: when a generated image is null (timeout/fail), show `.way-img-placeholder` div — never fall back to `st.photo`
- **`.reveal` sections**: use IntersectionObserver in `prepLanding()` to add `.in` class — they start `opacity:0`

## Airtable schema
**Contacts**: `Email`, `Name`, `Instagram Handle`, `Joined At`
**Feedback**: `Email`, `Prompt`, `Piece Link` (look URL), `Photo` (attachments), `Looks Output`, `Rating`, `User Feedback`, `Created At`

## Dashboard breadcrumb system (`_rbInitBreadcrumb`)

Injected as an IIFE inside `__robes_personalize`. Appends a `#rb-crumb` span to `.nav-l` (the left side of the main nav). The bundle's own `#nav-breadcrumb` is hidden on init.

### Finalized breadcrumb pattern
- **Wordmark** (`ROBES`) is always permanent — routes home, never replaced
- Trail is appended only on sub-pages:
  - `ROBES / Wardrobe` — when wardrobe panel is open
  - `ROBES / Style a piece` — when kpResultPage overlay is showing
  - `ROBES / {Title}` — moodboard opened from dashboard
  - `ROBES / Your Moodboards / {Title}` — moodboard opened from full list
  - `ROBES / Recent looks / Your look` — today's looks subtab
  - `ROBES / Pack a trip` — trip subtab
  - `ROBES / Plan the week` — week subtab

### API
- `window.rbSetCrumb(segments)` — segments is an array of `{ label, action? }`. Last segment is plain text; earlier ones are clickable buttons.
- `window.rbClearCrumb()` — hides crumb, restores wordmark display

### Wordmark onclick
Wired in `_rbInitBreadcrumb` to: clear crumb, close moodboard result + list, hide kp/dl/tv result pages, close wardrobe panel (via the nav wardrobe toggle button), then `_rbNav('/dashboard')` + `App.goHome()`. The toggle tracks its own open state, so when the panel was opened programmatically (direct `/wardrobe` load) the click can no-op — if the panel still has `.visible` after the toggle, the handler hard-routes with `window.location.assign('/dashboard')`.

### Wardrobe crumb (`_rbObserveWardrobe` IIFE)
MutationObserver on `.wardrobe-panel` watches for the `.visible` class being added/removed.
- **Wordmark visibility**: the bundle hides `#nav-wordmark` when showing subpages. Fix uses `wm.style.setProperty('display', 'inline', 'important')` plus a second MutationObserver on the wordmark itself that immediately restores it if the bundle re-hides it. Both are torn down when panel closes.
- `rbClearCrumb()` + `wm.style.removeProperty('display')` on panel close.

### Style a piece crumb (`kpResultPage`)
- `kpResultPage` is `z-index:40` — deliberately below the main nav's `z-index:50` so the nav sits on top of the overlay
- No inline nav inside kpResultPage — crumb lives in the real main nav with correct serif font
- `rbSetCrumb([{ label: 'Style a piece' }])` called when kpResultPage renders
- `rbClearCrumb()` called in `__kpGoBack()`

### Moodboard crumb (`_mbOpenedFromList` flag)
- `window._mbOpenedFromList` — set true when `__mbOpenSaved` is called while the list page is visible
- `_mbShowResult` reads this flag to decide between 1-segment and 2-segment crumb
- `__mbCloseResult` resets the flag and clears crumb

### Subtab crumbs (`_rbPatchSubtabs` IIFE)
Patches `App.setSubtab` to set/clear crumb based on which tab is active. Polls 250ms until `App.setSubtab` exists.

## Moodboard item interactivity & swap modal (PRD 3.B)

### Cold-Start Wardrobe Threshold
**Onboarding-to-retention audit (2026-07-08) — WAW rework**: the moodboard rail reward is no longer gated on the 15-item cold-start. The rail now shows the "✓ Yours" green badge + "In your wardrobe" pill + "Swap out" link on **any** genuine `wardrobe_match`, at any wardrobe count (the server matches by category at every count in `/api/moodboard`); only unmatched items show the circular Swap button + retailer/price pills. Rationale: every catalogued piece should earn visible proof it's being used from item #1, not stay hidden until 15 (the old `isColdStart = _waItems.length < 15` display gate has been removed from `_mbShowResult`).

The 15-item boundary still exists elsewhere as the styling posture threshold (see `styleDnaPromptBlock` State A/hybrid/B and the per-endpoint wardrobe-state directives), just not as a moodboard-reward gate.

### `the_look` Gemini schema additions
Two new fields added to every item in `the_look` array (in `server.js` system prompt):
- `retailer_hint` — best retailer for the piece (e.g. `"Net-a-Porter"`, `"ASOS"`)
- `price_point` — realistic EUR price (e.g. `"€89"`, `"€245"`)

`maxOutputTokens` raised from 4000 → 5000 to fit the richer output without truncation.

**`thinkingConfig: { thinkingBudget: 0 }` is mandatory on the `/api/moodboard` Gemini call** (added 2026-07-08). It was the one JSON endpoint missing it — `gemini-2.5-flash` counts thinking tokens inside `maxOutputTokens`, so on the large `the_look` JSON the response truncated mid-object and/or ran slow enough to fall through to `gemini-2.5-pro` and blow the gateway timeout, surfacing client-side as `Unexpected end of JSON input` in `__mbRunGeneration`. Never remove it. The client (`__mbRunGeneration`) also now wraps the success-path `res.json()` in try/catch so a cut-off body shows a clean "please try again" toast instead of a raw parse error.

### Gemini model chain (`server.js`)
Final working fallback chain (as of 2026-06-29):
```javascript
const MODELS = ['gemini-2.5-flash', 'gemini-2.5-pro'];
// timeout: 30000ms per model, 1 attempt per model
```
Removed deprecated models: `gemini-2.0-flash` (404), `gemini-2.5-flash-lite-preview-06-17` (404), `gemini-1.5-flash` (404). Never add these back.

### Rail rendering (`_mbShowResult`) — aligned with the Daily rack (amendments 2026-07)
The rail's eyebrow reads **"The rack"**. Each `.mbr-card` in `#mb-rail-pieces` now mirrors the Daily rack's content model: 44×44 thumbnail (owned photo or serif monogram) / name / brand / **one** provenance line — either a single `✓ In your wardrobe` (+ `Worn N×`) OR `retailer · price` (the old card showed the wardrobe tag twice — a "Yours" CTA pill AND an "In your wardrobe" pill; fixed). Below that an actions row: **flick-through** arrows (`__mbFlip(idx, dir)` — cycles the piece's owned same-category options, shown only when >1 option exists) + **Swap**. A dashed **`+ Add a piece`** button ends the list (`__mbAddPiece` → arms `_waAfterAdd` → opens `WA.open()`; the new owned piece is pushed onto `the_look` and joins the rack). `_mbOpts`/`_mbOptIdx` compute the flick set (option 0 = the served state snapshot in `item._orig`); `_mbFlip`/`__mbSwapApply`/`__mbAddPiece` all persist + re-render via `_mbCommitLook`. Note: swaps/flicks update the **rack** (and its thumbnails); the editorial **mosaic** collage is not per-piece imagery, so the big visual is unchanged by a swap (unlike the Daily board, whose tiles ARE the pieces). `.mbr-*` styles live in `dashboard.html`. The old rail footer — the "Manage your wardrobe" nudge + a "Shop all" link — was **removed** (redundant with `+ Add a piece` and the wardrobe surfaces); the Daily rack's per-card "Shop" CTA was removed for the same reason.

Pills (one set, chosen by state):
- **Wardrobe matched** (State B only): green "In your wardrobe" pill + "Worn N×" text — `times_worn` looked up from `_waItems.find(w => w.id === match.id)` at render time, NOT stored on `wardrobe_match`
- **Unmatched**: retailer pill (max-width 90px, ellipsis) + price pill

Sidebar subtitle: `"N pieces · M from your wardrobe"` when matches exist, else `"N pieces"`.

### Overflow fix for Swap buttons
Bundle ancestor elements have `overflow:hidden` set via inline styles that CSS `!important` overrides cannot beat. Fix: after building `railEl.innerHTML`, walk 8 ancestor elements and call `el.style.setProperty('overflow', 'visible', 'important')` on each. This must run on every re-render of the rail.

### Swap modal (`window.__mbSwap(idx)`)
Centered dialog (`position:fixed;inset:0;align-items:center;justify-content:center;padding:24px`), `z-index:950`, `border-radius:20px`, `max-width:480px`, `max-height:80vh`, overflow-y:auto.

Structure:
1. Header: "SWAP THIS PIECE" label + item name (serif) + italic brand/retailer
2. "FROM YOUR WARDROBE" — 4-col grid of `_waItems` filtered by matching category; tap a card to call `__mbSwapApply`
3. If no wardrobe items in category: AI alternative suggestion copy
4. Two side-by-side CTAs: **[Snap mine]** (calls `__mbSnapMine`) + **[Shop via Affiliate →]** (calls `window.__rbAffiliateSoon(modalId)` — closes the swap modal, then opens the bundle's Coming Soon dialog; same handler on the daily-look + travel swap modals)
5. "Opens [retailer] · [price]" centered text below CTAs

### `window.__mbSwapApply(lookIdx, wardrobeId)`
Mutates `window.__mbCurrentLook[lookIdx].wardrobe_match = { id, label, image_url, color }`, removes modal, re-renders rail, shows toast.

### `window.__mbSnapMine()`
Closes swap modal, opens `WA.open()` to add a new wardrobe item.

### `wardrobe_match` object
Stored on each `the_look` item after a swap. Shape: `{ id, label, image_url, color }`. Never stores `times_worn` — always look that up from `_waItems` at render time.

## Common gotchas
- Edit modal "Update piece" button: the bundle's `validate()` only enables `#wa-cta` when `#wa-label-in` is non-empty, and it last runs inside `open()` while the field is still blank. `_waOpenEdit` must re-run `WA.validate()` (or dispatch `input`) after populating the saved label, or the button stays greyed out.
- `#wa-sw-name` is a data-only carrier that `WA.submit` reads for the colour — `__rbInjectSwatches` sets its text **and** `display:none`, because the colour is shown in the injected `#rb-sw-label` header and the raw span otherwise leaks ("Black") as stray text under the silhouette pills in the edit modal.
- Moodboard mosaic on mobile (`@media(max-width:620px)`): the hero and the editorial-text tile each span the full width (`grid-column:1/-1`) so the long editorial copy can't stretch the hero into a giant block; the remaining images/`.mb-ph-cell` placeholders are forced square. The renderer sets inline `height:100%` on mosaic imgs, so the mobile rules use `height:auto !important` to win. Both mosaic render paths (`_mbShowResult` + `_mbPollImages` re-render) must give empty cells the `mb-ph-cell` class or they collapse on mobile.
- Moodboard rail: shop (unmatched) items have no photo — the thumbnail is a serif monogram of the item name's first letter on a cream tile, **not** a generic image-icon SVG (which reads as a broken/loading thumbnail).
- `paintProgress()` guards for missing `#nav-progress` — don't add back the null check removal
- `gemini-3.1-flash-image` is slow (~30s) and occasionally times out — 40s server timeout, images run in parallel
- `callStyle()` fires early from `submitStyle()` — don't move it later or users wait longer
- `persist()` strips `photo` and `generatedImages` from history when localStorage is full
- Look share page (`/look/:id`) serves `look.html` which fetches `/api/look/:id` — data lives in-memory, expires 48h
- Supabase client must be named `sbClient` not `supabase` to avoid conflict with `window.supabase` CDN global
- Dashboard `window.__robes_session` is set async by the bundle — always read uid/token via `_waUid()`/`_waToken()` helpers, never capture them once at init time
- Dashboard bundle's `renderWardrobe()` is a private closure — `App.renderWardrobe` does not exist; use MutationObserver on `#wg-grid` to defend against it
- `_waBuildFilters()` checks for existing non-onclick pills before rebuilding to avoid resetting active filter state on async reloads
- Dashboard bundle ships `[Weekly Planner, Travel Edit, Key Piece]` — always destructure in that order; the v2 layout reorders them at runtime
- SVG data URLs must not contain XML comments (`<!-- -->`) — they break URI encoding silently
- `_origStepHTML` must NOT be cleared on `WA.close` — it is captured once and reused across all subsequent edit opens
- Edit modal: bundle form is restored at 50ms (inside patched `WA.open`), field population at 60ms (inside `_waOpenEdit` setTimeout) — order matters
- `window.__waSawItemDna` is set by `_runStep3` (add flow) and `_waOpenEdit` (edit flow); always a deep copy so mutations don't affect `_waItems`
- `#wa-swatches` is the bundle's swatch container — `__rbInjectSwatches` replaces its innerHTML with our two-row layout
- `kpResultPage` z-index is 40 (below nav's 50) — main nav floats above the overlay. Never raise it above 50 or the crumb styling breaks
- Bundle hides `#nav-wordmark` when wardrobe opens — use `setProperty('display','inline','important')` + a second MutationObserver on the wordmark to defend against re-hiding; `removeProperty('display')` on close
- Wordmark onclick must close the wardrobe panel (`wp.classList.remove('visible')`) — without this, clicking ROBES clears the crumb but the panel stays open
- `_rbTimeAgo(iso)` must be declared as a named `function` declaration (not an expression) — if the declaration is lost, JS throws on the body's `return` statements and the whole dashboard fails to load
- `gemini-2.5-flash` **thinking tokens count inside `maxOutputTokens`** — structured-output endpoints returning large JSON must set `thinkingConfig: { thinkingBudget: 0 }` or the response truncates mid-JSON and the parse throws (this is why wardrobe analyse went 200 → 600 tokens, and why stylenotes analyse disables thinking)
- A Gemini account **out of credit/quota** surfaces as 502 `analysis_failed` from `/api/stylenotes/analyse` — check the `reason` field in the response body (browser console) or Railway logs before debugging code
- Vision-extraction endpoints that map to enums MUST set `temperature: 0` — at the default 1.0 the same photo samples different enum values across runs, which the deterministic engine then turns into different archetypes (looks like a mapping bug but is really nondeterministic extraction)
- The wardrobe category `<select>` (`#wa-cat` in dashboard.html) must always include an `Other` option matching the server's `category` fallback — without it, any item analysed/saved with `category: 'Other'` renders as a blank dropdown that looks broken
- `window.__robes_personalize`'s weather block (geolocation + Open-Meteo) must stay wrapped in its own IIFE with its own early returns — an early `return` written directly in the outer function body (e.g. `if (!weatherEl) return;`) silently aborts every wiring after it (wardrobe, lookbook, moodboards, breadcrumbs), which is invisible unless you check `typeof window.__kpRenderResult` after boot
- **Temporal-dead-zone trap in `__robes_personalize`**: any state a function reads must be initialised with `var` (or declared *above* the earliest caller), never a `let`/`const` further down the function body. `_waSyncCounts` runs early in boot and calls `_rbUpdateMasthead` → `_rbIsReturner`; the masthead's `_rbLastVisitTs`/`_rbPriorVisit` were originally `let` near the *end* of the function, so the early read threw `Cannot access '_rbPriorVisit' before initialization`, the boot `try/catch` fired, and every user got "Something went wrong loading your dashboard." The `if (typeof _rbUpdateMasthead === 'function')` guard does NOT protect you — hoisted function declarations are callable inside the TDZ; only the `let`/`const` *variables* they touch throw. It surfaced for all users because `_rbIsReturner` only reaches the TDZ var when `profiles.onboarded_at` is set, and the onboarding migration backfilled that for everyone (an empty-profile harness booted fine and hid the bug). Fix: declare such vars with `var` so they hoist to `undefined` (falsy → harmless early read), and let the real value land when execution reaches the assignment.
- Saved lookbook/moodboard entries never store base64 image data (`snAdd`/`_mbAdd`/`_kpPersistImages` only accept strings starting with `http`) — base64 blows the localStorage quota and can't sync to the `lookbook_items.data` jsonb column at any reasonable size
- Lookbook/moodboard localStorage keys (`SN_KEY()`/`MB_KEY()`) resolve the uid **lazily on every call** and return null (no-op) until the session lands — a captured-once uid fell back to `'anon'`, writing saves into a shared `robes_style_notes__anon` key that surfaced as phantom "15 key pieces" on other accounts in the same browser; `__anon` keys are purged at boot
- The moodboard panel overlay must stay at `z-index:40` (below the nav's 50, below the rename/share sheets' 200 and the Coming Soon dialog's 300) — raising it buries the avatar dropdown and every board modal, which reads as "dead CTAs"
- iOS input-focus auto-zoom: every page carries a snippet after the viewport meta that (iOS only) rewrites it with `maximum-scale=1` — Safari still allows pinch gestures, Android is untouched; don't add sub-16px font-size inputs and expect the meta alone to save you on other browsers
- Never put a `capture` attribute on file PICKER inputs — it forces straight-to-camera on mobile and hides the native picker (Photo Library / Take Photo / Choose File). The bundle's `pickCamera` helpers were rewritten to `removeAttribute('capture')` for the same reason. The ONE legitimate use is a dedicated "Take a photo" button with its own separate capture input beside the capture-less picker (`#wa-rb-cam` beside `#wa-rb-file` in add step 1, 2026-08-05) — the button then does exactly what it says
- `_kpActiveSaveId` / `panel._savedId` track which lookbook/moodboard row is "live" during a render so background image polling (`_kpPollImages`/`_mbPollImages`) patches the *saved* entry, not just the on-screen DOM — skip this wiring and reopening a look/board after generation finishes shows blanks again
- Supabase tables that are **insert-only for ordinary users** (admin-only SELECT, e.g. `events`/`feedback`) must be written with `Prefer: return=minimal` (`_rbCapturePost`), never `_waFetch` — `return=representation` makes PostgREST run `INSERT…RETURNING`, Postgres checks the returned row against SELECT policies, and the whole insert fails 42501 for every non-admin. Surfaced launch day as "feedback isn't logging"
